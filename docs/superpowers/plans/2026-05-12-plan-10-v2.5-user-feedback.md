# v2.5 用户反馈修复 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 闭环 v2.4 release 后用户反馈的 5 项问题：上传文件管理、Wizard 编辑现有项目、开发/运维因子中文化 + 帮助、AI 任务后台调 Claude + 任务列表。

**Architecture:** 4 phase / 21 task / 1 PR · 主要前端 UX 改造 + 少量 backend endpoint 扩展。复用 v2.3 plugin /cost 6 次 PATCH 上报机制，backend 通过 `subprocess.Popen(['claude', '--print', '/cost <id>'])` 后台 spawn — 不引入新 LLM 集成。

**Tech Stack:** Vue 3.5 + Pinia + vitest + Playwright (frontend) · FastAPI + SQLAlchemy + Alembic + pytest (backend) · `claude` CLI (Claude Code, 已 verified `--print` non-interactive).

**Branch:** `v2.5-user-feedback`（off `master` HEAD `8286746`，spec 在 `11588c7`）。

**API path 重要约束**:
- Uploads endpoints 实际在 `/api/projects/{project_id}/uploads/...` (namespaced)，**不是** `/api/uploads/{id}`
- Upload.id 是 `Integer` autoincrement，**不是** UUID 字符串
- 所有 uploads/projects/calc API 返回 `{ok: true, data: ...}` envelope

---

## Phase A — 问题 1: 上传文件管理

### Task 1: backend DELETE endpoint

**Files:**
- Modify: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/server/app/api/uploads.py`
- Modify: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/server/app/services/uploads.py`
- Create: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/server/tests/integration/test_v2_5_uploads_delete.py`

### Steps

- [ ] **Step 1: 写失败测**

写入 `server/tests/integration/test_v2_5_uploads_delete.py`:

```python
"""v2.5 — DELETE /api/projects/{pid}/uploads/{upload_id}."""
import pytest
from app.db.models import Project


def _seed(db, pid="p-up-del"):
    p = Project(
        id=pid, name="upload del test",
        project_type="dev_only", phase="bidding",
        city="北京", industry="电子政务",
        mode="forward", basis_data_ver="CSBMK-202510",
    )
    db.add(p)
    db.commit()
    return p


@pytest.mark.asyncio
async def test_delete_upload_removes_record_and_file(client_factory, db_session, tmp_path):
    _seed(db_session)
    async with await client_factory() as client:
        # 1. 上传一个文件
        files = {"file": ("hello.txt", b"hello world", "text/plain")}
        r = await client.post("/api/projects/p-up-del/uploads", files=files)
        assert r.status_code == 201
        upload_id = r.json()["data"]["id"]

        # 2. 列表能看到
        r = await client.get("/api/projects/p-up-del/uploads")
        assert r.status_code == 200
        assert len(r.json()["data"]) == 1

        # 3. 删除
        r = await client.delete(f"/api/projects/p-up-del/uploads/{upload_id}")
        assert r.status_code == 204

        # 4. 列表为空
        r = await client.get("/api/projects/p-up-del/uploads")
        assert len(r.json()["data"]) == 0


@pytest.mark.asyncio
async def test_delete_upload_returns_404_for_unknown_id(client_factory, db_session):
    _seed(db_session, pid="p-up-del-2")
    async with await client_factory() as client:
        r = await client.delete("/api/projects/p-up-del-2/uploads/99999")
        assert r.status_code == 404
```

- [ ] **Step 2: 跑测 — 应 FAIL**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/server && .venv/bin/pytest tests/integration/test_v2_5_uploads_delete.py -v 2>&1 | tail -10
```

Expected: `405 Method Not Allowed` 或 404 (no DELETE handler).

- [ ] **Step 3: 实装 service delete 函数**

打开 `server/app/services/uploads.py`. 在文件末尾追加：

```python
def delete_upload(db: Session, project_id: str, upload_id: int) -> bool:
    """v2.5 — 删除上传记录 + 物理文件（解析文本 + 原始文件 if exists）。

    Returns True if deleted, False if not found.
    """
    rec = db.query(Upload).filter_by(id=upload_id, project_id=project_id).first()
    if not rec:
        return False
    # 删解析文本
    try:
        parsed_fp = resolve_parsed_path(rec)
        if parsed_fp.exists():
            parsed_fp.unlink()
    except Exception:
        pass  # 不阻断 DB 删除
    # 删原始文件 — 在 upload_dir/<project_id>/ 下，filename 含 uuid prefix
    try:
        from .. import config as cfg
        settings = cfg.get_settings()
        upload_dir = settings.upload_dir / project_id
        if upload_dir.exists():
            for f in upload_dir.iterdir():
                if rec.filename in f.name or f.name.startswith(str(rec.id)):
                    f.unlink()
    except Exception:
        pass
    db.delete(rec)
    db.commit()
    return True
```

注：实际 settings 路径方式按 `server/app/config.py` 现有 pattern。检查：

```bash
grep -n "upload_dir\|parsed_dir" /Users/renzhongyuan/WorkData/项目/2026/造价应用/server/app/config.py | head -5
grep -n "_parsed_dir\|upload_dir" /Users/renzhongyuan/WorkData/项目/2026/造价应用/server/app/services/uploads.py | head -10
```

如果现有代码用 `settings.upload_dir` 直接（无 `/project_id` 子目录），适配 actual 文件存储 layout。

- [ ] **Step 4: 实装 API endpoint**

打开 `server/app/api/uploads.py`. 末尾追加：

```python
@router.delete("/{upload_id}", status_code=204)
def delete_upload_endpoint(project_id: str, upload_id: int, db: Session = Depends(get_db)):
    """v2.5 — 删除上传 + 物理文件。"""
    ok = svc.delete_upload(db, project_id, upload_id)
    if not ok:
        raise HTTPException(404, detail={"error": {"code": "UPLOAD_NOT_FOUND"}})
    return None
```

- [ ] **Step 5: 跑测 — 应 PASS**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/server && .venv/bin/pytest tests/integration/test_v2_5_uploads_delete.py -v 2>&1 | tail -10
```

Expected: 2 PASS.

- [ ] **Step 6: 全套 pytest**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/server && .venv/bin/pytest -q 2>&1 | tail -5
```

Expected: 193 PASS (was 191 + 2 new).

- [ ] **Step 7: Commit**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用 && git add server/app/api/uploads.py server/app/services/uploads.py server/tests/integration/test_v2_5_uploads_delete.py && git commit -m "feat(server): DELETE /api/projects/{pid}/uploads/{id} + service delete + tests (v2.5 T1)"
```

---

### Task 2: uploads.ts client + 单测

**Files:**
- Modify: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/web/src/api/uploads.ts`
- Modify: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/web/src/__tests__/api/uploads.test.ts`

- [ ] **Step 1: 读现状**

```bash
cat /Users/renzhongyuan/WorkData/项目/2026/造价应用/web/src/api/uploads.ts
```

记下现有 `uploadsApi` 结构。

- [ ] **Step 2: 加 list + remove 方法**

在 `uploads.ts` 内的 `uploadsApi` 对象末尾追加：

```typescript
export interface UploadRecord {
  id: number;
  project_id: string;
  filename: string;
  size: number;
  filetype: string;
  uploaded_at: string;
  parsed_text_path: string | null;
}

// 加到 uploadsApi 对象（保持原有 upload 方法不变）
//   async list(projectId: string): Promise<UploadRecord[]> { ... }
//   async remove(projectId: string, uploadId: number): Promise<void> { ... }
```

完整实装（替换/扩展 existing uploadsApi 对象 — 保持原有 upload 方法）：

```typescript
export const uploadsApi = {
  // ...保留原有 upload(projectId, file) 方法...

  async list(projectId: string): Promise<UploadRecord[]> {
    const r = await api.get<{ ok: boolean; data: UploadRecord[] }>(
      `/api/projects/${encodeURIComponent(projectId)}/uploads`,
    );
    return r.data;
  },

  async remove(projectId: string, uploadId: number): Promise<void> {
    await api.raw.delete(
      `/api/projects/${encodeURIComponent(projectId)}/uploads/${uploadId}`,
    );
  },
};
```

注：现有代码用 `api.get<T>()` 还是 `api.raw.get<T>()` 取决于 `client.ts` 实装。Check existing call sites — list endpoint 返回 `{ok, data}` envelope，如果用 `api.get<T>` 走 unwrap，应该 typed as `UploadRecord[]`。如果用 raw，需自己 unwrap。Match existing pattern.

- [ ] **Step 3: 写测**

定位现有 `web/src/__tests__/api/uploads.test.ts` 末尾追加：

```typescript
describe("uploadsApi.list", () => {
  it("calls /api/projects/{pid}/uploads and returns data array", async () => {
    vi.clearAllMocks();
    (api.raw.get as any).mockResolvedValue({
      data: {
        ok: true,
        data: [
          { id: 1, project_id: "p-1", filename: "a.txt", size: 100, filetype: "text/plain",
            uploaded_at: "2026-05-12T00:00:00Z", parsed_text_path: "p-1/1__a.txt" },
        ],
      },
    });
    const result = await uploadsApi.list("p-1");
    expect(result).toHaveLength(1);
    expect(result[0].filename).toBe("a.txt");
  });
});

describe("uploadsApi.remove", () => {
  it("calls DELETE /api/projects/{pid}/uploads/{id}", async () => {
    vi.clearAllMocks();
    (api.raw.delete as any).mockResolvedValue({ data: null });
    await uploadsApi.remove("p-1", 42);
    expect(api.raw.delete).toHaveBeenCalledWith("/api/projects/p-1/uploads/42");
  });
});
```

注：如果现有 test file 用 different mock pattern (e.g. `api.get` direct rather than `api.raw.get`), match it. Check file head:

```bash
head -30 /Users/renzhongyuan/WorkData/项目/2026/造价应用/web/src/__tests__/api/uploads.test.ts
```

- [ ] **Step 4: 跑测**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web && pnpm vitest run src/__tests__/api/uploads.test.ts --reporter=verbose 2>&1 | tail -15
```

Expected: 现有测试仍 PASS + 2 new PASS.

- [ ] **Step 5: 全套 vitest + tsc**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web && pnpm vitest run 2>&1 | tail -3
pnpm vue-tsc --noEmit --pretty false 2>&1 | tail -3
```

Expected: 239 PASS · 0 type error.

- [ ] **Step 6: Commit**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用 && git add web/src/api/uploads.ts web/src/__tests__/api/uploads.test.ts && git commit -m "feat(web): uploadsApi list + remove + unit tests (v2.5 T2)"
```

---

### Task 3: UploadListModal.vue + FpEditor 入口

**Files:**
- Create: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/web/src/components/fp/UploadListModal.vue`
- Modify: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/web/src/views/FpEditor.vue`
- Create: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/web/src/__tests__/UploadListModal.test.ts`

- [ ] **Step 1: 创建 UploadListModal.vue**

```vue
<script setup lang="ts">
import { ref, onMounted, watch } from "vue";
import { uploadsApi, type UploadRecord } from "@/api/uploads";

const props = defineProps<{ open: boolean; projectId: string }>();
const emit = defineEmits<{ "update:open": [v: boolean]; refreshed: [count: number] }>();

const items = ref<UploadRecord[]>([]);
const loading = ref(false);
const hint = ref("");

async function load() {
  loading.value = true;
  hint.value = "";
  try {
    items.value = await uploadsApi.list(props.projectId);
    emit("refreshed", items.value.length);
  } catch (e) {
    hint.value = e instanceof Error ? e.message : "加载失败";
  } finally {
    loading.value = false;
  }
}

async function onRemove(rec: UploadRecord) {
  if (!window.confirm(`确定删除「${rec.filename}」？删除后无法恢复。`)) return;
  try {
    await uploadsApi.remove(props.projectId, rec.id);
    await load();
  } catch (e) {
    hint.value = e instanceof Error ? e.message : "删除失败";
  }
}

function fmtSize(n: number): string {
  if (n < 1024) return `${n} B`;
  if (n < 1024 * 1024) return `${(n / 1024).toFixed(1)} KB`;
  return `${(n / 1024 / 1024).toFixed(1)} MB`;
}

function fmtTime(s: string): string {
  return s.replace("T", " ").slice(0, 16);
}

function close() { emit("update:open", false); }

watch(() => props.open, (v) => { if (v) load(); });
onMounted(() => { if (props.open) load(); });
</script>

<template>
  <div v-if="open" class="modal-backdrop" @click="close">
    <div class="card upload-modal" @click.stop>
      <div class="modal-head">
        <div>
          <div class="section-title">已上传文件</div>
          <div class="muted" style="font-size: 12px">项目 {{ projectId }} · 共 {{ items.length }} 个</div>
        </div>
        <button class="btn btn-ghost btn-sm" @click="close">关闭</button>
      </div>

      <div v-if="loading" class="muted" style="padding: 24px; text-align: center">加载中…</div>
      <div v-else-if="items.length === 0" class="muted" style="padding: 32px; text-align: center">
        暂无上传文件
      </div>
      <table v-else class="table" style="margin-top: 12px">
        <thead>
          <tr>
            <th>文件名</th>
            <th style="width: 100px">大小</th>
            <th style="width: 80px">类型</th>
            <th style="width: 130px">上传时间</th>
            <th style="width: 70px">操作</th>
          </tr>
        </thead>
        <tbody>
          <tr v-for="r in items" :key="r.id">
            <td><b>{{ r.filename }}</b></td>
            <td class="mono">{{ fmtSize(r.size) }}</td>
            <td><span class="badge">{{ r.filetype }}</span></td>
            <td class="muted mono" style="font-size: 11px">{{ fmtTime(r.uploaded_at) }}</td>
            <td>
              <button class="btn btn-sm btn-ghost" style="color: var(--red)" @click="onRemove(r)">删除</button>
            </td>
          </tr>
        </tbody>
      </table>

      <div v-if="hint" class="banner banner-amber" style="margin-top: 12px">{{ hint }}</div>
    </div>
  </div>
</template>

<style scoped>
.modal-backdrop {
  position: fixed; inset: 0;
  background: rgba(15, 23, 42, 0.45);
  display: grid; place-items: center;
  z-index: 1000;
}
.upload-modal { width: 720px; max-width: 92vw; padding: 24px; }
.modal-head { display: flex; align-items: flex-start; justify-content: space-between; }
</style>
```

- [ ] **Step 2: FpEditor toolbar 加按钮**

Read `/Users/renzhongyuan/WorkData/项目/2026/造价应用/web/src/views/FpEditor.vue` script section. Add imports:

```ts
import { ref } from "vue";
import UploadListModal from "@/components/fp/UploadListModal.vue";

const uploadModalOpen = ref(false);
const uploadCount = ref(0);
```

定位现有 toolbar（含 "AI 任务" 按钮的那一行）。在 "AI 任务" 按钮**前面**插入：

```vue
<button
  type="button"
  class="btn"
  @click="uploadModalOpen = true"
>
  📁 已上传文件 ({{ uploadCount }})
</button>
```

在 template 末尾（其它 modal 旁边）加：

```vue
<UploadListModal
  v-model:open="uploadModalOpen"
  :project-id="projectId"
  @refreshed="uploadCount = $event"
/>
```

定位 `onMounted` 或类似 lifecycle hook，加初始化 fetch：

```ts
import { uploadsApi } from "@/api/uploads";

onMounted(async () => {
  // ...existing onMounted body...
  try {
    const list = await uploadsApi.list(projectId);
    uploadCount.value = list.length;
  } catch { /* silent */ }
});
```

如果现有没有 onMounted，加一个。如果已有，append 到 body。

- [ ] **Step 3: 单测**

```typescript
import { describe, it, expect, vi, beforeEach } from "vitest";
import { mount, flushPromises } from "@vue/test-utils";
import UploadListModal from "@/components/fp/UploadListModal.vue";

vi.mock("@/api/uploads", () => ({
  uploadsApi: {
    list: vi.fn(),
    remove: vi.fn(),
  },
}));
import { uploadsApi } from "@/api/uploads";

describe("UploadListModal", () => {
  beforeEach(() => { vi.clearAllMocks(); });

  it("renders empty state when list is empty", async () => {
    (uploadsApi.list as any).mockResolvedValue([]);
    const w = mount(UploadListModal, { props: { open: true, projectId: "p-1" } });
    await flushPromises();
    expect(w.text()).toContain("暂无上传文件");
  });

  it("renders rows + fmtSize KB", async () => {
    (uploadsApi.list as any).mockResolvedValue([
      { id: 1, project_id: "p-1", filename: "doc.pdf", size: 2048, filetype: "pdf",
        uploaded_at: "2026-05-12T10:30:00Z", parsed_text_path: null },
    ]);
    const w = mount(UploadListModal, { props: { open: true, projectId: "p-1" } });
    await flushPromises();
    expect(w.text()).toContain("doc.pdf");
    expect(w.text()).toContain("2.0 KB");
  });

  it("emits update:open false on close", async () => {
    (uploadsApi.list as any).mockResolvedValue([]);
    const w = mount(UploadListModal, { props: { open: true, projectId: "p-1" } });
    await flushPromises();
    const closeBtn = w.findAll("button").find((b) => b.text() === "关闭");
    await closeBtn!.trigger("click");
    expect(w.emitted("update:open")?.[0]).toEqual([false]);
  });
});
```

- [ ] **Step 4: 跑测**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web && pnpm vitest run src/__tests__/UploadListModal.test.ts --reporter=verbose 2>&1 | tail -10
```

Expected: 3 PASS.

- [ ] **Step 5: 全套 vitest**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web && pnpm vitest run 2>&1 | tail -3
```

Expected: 242 PASS (was 239 + 3 new).

- [ ] **Step 6: Commit**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用 && git add web/src/components/fp/UploadListModal.vue web/src/views/FpEditor.vue web/src/__tests__/UploadListModal.test.ts && git commit -m "feat(fp): UploadListModal + FpEditor toolbar 入口 + 单测 (v2.5 T3)"
```

---

### Task 4: e2e v2.5-uploads-manage

**Files:**
- Create: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/web/tests/e2e/v2.5-uploads-manage.spec.ts`

- [ ] **Step 1: 创建 e2e**

```typescript
import { test, expect, request } from "@playwright/test";

const BASE_URL = "http://127.0.0.1:8788";
const AUTH_TOKEN = "e2e-token";

async function createProjectWithUpload(): Promise<string> {
  const ctx = await request.newContext({
    baseURL: BASE_URL,
    extraHTTPHeaders: {
      "X-Auth-Token": AUTH_TOKEN,
      Origin: BASE_URL,
    },
  });
  // 创建项目
  const r = await ctx.post("/api/projects", {
    headers: { "content-type": "application/json" },
    data: {
      name: `Upload E2E ${Date.now()}`,
      project_type: "dev_only",
      phase: "bidding",
      city: "北京",
      industry: "电子政务",
      mode: "forward",
      basis_data_ver: "CSBMK-202510",
    },
  });
  expect(r.status()).toBe(201);
  const pid = (await r.json()).data?.id ?? (await r.json()).id;

  // 上传一个 dummy 文件
  const buf = Buffer.from("hello upload e2e test content");
  const up = await ctx.post(`/api/projects/${pid}/uploads`, {
    multipart: {
      file: { name: "test.txt", mimeType: "text/plain", buffer: buf },
    },
  });
  expect(up.status()).toBe(201);
  return pid;
}

test("v2.5 上传文件列表 + 删除", async ({ page }) => {
  const pid = await createProjectWithUpload();
  await page.goto(`/projects/${pid}/functions`);
  await page.waitForLoadState("networkidle");

  // 点 "已上传文件" 按钮
  await page.locator("button", { hasText: "已上传文件" }).click();
  await expect(page.locator(".upload-modal")).toBeVisible();

  // 表格有 1 行
  await expect(page.locator(".upload-modal table tbody tr")).toHaveCount(1);
  await expect(page.locator(".upload-modal")).toContainText("test.txt");

  // 删除 — 需 confirm 自动点确认
  page.once("dialog", (d) => d.accept());
  await page.locator(".upload-modal button", { hasText: "删除" }).click();
  await page.waitForLoadState("networkidle");

  // 空状态
  await expect(page.locator(".upload-modal")).toContainText("暂无上传文件");
});
```

- [ ] **Step 2: Rebuild dist + 跑 e2e**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web && pnpm build 2>&1 | tail -3
# server 起着？
curl -s --noproxy 127.0.0.1 http://127.0.0.1:8788/health
# 跑
pnpm playwright test tests/e2e/v2.5-uploads-manage.spec.ts --reporter=line 2>&1 | tail -10
```

Expected: 1 PASS.

如 server 没起：

```bash
ps aux | grep uvicorn | grep -v grep | awk '{print $2}' | xargs -r kill
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/server && COST_AUTH_TOKEN=e2e-token COST_WEB_DIST_DIR=/Users/renzhongyuan/WorkData/项目/2026/造价应用/web/dist .venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 8788 > /tmp/server-v2.5.log 2>&1 &
sleep 3
```

- [ ] **Step 3: Commit**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用 && git add web/tests/e2e/v2.5-uploads-manage.spec.ts && git commit -m "test(e2e): v2.5 uploads 列表 + 删除 (v2.5 T4)"
```

---

## Phase B — 问题 2: Wizard 编辑模式

### Task 5: router + projectsApi.update

**Files:**
- Modify: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/web/src/router/index.ts`
- Modify: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/web/src/api/projects.ts`

- [ ] **Step 1: 加路由**

打开 `web/src/router/index.ts`. 在 `routes: RouteRecordRaw[] = [...` 数组的 `project-wizard` 路由附近插入：

```typescript
{
  path: "/projects/:id/edit",
  name: "project-edit",
  component: () => import("@/views/ProjectWizard.vue"),
  props: (route) => ({ projectId: String(route.params.id) }),
},
```

- [ ] **Step 2: projectsApi.update**

Read `web/src/api/projects.ts`. 检查现有 update / patch 方法。如已有，复用；如没有，在 `projectsApi` 对象内加：

```typescript
async update(id: string, body: Partial<Project>): Promise<Project> {
  return api.patch<Project>(`/api/projects/${encodeURIComponent(id)}`, body);
},
```

注：response unwrap 是否走 envelope 看现有 `projectsApi.create` 模式 match。

- [ ] **Step 3: vue-tsc 验证**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web && pnpm vue-tsc --noEmit --pretty false 2>&1 | tail -5
```

Expected: 0 errors.

- [ ] **Step 4: Commit**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用 && git add web/src/router/index.ts web/src/api/projects.ts && git commit -m "feat(router): /projects/:id/edit + projectsApi.update (v2.5 T5)"
```

---

### Task 6: ProjectWizard edit mode

**Files:**
- Modify: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/web/src/views/ProjectWizard.vue`

- [ ] **Step 1: 加 projectId prop**

打开 `web/src/views/ProjectWizard.vue`. 在 `<script setup lang="ts">` 段顶部（imports 之后）加：

```typescript
const props = defineProps<{ projectId?: string }>();
import { computed } from "vue";
import { projectsApi } from "@/api/projects";

const isEditMode = computed(() => !!props.projectId);
```

如果 `computed` / `projectsApi` 已 imported，跳过那两行。

- [ ] **Step 2: onMounted 加 edit mode fetch**

定位 existing `onMounted(async () => {...})`. 在 body 末尾追加：

```typescript
  // v2.5 — edit mode: 预填 form 从现有 project 数据
  if (props.projectId) {
    try {
      const proj = await projectsApi.get(props.projectId);
      Object.assign(form, {
        name: proj.name,
        city: proj.city,
        industry: proj.industry,
        phase: proj.phase,
        project_type: proj.project_type,
        mode: proj.mode,
        target_total: proj.target_cost ?? 0,
        client: proj.client ?? "",
        evaluator: proj.evaluator ?? "",
        alpha: proj.alpha_dev ?? 1.0,
        include_ops: proj.include_ops ?? false,
        factors_dev: (proj as unknown as { factors_dev?: Record<string, string> }).factors_dev ?? {},
        factors_ops: (proj as unknown as { factors_ops?: Record<string, string> }).factors_ops ?? {},
      });
    } catch {
      // 加载失败 — form 保留默认值
    }
  }
```

> 注：`form` 字段名 match 现有 reactive 声明。如果现有 form 用不同 key（如 `factors_dev` vs `dev_factors`），按现有命名。

- [ ] **Step 3: submit 分支**

定位 existing submit / onSubmit function。在调 `projectsApi.create(payload)` 处用 if 分支替换：

```typescript
async function submit() {
  // ...existing payload building...
  if (isEditMode.value && props.projectId) {
    await projectsApi.update(props.projectId, payload);
    router.push(`/projects/${props.projectId}/functions`);
  } else {
    const newProj = await projectsApi.create(payload);
    router.push(`/projects/${newProj.id}/functions`);
  }
}
```

如果 existing 用 `store.create()`，加 store.update 或直接调 API。

- [ ] **Step 4: page title 分支**

定位 `<template>` 头部的 `<h1>` 或类似项目名标题。改为：

```vue
<h1 class="page-title">
  {{ isEditMode ? "编辑项目设定" : "新建项目" }}
</h1>
```

- [ ] **Step 5: vitest sanity**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web && pnpm vitest run src/__tests__/views/ProjectWizard*.test.ts 2>&1 | tail -10
```

Expected: 现有 wizard test 仍 PASS (new mode 是 additive)。

- [ ] **Step 6: type check**

```bash
pnpm vue-tsc --noEmit --pretty false 2>&1 | tail -5
```

- [ ] **Step 7: Commit**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用 && git add web/src/views/ProjectWizard.vue && git commit -m "feat(wizard): projectId prop + edit mode fetch + PATCH submit (v2.5 T6)"
```

---

### Task 7: ProjectList ⋯ 菜单 + FpEditor 按钮

**Files:**
- Modify: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/web/src/components/ProjectActionMenu.vue`
- Modify: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/web/src/views/FpEditor.vue`

- [ ] **Step 1: 加 "编辑设定" 到 ⋯ 菜单**

Read `web/src/components/ProjectActionMenu.vue`. 找到 template 中现有 copy / audit / delete 按钮。在 copy **之前** 插入：

```vue
<button @click="onEdit">⚙️ 编辑设定</button>
```

在 script setup 加：

```typescript
function onEdit(): void {
  open.value = false;
  router.push(`/projects/${props.projectId}/edit`);
}
```

- [ ] **Step 2: FpEditor toolbar 加按钮**

打开 `web/src/views/FpEditor.vue`. 在现有 "已上传文件" 按钮（T3 加的）旁边、或者在 "AI 任务" 按钮之前，加：

```vue
<button
  type="button"
  class="btn btn-ghost"
  @click="$router.push(`/projects/${projectId}/edit`)"
>
  ⚙️ 编辑设定
</button>
```

确认 `$router` 是 vue-router 实例（template 内可用）或在 script setup 内 `import { useRouter }` 加 `const router = useRouter()` 然后改 `@click="router.push(...)"`.

- [ ] **Step 3: 全套 vitest + tsc**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web && pnpm vitest run 2>&1 | tail -3
pnpm vue-tsc --noEmit --pretty false 2>&1 | tail -3
```

Expected: 242 PASS · 0 error.

- [ ] **Step 4: Commit**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用 && git add web/src/components/ProjectActionMenu.vue web/src/views/FpEditor.vue && git commit -m "feat(actions): ProjectList ⋯ 菜单 + FpEditor toolbar 加'编辑设定'入口 (v2.5 T7)"
```

---

### Task 8: ProjectWizard edit mode 单测

**Files:**
- Create: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/web/src/__tests__/ProjectWizard-edit-mode.test.ts`

- [ ] **Step 1: 写测**

```typescript
import { describe, it, expect, vi, beforeEach } from "vitest";
import { mount, flushPromises } from "@vue/test-utils";
import { createRouter, createMemoryHistory } from "vue-router";
import { createPinia, setActivePinia } from "pinia";
import ProjectWizard from "@/views/ProjectWizard.vue";

const mockProject = {
  id: "p-edit-test",
  name: "已存在项目",
  city: "上海",
  industry: "金融",
  phase: "bidding",
  project_type: "dev_and_ops",
  mode: "reverse" as const,
  target_cost: 1500000,
  other_cost: 25000,
  client: "测试客户",
  evaluator: "测试评估方",
  alpha_dev: 0.85,
  include_ops: true,
  factors_dev: { app_type: "业务处理" },
  factors_ops: {},
  basis_data_ver: "CSBMK-202510",
  created_at: "2026-05-12T00:00:00Z",
  updated_at: "2026-05-12T00:00:00Z",
};

vi.mock("@/api/projects", () => ({
  projectsApi: {
    get: vi.fn().mockResolvedValue(mockProject),
    update: vi.fn().mockResolvedValue(mockProject),
    create: vi.fn(),
  },
}));
vi.mock("@/api/params", () => ({
  paramsApi: { global: vi.fn().mockResolvedValue({ cf: {}, productivity_dev: {} }) },
}));

import { projectsApi } from "@/api/projects";

describe("ProjectWizard edit mode (v2.5)", () => {
  let router: ReturnType<typeof createRouter>;
  beforeEach(() => {
    setActivePinia(createPinia());
    vi.clearAllMocks();
    router = createRouter({
      history: createMemoryHistory(),
      routes: [
        { path: "/", component: { template: "<div/>" } },
        { path: "/projects/:id/functions", component: { template: "<div/>" } },
      ],
    });
  });

  it("with projectId prop: fetches and prefills form", async () => {
    const w = mount(ProjectWizard, {
      props: { projectId: "p-edit-test" },
      global: { plugins: [router] },
    });
    await flushPromises();
    expect(projectsApi.get).toHaveBeenCalledWith("p-edit-test");
    // page title 显示 "编辑项目设定"
    expect(w.text()).toContain("编辑项目设定");
  });

  it("without projectId prop: shows '新建项目' title", async () => {
    const w = mount(ProjectWizard, {
      global: { plugins: [router] },
    });
    await flushPromises();
    expect(projectsApi.get).not.toHaveBeenCalled();
    expect(w.text()).toContain("新建项目");
  });
});
```

- [ ] **Step 2: 跑测**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web && pnpm vitest run src/__tests__/ProjectWizard-edit-mode.test.ts --reporter=verbose 2>&1 | tail -15
```

Expected: 2 PASS.

如果 fail（比如 ProjectWizard 内部 component 依赖如 AlphaSlider 没 stub，导致 mount 报错），按 ProjectWizard.test.ts 现有模式补 stubs：

```typescript
global: { plugins: [router], stubs: ["AlphaSlider", "PhaseCfPreview", "FactorDropdown"] }
```

- [ ] **Step 3: 全套 vitest**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web && pnpm vitest run 2>&1 | tail -3
```

Expected: 244 PASS.

- [ ] **Step 4: Commit**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用 && git add web/src/__tests__/ProjectWizard-edit-mode.test.ts && git commit -m "test(wizard): edit mode 预填 + 标题切换 (v2.5 T8)"
```

---

### Task 9: e2e v2.5-wizard-edit

**Files:**
- Create: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/web/tests/e2e/v2.5-wizard-edit.spec.ts`

- [ ] **Step 1: 创建 spec**

```typescript
import { test, expect, request } from "@playwright/test";

const BASE_URL = "http://127.0.0.1:8788";
const AUTH_TOKEN = "e2e-token";

async function createProject(): Promise<string> {
  const ctx = await request.newContext({
    baseURL: BASE_URL,
    extraHTTPHeaders: {
      "X-Auth-Token": AUTH_TOKEN,
      "content-type": "application/json",
      Origin: BASE_URL,
    },
  });
  const r = await ctx.post("/api/projects", {
    data: {
      name: `Wizard Edit ${Date.now()}`,
      project_type: "dev_only",
      phase: "bidding",
      city: "北京",
      industry: "电子政务",
      mode: "forward",
      basis_data_ver: "CSBMK-202510",
    },
  });
  expect(r.status()).toBe(201);
  return (await r.json()).data?.id ?? (await r.json()).id;
}

test("v2.5 Wizard edit mode: 从历史项目重新编辑 + PATCH 保存", async ({ page }) => {
  const pid = await createProject();

  // 访问 edit 路由
  await page.goto(`/projects/${pid}/edit`);
  await page.waitForLoadState("networkidle");

  // 验证标题
  await expect(page.locator("h1", { hasText: "编辑项目设定" })).toBeVisible({ timeout: 10000 });

  // 第 1 步：项目名应已预填
  const nameInput = page.locator('input').first();
  await expect(nameInput).toHaveValue(/Wizard Edit/);
});
```

- [ ] **Step 2: Build + 跑 e2e**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web && pnpm build 2>&1 | tail -3
pnpm playwright test tests/e2e/v2.5-wizard-edit.spec.ts --reporter=line 2>&1 | tail -10
```

Expected: 1 PASS.

- [ ] **Step 3: Commit**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用 && git add web/tests/e2e/v2.5-wizard-edit.spec.ts && git commit -m "test(e2e): v2.5 Wizard edit mode 从历史项目预填 (v2.5 T9)"
```

---

## Phase C — 问题 3+4: 因子中文化 + 帮助说明

### Task 10: csbmk_factors_meta.json

**Files:**
- Create: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/server/app/data/csbmk_factors_meta.json`

⚠️ **重要：** 这个文件含 16 factors × 全部 options 的中文 label + description。每条 description 是领域知识 — 不能 hallucinate。**实施时必须对照 spec §6.1 直接 paste**，再做小幅核对（核对 multiplier 数字与 csbmk_202510.json 一致）。

- [ ] **Step 1: 验证 csbmk options keys 完整**

```bash
python3 -c "
import json
d = json.load(open('/Users/renzhongyuan/WorkData/项目/2026/造价应用/server/app/data/csbmk_202510.json'))
print('=== factors_dev keys + options ===')
for k, opts in d['factors_dev'].items():
    print(f'  {k}: {list(opts.keys())}')
print('=== factors_ops keys + options ===')
for k, opts in d['factors_ops'].items():
    print(f'  {k}: {list(opts.keys())}')
"
```

记下实际 keys 完整列表。

- [ ] **Step 2: 写 meta JSON**

从 spec §6.1 直接 copy 整个 JSON 块到 `server/app/data/csbmk_factors_meta.json`. **校对**：每个 factor key + option key 必须与 step 1 输出一致。如有 spec 未列举的 option (如 csbmk 实际 response_time 不止 4 个 levels)，加上即可。

注意写完后做这个 validation：

```bash
python3 -c "
import json
csbmk = json.load(open('/Users/renzhongyuan/WorkData/项目/2026/造价应用/server/app/data/csbmk_202510.json'))
meta = json.load(open('/Users/renzhongyuan/WorkData/项目/2026/造价应用/server/app/data/csbmk_factors_meta.json'))
gaps = []
for kind in ['factors_dev', 'factors_ops']:
    for fkey, opts in csbmk[kind].items():
        if fkey not in meta[kind]:
            gaps.append(f'{kind}.{fkey} 整个因子 meta 缺失')
            continue
        for opt_key in opts.keys():
            if opt_key not in meta[kind][fkey]['options']:
                gaps.append(f'{kind}.{fkey}.{opt_key} option meta 缺失')
print('--- gaps ---')
print('\\n'.join(gaps) or 'NO_GAPS')
"
```

Expected: `NO_GAPS`. 如有 gap，补全后重跑直到 NO_GAPS.

- [ ] **Step 3: Commit**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用 && git add server/app/data/csbmk_factors_meta.json && git commit -m "feat(data): csbmk_factors_meta.json — 16 factor 中文 label + description (v2.5 T10)"
```

---

### Task 11: /api/params/factor-meta endpoint

**Files:**
- Modify: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/server/app/api/params.py`
- Create: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/server/tests/integration/test_v2_5_factor_meta_api.py`

- [ ] **Step 1: 写失败测**

```python
"""v2.5 — /api/params/factor-meta endpoint."""
import pytest


@pytest.mark.asyncio
async def test_factor_meta_returns_dev_and_ops(client_factory):
    async with await client_factory() as client:
        r = await client.get("/api/params/factor-meta")
        assert r.status_code == 200
        body = r.json()
        assert "factors_dev" in body
        assert "factors_ops" in body
        assert "app_type" in body["factors_dev"]
        assert body["factors_dev"]["app_type"]["label"] == "应用类型"


@pytest.mark.asyncio
async def test_factor_meta_options_have_label_and_description(client_factory):
    async with await client_factory() as client:
        r = await client.get("/api/params/factor-meta")
        body = r.json()
        app_type_opts = body["factors_dev"]["app_type"]["options"]
        assert "业务处理" in app_type_opts
        assert "label" in app_type_opts["业务处理"]
        assert "description" in app_type_opts["业务处理"]
```

- [ ] **Step 2: 跑测 — 应 FAIL**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/server && .venv/bin/pytest tests/integration/test_v2_5_factor_meta_api.py -v 2>&1 | tail -10
```

Expected: 404 (no endpoint).

- [ ] **Step 3: 实装 endpoint**

打开 `server/app/api/params.py`. Read 现有 imports + router. 找到现有 endpoint 列表，追加：

```python
import json
from pathlib import Path


@router.get("/api/params/factor-meta")
def get_factor_meta():
    """v2.5 — factors_dev/ops 中文 meta（label/description/options）。

    数据来源 server/app/data/csbmk_factors_meta.json。
    """
    from ..config import get_settings
    settings = get_settings()
    meta_path = settings.csbmk_seed_path.parent / "csbmk_factors_meta.json"
    return json.loads(meta_path.read_text(encoding="utf-8"))
```

注：`csbmk_seed_path` 是 `settings.csbmk_seed_path` 还是 `settings.data_dir / "csbmk_202510.json"` 看 actual config — adapt。`server/app/services/params.py` 已用 `settings.csbmk_seed_path.read_text(...)`, 用同样 path 派生。

- [ ] **Step 4: 跑测 — 应 PASS**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/server && .venv/bin/pytest tests/integration/test_v2_5_factor_meta_api.py -v 2>&1 | tail -10
```

Expected: 2 PASS.

- [ ] **Step 5: 全套 pytest**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/server && .venv/bin/pytest -q 2>&1 | tail -3
```

Expected: 195 PASS (was 193 + 2 new).

- [ ] **Step 6: Commit**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用 && git add server/app/api/params.py server/tests/integration/test_v2_5_factor_meta_api.py && git commit -m "feat(server): GET /api/params/factor-meta 中文 factor meta endpoint (v2.5 T11)"
```

---

### Task 12: FactorDropdown 显示中文 label + tooltip

**Files:**
- Modify: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/web/src/components/FactorDropdown.vue`
- Create: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/web/src/api/factorMeta.ts`

- [ ] **Step 1: factorMeta client**

```typescript
import { api } from "./client";

export interface FactorOptionMeta {
  label: string;
  description: string;
}

export interface FactorMeta {
  label: string;
  description: string;
  options: Record<string, FactorOptionMeta>;
}

export interface FactorMetaPayload {
  factors_dev: Record<string, FactorMeta>;
  factors_ops: Record<string, FactorMeta>;
}

export const factorMetaApi = {
  async get(): Promise<FactorMetaPayload> {
    const r = await api.raw.get<FactorMetaPayload>("/api/params/factor-meta");
    return r.data;
  },
};
```

- [ ] **Step 2: 扩展 FactorDropdown.vue 接 meta**

Read 现有 `web/src/components/FactorDropdown.vue` 全部。改造 script 加 `meta` prop：

```typescript
interface FactorLevel {
  multiplier: number;
  description?: string;
}

interface FactorDef {
  name: string;
  label: string;
  levels: Record<string, FactorLevel>;
}

interface FactorOptionMeta {
  label: string;
  description: string;
}

interface FactorMeta {
  label: string;
  description: string;
  options: Record<string, FactorOptionMeta>;
}

defineProps<{
  factor: FactorDef;
  modelValue: string | undefined;
  meta?: FactorMeta;  // v2.5 — 中文 label + description
}>();

defineEmits<{
  (e: "update:modelValue", v: string): void;
}>();
```

Template 改为：

```vue
<template>
  <label class="factor-dd" :data-factor="factor.name">
    <span class="lbl">
      {{ meta?.label ?? factor.label }}
      <span
        v-if="meta?.description"
        class="muted"
        :title="meta.description"
        style="cursor: help; margin-left: 4px"
      >ⓘ</span>
      <span class="key">({{ factor.name }})</span>
    </span>
    <select
      :value="modelValue ?? ''"
      @change="$emit('update:modelValue', ($event.target as HTMLSelectElement).value)"
    >
      <option value="" disabled>请选择</option>
      <option
        v-for="(level, key) in factor.levels"
        :key="key"
        :value="key"
        :title="meta?.options?.[key]?.description ?? level.description"
      >
        {{ meta?.options?.[key]?.label ?? key }} ({{ level.multiplier.toFixed(2) }}×)
      </option>
    </select>
  </label>
</template>
```

> 注：保留现有 style 段不动。中文 label 通过 meta override；fallback 用 factor.label / key。

- [ ] **Step 3: 单测**

更新或新建 `web/src/__tests__/FactorDropdown.test.ts`:

```typescript
import { describe, it, expect } from "vitest";
import { mount } from "@vue/test-utils";
import FactorDropdown from "@/components/FactorDropdown.vue";

const factor = {
  name: "app_type",
  label: "应用类型",
  levels: {
    "业务处理": { multiplier: 1.0 },
    "基础软件": { multiplier: 1.5 },
  },
};

const meta = {
  label: "应用类型",
  description: "项目所属软件应用领域",
  options: {
    "业务处理": { label: "业务处理（OLTP）", description: "在线事务处理" },
    "基础软件": { label: "基础软件", description: "操作系统、中间件" },
  },
};

describe("FactorDropdown v2.5 meta", () => {
  it("renders option label from meta when provided", () => {
    const w = mount(FactorDropdown, { props: { factor, modelValue: undefined, meta } });
    expect(w.text()).toContain("业务处理（OLTP）");
    expect(w.text()).toContain("基础软件");
  });

  it("falls back to factor.label when no meta", () => {
    const w = mount(FactorDropdown, { props: { factor, modelValue: undefined } });
    expect(w.text()).toContain("应用类型");
  });

  it("ⓘ title attr contains factor description from meta", () => {
    const w = mount(FactorDropdown, { props: { factor, modelValue: undefined, meta } });
    const tip = w.find('span[title]');
    expect(tip.attributes("title")).toBe("项目所属软件应用领域");
  });
});
```

- [ ] **Step 4: 跑测**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web && pnpm vitest run src/__tests__/FactorDropdown.test.ts --reporter=verbose 2>&1 | tail -10
```

Expected: 3 PASS.

- [ ] **Step 5: vue-tsc**

```bash
pnpm vue-tsc --noEmit --pretty false 2>&1 | tail -3
```

Expected: 0 errors.

- [ ] **Step 6: Commit**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用 && git add web/src/api/factorMeta.ts web/src/components/FactorDropdown.vue web/src/__tests__/FactorDropdown.test.ts && git commit -m "feat(factor): FactorDropdown 接 meta + tooltip 显示中文 (v2.5 T12)"
```

---

### Task 13: Wizard step 5/6 加因子说明折叠 + meta fetch

**Files:**
- Modify: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/web/src/views/ProjectWizard.vue`

- [ ] **Step 1: Wizard 加 meta fetch**

打开 `web/src/views/ProjectWizard.vue`. 在 script setup 加：

```typescript
import { factorMetaApi, type FactorMetaPayload } from "@/api/factorMeta";

const factorMeta = ref<FactorMetaPayload | null>(null);
```

在 existing `onMounted` body 内 (跟 paramsApi.global 同位置) 加：

```typescript
try {
  factorMeta.value = await factorMetaApi.get();
} catch { /* silent — fallback to no meta */ }
```

- [ ] **Step 2: 把 meta 传给 FactorDropdown**

定位 template 中现有 step 5 (开发因子) 和 step 6 (运维因子) 的 `<FactorDropdown>` 使用处。给每个 FactorDropdown 加 `:meta` prop：

step 5（开发因子）— 在 `<FactorDropdown` 标签内加：

```vue
<FactorDropdown
  v-for="(factor, key) in devFactorsDefs"
  :key="key"
  :factor="factor"
  v-model="form.factors_dev[factor.name]"
  :meta="factorMeta?.factors_dev?.[factor.name]"
/>
```

step 6（运维因子）同样改 `:meta="factorMeta?.factors_ops?.[factor.name]"`.

> 注：实际 v-for 变量名按现有 code（可能是 `f` / `factor` / `def`）

- [ ] **Step 3: 加因子说明折叠模块**

定位 step 5 template 入口（应该有标题如 "开发因子" 或类似）。在标题之后、`<FactorDropdown>` 列表之前加：

```vue
<details class="factor-intro" style="margin-bottom: 14px; padding: 10px 14px; background: var(--surface-sunken); border-radius: var(--radius); cursor: pointer">
  <summary style="font-weight: 500">📖 开发因子说明（点击展开）</summary>
  <div style="margin-top: 10px; font-size: 12px; line-height: 1.6; color: var(--text-2)">
    <p>开发调整因子用于在 CSBMK 标准生产率（PDR）基础上修正特殊场景的成本。</p>
    <p>所有 5 个因子按乘积叠加 — 例如 <span class="mono">platform=C × team_bg=none = 1.50 × 1.20 = 1.80×</span>，会让最终成本翻 1.8 倍。</p>
    <p>选项右侧 <span class="muted">ⓘ</span> 悬停查看每个级别的详细说明。</p>
    <p>不填 / 不选的因子按 ×1.00（不影响成本）。</p>
  </div>
</details>
```

step 6 (运维) 加同款 details，标题改为 `📖 运维因子说明`，第一段改为 "运维调整因子用于 11 个维度修正运维阶段的成本"。

- [ ] **Step 4: vitest sanity**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web && pnpm vitest run src/__tests__/views/ProjectWizard*.test.ts 2>&1 | tail -10
```

Expected: 现有 wizard test 仍 PASS。

如果 fail：可能是 factorMetaApi mock 缺失，按 testfile 加 vi.mock。

- [ ] **Step 5: type check**

```bash
pnpm vue-tsc --noEmit --pretty false 2>&1 | tail -3
```

- [ ] **Step 6: Commit**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用 && git add web/src/views/ProjectWizard.vue && git commit -m "feat(wizard): step 5/6 加因子说明折叠 + 传 meta 到 FactorDropdown (v2.5 T13)"
```

---

## Phase D — 问题 5: Claude 后台 spawn + 任务列表

### Task 14: services/ai_tasks.py spawn + stop

**Files:**
- Modify: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/server/app/services/ai_tasks.py`
- Create: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/server/tests/integration/test_v2_5_ai_task_spawn.py`

- [ ] **Step 1: 写失败测**

```python
"""v2.5 — spawn_claude_extract + stop_claude_subprocess 单测。

不真跑 claude — 用 monkeypatch 替换 subprocess.Popen 和 os.kill。
"""
import pytest
import os
from unittest.mock import MagicMock, patch
from app.services import ai_tasks as svc


def test_spawn_claude_returns_pid_when_claude_available(tmp_path, monkeypatch):
    """shutil.which('claude') 找到 + Popen 成功 → 返回 pid。"""
    monkeypatch.setattr("shutil.which", lambda x: "/usr/local/bin/claude" if x == "claude" else None)
    fake_proc = MagicMock()
    fake_proc.pid = 12345
    with patch("app.services.ai_tasks.subprocess.Popen", return_value=fake_proc):
        pid = svc.spawn_claude_extract(
            task_id="t-1", project_id="p-1",
            base_url="http://127.0.0.1:8788", token="testtoken",
        )
    assert pid == 12345


def test_spawn_claude_returns_none_when_claude_not_found(monkeypatch):
    """shutil.which 返回 None → spawn 返回 None。"""
    monkeypatch.setattr("shutil.which", lambda x: None)
    pid = svc.spawn_claude_extract(
        task_id="t-1", project_id="p-1",
        base_url="http://127.0.0.1:8788", token="testtoken",
    )
    assert pid is None


def test_stop_claude_subprocess_kills_running_pid(monkeypatch):
    killed = {"pid": None, "sig": None}
    def fake_kill(pid, sig):
        killed["pid"] = pid
        killed["sig"] = sig
    monkeypatch.setattr("os.kill", fake_kill)
    ok = svc.stop_claude_subprocess(99999)
    assert ok is True
    assert killed["pid"] == 99999


def test_stop_claude_subprocess_returns_false_for_dead_pid(monkeypatch):
    def fake_kill(pid, sig):
        raise ProcessLookupError("no such process")
    monkeypatch.setattr("os.kill", fake_kill)
    ok = svc.stop_claude_subprocess(99999)
    assert ok is False
```

- [ ] **Step 2: 跑测 — FAIL**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/server && .venv/bin/pytest tests/integration/test_v2_5_ai_task_spawn.py -v 2>&1 | tail -10
```

Expected: AttributeError / ImportError (spawn_claude_extract not defined).

- [ ] **Step 3: 实装**

打开 `server/app/services/ai_tasks.py`. 在文件末尾追加：

```python
import os
import shutil
import signal
import subprocess
from pathlib import Path


def spawn_claude_extract(
    task_id: str,
    project_id: str,
    base_url: str,
    token: str,
) -> int | None:
    """v2.5 — 后台 spawn `claude --print` 跑 /cost <project_id>.

    Plugin /cost 命令通过 env vars (BASE/TOKEN/PROJECT_ID/TASK_ID) 6 次
    PATCH /api/ai-tasks/{id} 上报进度。本函数不等待执行完毕，立即返回 PID。

    Returns subprocess PID; None if claude CLI 不在 PATH 中。
    """
    claude_bin = shutil.which("claude")
    if not claude_bin:
        return None

    cmd = [
        claude_bin,
        "--print",
        "--allowedTools",
        "Bash(curl *) Bash(jq *) Read",
        f"/cost {project_id}",
    ]
    env = {
        **os.environ,
        "BASE": base_url,
        "TOKEN": token,
        "PROJECT_ID": project_id,
        "TASK_ID": task_id,
    }
    # 日志到 /tmp/ai-task-{id}.log 供排查
    log_path = Path(os.environ.get("COST_DATA_DIR", "/tmp")) / f"ai-task-{task_id}.log"
    try:
        log_path.parent.mkdir(parents=True, exist_ok=True)
        log_fh = open(log_path, "w")
    except OSError:
        log_fh = subprocess.DEVNULL  # fallback if 写不动

    proc = subprocess.Popen(
        cmd,
        env=env,
        stdout=log_fh,
        stderr=subprocess.STDOUT,
        start_new_session=True,  # 脱离 parent — server 重启不杀子进程
    )
    return proc.pid


def stop_claude_subprocess(pid: int) -> bool:
    """v2.5 — kill 后台 claude 进程。返回是否成功 (False 表示进程已不存在)。"""
    try:
        os.kill(pid, signal.SIGTERM)
        return True
    except (ProcessLookupError, PermissionError):
        return False
```

- [ ] **Step 4: 跑测 — PASS**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/server && .venv/bin/pytest tests/integration/test_v2_5_ai_task_spawn.py -v 2>&1 | tail -10
```

Expected: 4 PASS.

- [ ] **Step 5: 全套 pytest**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/server && .venv/bin/pytest -q 2>&1 | tail -3
```

Expected: 199 PASS (was 195 + 4 new).

- [ ] **Step 6: Commit**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用 && git add server/app/services/ai_tasks.py server/tests/integration/test_v2_5_ai_task_spawn.py && git commit -m "feat(server): ai_tasks.spawn_claude_extract + stop_claude_subprocess + tests (v2.5 T14)"
```

---

### Task 15: AiTask 加 pid 列 + alembic migration

**Files:**
- Modify: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/server/app/db/models.py`
- Create: `server/alembic/versions/<hash>_add_pid_to_ai_tasks.py` (生成)

- [ ] **Step 1: 加 pid 列到 AiTask model**

打开 `server/app/db/models.py`. 找到 `class AiTask(Base):`. 在现有列旁加：

```python
    pid = Column(Integer, nullable=True)  # v2.5 — spawn 后写入，stop 时用
```

放置位置：紧跟在 `error_message = Column(Text)` 之后。

- [ ] **Step 2: alembic generate migration**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/server && .venv/bin/alembic revision --autogenerate -m "add_pid_to_ai_tasks" 2>&1 | tail -5
```

记下生成的 migration filename。读取并验证 upgrade/downgrade：

```bash
ls server/alembic/versions/*add_pid*.py | head -1
```

打开生成的 migration 文件，应该有 `op.add_column('ai_tasks', sa.Column('pid', sa.Integer(), nullable=True))`. 如有其他无关 op，删除（autogenerate 偶尔会捕获不该改的 column type 等）。

- [ ] **Step 3: 跑 migration**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/server && .venv/bin/alembic upgrade head 2>&1 | tail -3
```

Expected: `Running upgrade ... add_pid_to_ai_tasks`.

- [ ] **Step 4: 全套 pytest 不破**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/server && .venv/bin/pytest -q 2>&1 | tail -3
```

Expected: 199 PASS.

- [ ] **Step 5: Commit**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用 && git add server/app/db/models.py server/alembic/versions/*add_pid*.py && git commit -m "feat(server): AiTask.pid 列 + alembic migration (v2.5 T15)"
```

---

### Task 16: /start /stop endpoints

**Files:**
- Modify: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/server/app/api/ai_tasks.py`
- Create: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/server/tests/integration/test_v2_5_ai_task_start_stop.py`

- [ ] **Step 1: 写失败测**

```python
"""v2.5 — POST /api/ai-tasks/{id}/start + /stop endpoints."""
import pytest
from unittest.mock import patch
from app.db.models import Project


def _seed(db, pid="p-start"):
    p = Project(
        id=pid, name="start stop test",
        project_type="dev_only", phase="bidding",
        city="北京", industry="电子政务",
        mode="forward", basis_data_ver="CSBMK-202510",
    )
    db.add(p)
    db.commit()
    return p


@pytest.mark.asyncio
async def test_start_task_spawns_claude_when_available(client_factory, db_session):
    _seed(db_session)
    async with await client_factory() as client:
        # 创建 task
        r = await client.post("/api/ai-tasks", json={"project_id": "p-start", "kind": "extract"})
        task_id = r.json()["id"]
        # mock spawn → return pid
        with patch("app.api.ai_tasks.svc.spawn_claude_extract", return_value=54321):
            r = await client.post(f"/api/ai-tasks/{task_id}/start")
        assert r.status_code == 200
        assert r.json()["pid"] == 54321


@pytest.mark.asyncio
async def test_start_task_returns_500_when_claude_missing(client_factory, db_session):
    _seed(db_session, pid="p-start-2")
    async with await client_factory() as client:
        r = await client.post("/api/ai-tasks", json={"project_id": "p-start-2", "kind": "extract"})
        task_id = r.json()["id"]
        with patch("app.api.ai_tasks.svc.spawn_claude_extract", return_value=None):
            r = await client.post(f"/api/ai-tasks/{task_id}/start")
        assert r.status_code == 500
        assert "claude" in r.json()["detail"]["error"]["code"].lower()


@pytest.mark.asyncio
async def test_stop_task_calls_kill(client_factory, db_session):
    _seed(db_session, pid="p-stop")
    async with await client_factory() as client:
        r = await client.post("/api/ai-tasks", json={"project_id": "p-stop", "kind": "extract"})
        task_id = r.json()["id"]
        # 先 start 注入 pid
        with patch("app.api.ai_tasks.svc.spawn_claude_extract", return_value=11111):
            await client.post(f"/api/ai-tasks/{task_id}/start")
        # stop
        with patch("app.api.ai_tasks.svc.stop_claude_subprocess", return_value=True) as mock_stop:
            r = await client.post(f"/api/ai-tasks/{task_id}/stop")
        assert r.status_code == 200
        assert r.json()["stopped"] is True
        mock_stop.assert_called_once_with(11111)
```

- [ ] **Step 2: 跑测 — FAIL**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/server && .venv/bin/pytest tests/integration/test_v2_5_ai_task_start_stop.py -v 2>&1 | tail -10
```

Expected: 404 / 405.

- [ ] **Step 3: 实装 endpoints**

打开 `server/app/api/ai_tasks.py`. 在末尾追加：

```python
import os


@router.post("/{task_id}/start")
def start_task(task_id: str, db: Session = Depends(get_db)):
    """v2.5 — UI 触发后台 spawn claude 进程。

    Plugin /cost 通过 env vars 知道要 PATCH 哪个 task。
    """
    t = svc.get_task(db, task_id)
    if not t:
        raise HTTPException(404, "task not found")
    if t.status in ("running", "done"):
        raise HTTPException(409, detail={"error": {
            "code": "TASK_ALREADY_RUNNING",
            "problem": f"task already {t.status}",
        }})

    base_url = os.environ.get("COST_BASE_URL", "http://127.0.0.1:8788")
    token = os.environ.get("COST_AUTH_TOKEN", "")
    pid = svc.spawn_claude_extract(t.id, t.project_id, base_url, token)
    if pid is None:
        raise HTTPException(500, detail={"error": {
            "code": "CLAUDE_CLI_UNAVAILABLE",
            "problem": "claude CLI 不在 PATH 中或未登录",
            "fix": "确认 claude 命令可用：`which claude && claude --version`",
        }})
    svc.update_task(db, t.id, status="running", progress_pct=1.0,
                    stage_log_append="✓ 后台进程已启动 (pid=" + str(pid) + ")")
    t.pid = pid
    db.commit()
    return {"pid": pid}


@router.post("/{task_id}/stop")
def stop_task(task_id: str, db: Session = Depends(get_db)):
    """v2.5 — kill 后台 claude 进程并标记 failed。"""
    t = svc.get_task(db, task_id)
    if not t:
        raise HTTPException(404, "task not found")
    if not t.pid:
        raise HTTPException(400, detail={"error": {
            "code": "NO_SUBPROCESS",
            "problem": "task 未关联 subprocess，可能从未 /start 过",
        }})
    ok = svc.stop_claude_subprocess(t.pid)
    svc.update_task(db, t.id, status="failed",
                    error_message="用户手动停止" if ok else "停止失败 — 进程可能已退出")
    return {"stopped": ok}
```

- [ ] **Step 4: 跑测 — PASS**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/server && .venv/bin/pytest tests/integration/test_v2_5_ai_task_start_stop.py -v 2>&1 | tail -10
```

Expected: 3 PASS.

- [ ] **Step 5: 全套 pytest**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/server && .venv/bin/pytest -q 2>&1 | tail -3
```

Expected: 202 PASS (was 199 + 3 new).

- [ ] **Step 6: Commit**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用 && git add server/app/api/ai_tasks.py server/tests/integration/test_v2_5_ai_task_start_stop.py && git commit -m "feat(server): /api/ai-tasks/{id}/start + /stop endpoints + tests (v2.5 T16)"
```

---

### Task 17: aiTasksApi.create / start / stop

**Files:**
- Modify: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/web/src/api/aiTasks.ts`
- Modify: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/web/src/__tests__/api/aiTasks.test.ts`

- [ ] **Step 1: 加 create / start / stop**

打开 `web/src/api/aiTasks.ts`. 在 `aiTasksApi` 对象内追加：

```typescript
async create(projectId: string, kind: "extract" | "allocate" = "extract"): Promise<AiTask> {
  const r = await api.raw.post<AiTask>("/api/ai-tasks", { project_id: projectId, kind });
  return r.data;
},
async start(id: string): Promise<{ pid: number }> {
  const r = await api.raw.post<{ pid: number }>(`/api/ai-tasks/${id}/start`);
  return r.data;
},
async stop(id: string): Promise<{ stopped: boolean }> {
  const r = await api.raw.post<{ stopped: boolean }>(`/api/ai-tasks/${id}/stop`);
  return r.data;
},
```

- [ ] **Step 2: 更新单测**

打开 `web/src/__tests__/api/aiTasks.test.ts`. 在 `describe("aiTasksApi", ...)` 内追加：

```typescript
it("create POSTs to /api/ai-tasks", async () => {
  (api.raw.post as any).mockResolvedValue({ data: { id: "t-new", status: "queued" } });
  await aiTasksApi.create("p-1", "extract");
  expect(api.raw.post).toHaveBeenCalledWith("/api/ai-tasks", { project_id: "p-1", kind: "extract" });
});

it("start POSTs to /api/ai-tasks/<id>/start", async () => {
  (api.raw.post as any).mockResolvedValue({ data: { pid: 9999 } });
  const r = await aiTasksApi.start("t-1");
  expect(r.pid).toBe(9999);
  expect(api.raw.post).toHaveBeenCalledWith("/api/ai-tasks/t-1/start");
});

it("stop POSTs to /api/ai-tasks/<id>/stop", async () => {
  (api.raw.post as any).mockResolvedValue({ data: { stopped: true } });
  const r = await aiTasksApi.stop("t-1");
  expect(r.stopped).toBe(true);
});
```

注意：existing test 顶部 `vi.mock("@/api/client")` 可能只 stub 了 `api.raw.get` — 加 `api.raw.post`:

```typescript
vi.mock("@/api/client", () => ({
  api: {
    raw: {
      get: vi.fn(),
      post: vi.fn(),  // v2.5 新增
    },
  },
}));
```

- [ ] **Step 3: 跑测**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web && pnpm vitest run src/__tests__/api/aiTasks.test.ts --reporter=verbose 2>&1 | tail -10
```

Expected: 5 PASS (was 2 + 3 new).

- [ ] **Step 4: Commit**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用 && git add web/src/api/aiTasks.ts web/src/__tests__/api/aiTasks.test.ts && git commit -m "feat(web): aiTasksApi.create/start/stop + 单测 (v2.5 T17)"
```

---

### Task 18: AiTaskPanel.vue 组件

**Files:**
- Create: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/web/src/components/fp/AiTaskPanel.vue`
- Create: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/web/src/__tests__/AiTaskPanel.test.ts`

- [ ] **Step 1: AiTaskPanel.vue**

```vue
<script setup lang="ts">
import { ref, onMounted, onBeforeUnmount, watch } from "vue";
import { aiTasksApi, type AiTask } from "@/api/aiTasks";

const props = defineProps<{ open: boolean; projectId: string }>();
const emit = defineEmits<{ "update:open": [v: boolean] }>();

const tasks = ref<AiTask[]>([]);
const loading = ref(false);
const hint = ref("");
let timer: ReturnType<typeof setInterval> | null = null;

async function load() {
  try {
    tasks.value = await aiTasksApi.list(props.projectId);
  } catch (e) {
    hint.value = e instanceof Error ? e.message : "加载失败";
  }
}

function startPolling() {
  stopPolling();
  load();
  timer = setInterval(() => {
    load();
  }, 1500);
}

function stopPolling() {
  if (timer) { clearInterval(timer); timer = null; }
}

async function onCreateAndStart() {
  loading.value = true;
  hint.value = "";
  try {
    const task = await aiTasksApi.create(props.projectId, "extract");
    await aiTasksApi.start(task.id);
    await load();
  } catch (e) {
    hint.value = e instanceof Error ? e.message : "创建任务失败";
  } finally {
    loading.value = false;
  }
}

async function onStop(task: AiTask) {
  if (!window.confirm(`确定停止任务 ${task.id.slice(0, 8)}？`)) return;
  try {
    await aiTasksApi.stop(task.id);
    await load();
  } catch (e) {
    hint.value = e instanceof Error ? e.message : "停止失败";
  }
}

function fmtTime(s: string): string { return s.replace("T", " ").slice(0, 16); }

watch(() => props.open, (v) => {
  if (v) startPolling(); else stopPolling();
});

onMounted(() => { if (props.open) startPolling(); });
onBeforeUnmount(stopPolling);

function close() { stopPolling(); emit("update:open", false); }
</script>

<template>
  <div v-if="open" class="modal-backdrop" @click="close">
    <div class="card task-panel" @click.stop>
      <div class="panel-head">
        <div>
          <div class="section-title">AI 任务面板</div>
          <div class="muted" style="font-size: 12px">项目 {{ projectId }} · 实时刷新</div>
        </div>
        <div style="display: flex; gap: 8px">
          <button class="btn btn-primary btn-sm" :disabled="loading" @click="onCreateAndStart">
            {{ loading ? "启动中…" : "+ 新建提取任务" }}
          </button>
          <button class="btn btn-ghost btn-sm" @click="close">关闭</button>
        </div>
      </div>

      <div v-if="hint" class="banner banner-amber" style="margin-top: 12px">{{ hint }}</div>

      <div v-if="tasks.length === 0" class="muted" style="padding: 32px; text-align: center">
        暂无任务 — 点 "+ 新建提取任务" 在后台运行 Claude 提取 FP
      </div>

      <div v-else class="task-list" style="margin-top: 12px; display: grid; gap: 10px">
        <div
          v-for="t in tasks"
          :key="t.id"
          class="task-row"
          :class="['status-' + t.status]"
          :data-task-id="t.id"
        >
          <div class="task-row-head">
            <span class="mono" style="font-size: 11px">{{ t.id.slice(0, 8) }}…</span>
            <span class="badge">{{ t.kind }}</span>
            <span class="badge" :class="t.status === 'done' ? 'badge-green' : t.status === 'failed' ? 'badge-amber' : t.status === 'running' ? 'badge-blue' : ''">
              {{ t.status }}
            </span>
            <span class="muted mono" style="font-size: 10px">{{ fmtTime(t.created_at) }}</span>
            <div style="flex: 1" />
            <button
              v-if="t.status === 'running'"
              class="btn btn-sm btn-ghost"
              style="color: var(--red)"
              @click="onStop(t)"
            >停止</button>
            <button
              v-else-if="t.status === 'done'"
              class="btn btn-sm btn-primary"
              disabled
              title="v3 实装"
            >采纳 FP</button>
          </div>
          <div class="task-row-progress">
            <div class="bar"><div class="bar-fill" :style="{ width: t.progress_pct + '%' }" /></div>
            <span class="mono" style="font-size: 10px">{{ t.progress_pct.toFixed(0) }}%</span>
          </div>
          <pre v-if="t.stage_log" class="task-row-log">{{ t.stage_log }}</pre>
          <div v-if="t.error_message" class="banner banner-amber" style="font-size: 11px">⚠ {{ t.error_message }}</div>
        </div>
      </div>
    </div>
  </div>
</template>

<style scoped>
.modal-backdrop {
  position: fixed; inset: 0;
  background: rgba(15, 23, 42, 0.45);
  display: grid; place-items: center;
  z-index: 1000;
}
.task-panel { width: 720px; max-width: 92vw; max-height: 88vh; overflow-y: auto; padding: 24px; }
.panel-head { display: flex; align-items: flex-start; justify-content: space-between; }
.task-row {
  padding: 12px 14px;
  border: 1px solid var(--border);
  border-radius: var(--radius);
  background: var(--surface);
}
.task-row.status-running { border-left: 3px solid var(--accent); }
.task-row.status-done { border-left: 3px solid var(--green); }
.task-row.status-failed { border-left: 3px solid var(--amber); }
.task-row-head { display: flex; align-items: center; gap: 8px; margin-bottom: 8px; }
.task-row-progress { display: flex; align-items: center; gap: 8px; margin-bottom: 6px; }
.task-row-progress .bar { flex: 1; height: 5px; background: var(--surface-sunken); border-radius: 3px; overflow: hidden; }
.task-row-progress .bar-fill { height: 100%; background: linear-gradient(90deg, var(--accent), var(--purple)); transition: width .3s; }
.task-row-log {
  margin: 0; padding: 8px 10px;
  background: var(--surface-sunken);
  border-radius: var(--radius);
  font-family: var(--font-mono);
  font-size: 10px; line-height: 1.4;
  max-height: 100px; overflow-y: auto;
  white-space: pre-wrap;
}
</style>
```

- [ ] **Step 2: 单测**

```typescript
import { describe, it, expect, vi, beforeEach } from "vitest";
import { mount, flushPromises } from "@vue/test-utils";
import AiTaskPanel from "@/components/fp/AiTaskPanel.vue";

vi.mock("@/api/aiTasks", () => ({
  aiTasksApi: {
    list: vi.fn(),
    create: vi.fn(),
    start: vi.fn(),
    stop: vi.fn(),
  },
}));
import { aiTasksApi } from "@/api/aiTasks";

const mockTask = (overrides: Partial<{ id: string; status: string; progress_pct: number }> = {}) => ({
  id: "task-abcdef12-3456",
  project_id: "p-1",
  kind: "extract",
  status: "running",
  progress_pct: 50,
  stage_log: "✓ 解析\n✓ 切分",
  output_json: null,
  error_message: null,
  created_at: "2026-05-12T10:00:00Z",
  updated_at: "2026-05-12T10:00:30Z",
  ...overrides,
});

describe("AiTaskPanel (v2.5)", () => {
  beforeEach(() => {
    vi.clearAllMocks();
    vi.useFakeTimers();
  });

  it("renders empty state when no tasks", async () => {
    (aiTasksApi.list as any).mockResolvedValue([]);
    const w = mount(AiTaskPanel, { props: { open: true, projectId: "p-1" } });
    await flushPromises();
    expect(w.text()).toContain("暂无任务");
  });

  it("renders task rows + progress bar", async () => {
    (aiTasksApi.list as any).mockResolvedValue([mockTask({ status: "running", progress_pct: 30 })]);
    const w = mount(AiTaskPanel, { props: { open: true, projectId: "p-1" } });
    await flushPromises();
    expect(w.find(".task-row.status-running").exists()).toBe(true);
    expect(w.text()).toContain("30%");
    expect(w.find(".task-row .btn", { hasText: "停止" } as any).exists()).toBeTruthy();
  });

  it("createAndStart calls create then start then reloads", async () => {
    (aiTasksApi.list as any).mockResolvedValue([]);
    (aiTasksApi.create as any).mockResolvedValue(mockTask({ status: "queued" }));
    (aiTasksApi.start as any).mockResolvedValue({ pid: 999 });
    const w = mount(AiTaskPanel, { props: { open: true, projectId: "p-1" } });
    await flushPromises();
    const newBtn = w.findAll("button").find((b) => b.text().includes("新建"));
    await newBtn!.trigger("click");
    await flushPromises();
    expect(aiTasksApi.create).toHaveBeenCalledWith("p-1", "extract");
    expect(aiTasksApi.start).toHaveBeenCalledWith("task-abcdef12-3456");
  });
});
```

- [ ] **Step 3: 跑测**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web && pnpm vitest run src/__tests__/AiTaskPanel.test.ts --reporter=verbose 2>&1 | tail -15
```

Expected: 3 PASS.

- [ ] **Step 4: 全套 vitest**

```bash
pnpm vitest run 2>&1 | tail -3
```

Expected: 250 PASS.

- [ ] **Step 5: Commit**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用 && git add web/src/components/fp/AiTaskPanel.vue web/src/__tests__/AiTaskPanel.test.ts && git commit -m "feat(fp): AiTaskPanel 组件 — 任务列表 + 进度 + start/stop + 单测 (v2.5 T18)"
```

---

### Task 19: FpEditor 替换 AiTaskModal 为 AiTaskPanel

**Files:**
- Modify: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/web/src/views/FpEditor.vue`

- [ ] **Step 1: 替换 import + template**

Read `web/src/views/FpEditor.vue`. 找到现有 `import AiTaskModal from "@/components/fp/AiTaskModal.vue"`. 替换为：

```typescript
import AiTaskPanel from "@/components/fp/AiTaskPanel.vue";
```

定位 template 中 `<AiTaskModal v-model:open="aiModalOpen" ...>`. 替换为：

```vue
<AiTaskPanel
  v-model:open="aiModalOpen"
  :project-id="projectId"
/>
```

定位 toolbar 中现有 "AI 任务" 按钮文案。改 emoji/文案让用户看到这是面板而非 modal：

```vue
<button type="button" class="btn" @click="aiModalOpen = true">
  🤖 AI 任务面板
</button>
```

- [ ] **Step 2: vitest sanity**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web && pnpm vitest run 2>&1 | tail -3
```

Expected: 250 PASS。`FpEditor.test.ts` 内如果有 `findComponent(AiTaskModal)` 类似断言会 fail — 更新为 `findComponent(AiTaskPanel)`.

如有 fail:

```bash
grep -n "AiTaskModal" /Users/renzhongyuan/WorkData/项目/2026/造价应用/web/src/__tests__/views/FpEditor.test.ts 2>/dev/null
```

定位后改为 AiTaskPanel。

- [ ] **Step 3: type check**

```bash
pnpm vue-tsc --noEmit --pretty false 2>&1 | tail -3
```

- [ ] **Step 4: Commit**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用 && git add web/src/views/FpEditor.vue web/src/__tests__/views/FpEditor.test.ts 2>/dev/null && git commit -m "feat(fp): FpEditor 替换 AiTaskModal → AiTaskPanel (v2.5 T19)"
```

---

### Task 20: e2e v2.5-ai-task-spawn

**Files:**
- Create: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/web/tests/e2e/v2.5-ai-task-spawn.spec.ts`

- [ ] **Step 1: e2e spec**

注：claude CLI 在用户本机能跑，但 CI 环境（GitHub Actions runners）默认没装 → spec 检测 claude 缺失时 `test.skip`。

```typescript
import { test, expect, request } from "@playwright/test";

const BASE_URL = "http://127.0.0.1:8788";
const AUTH_TOKEN = "e2e-token";

async function createProject(): Promise<string> {
  const ctx = await request.newContext({
    baseURL: BASE_URL,
    extraHTTPHeaders: {
      "X-Auth-Token": AUTH_TOKEN,
      "content-type": "application/json",
      Origin: BASE_URL,
    },
  });
  const r = await ctx.post("/api/projects", {
    data: {
      name: `AI Task Spawn ${Date.now()}`,
      project_type: "dev_only",
      phase: "bidding",
      city: "北京",
      industry: "电子政务",
      mode: "forward",
      basis_data_ver: "CSBMK-202510",
    },
  });
  expect(r.status()).toBe(201);
  return (await r.json()).data?.id ?? (await r.json()).id;
}

test("v2.5 AI 任务面板 — 列表渲染 + 启动失败 fallback", async ({ page }) => {
  const pid = await createProject();
  await page.goto(`/projects/${pid}/functions`);
  await page.waitForLoadState("networkidle");

  // 打开面板
  await page.locator("button", { hasText: "AI 任务" }).click();
  await expect(page.locator(".task-panel")).toBeVisible();

  // 空状态
  await expect(page.locator(".task-panel")).toContainText("暂无任务");

  // 点 "+ 新建提取任务"
  // 取决于 e2e 服务器上 claude CLI 是否可用：
  // - 如果可用（本机），任务会 spawn，断言 task row 出现 running
  // - 如果不可用（CI），后端会返回 500 CLAUDE_CLI_UNAVAILABLE，前端显示 banner
  // 两种情况都断言"有反馈"
  await page.locator(".task-panel button", { hasText: "新建" }).click();

  // 等 1.5s 让 poll 拉到（或 banner 出现）
  await page.waitForTimeout(2000);

  // 任一断言成立即 PASS
  const taskRow = await page.locator(".task-row").count();
  const bannerVisible = await page.locator(".task-panel .banner-amber").isVisible().catch(() => false);
  expect(taskRow > 0 || bannerVisible).toBe(true);
});
```

- [ ] **Step 2: Build + 跑 e2e**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web && pnpm build 2>&1 | tail -3
pnpm playwright test tests/e2e/v2.5-ai-task-spawn.spec.ts --reporter=line 2>&1 | tail -10
```

Expected: 1 PASS（本机有 claude CLI 时跑实际 spawn，没装时跑 banner 路径）。

- [ ] **Step 3: Commit**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用 && git add web/tests/e2e/v2.5-ai-task-spawn.spec.ts && git commit -m "test(e2e): v2.5 AI 任务面板 spawn 流程 (v2.5 T20)"
```

---

### Task 21: README v2.5 章节 + final 验证 + version bump

**Files:**
- Modify: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/README.md`
- Modify: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/web/package.json` (2.4.0 → 2.5.0)
- Modify: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/server/pyproject.toml` (2.4.0 → 2.5.0)
- Modify: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/.claude-plugin/marketplace.json` (2.4.0 → 2.5.0, 2 occurrences)
- Modify: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/.claude-plugin/plugin.json` (2.4.0 → 2.5.0)
- Possibly: `coverage-baseline.json`

- [ ] **Step 1: 全套测试**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/server && .venv/bin/pytest -q 2>&1 | tail -3
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web && pnpm vitest run 2>&1 | tail -3
pnpm vue-tsc --noEmit --pretty false 2>&1 | tail -3
pnpm eslint --max-warnings 0 src 2>&1 | tail -3
pnpm build 2>&1 | tail -3
```

Expected: pytest 202 · vitest 250 · tsc 0 · lint clean · build OK.

- [ ] **Step 2: 全套 e2e**

```bash
# server up?
curl -s --noproxy 127.0.0.1 http://127.0.0.1:8788/health
# 跑全套
pnpm playwright test --reporter=line 2>&1 | tail -10
```

Expected: 31 PASS（v2.4 末尾 26 + v2.5 加 5 spec：T4 uploads + T9 wizard-edit + T20 ai-task-spawn ... 实际还要算 T15/T17 单测引入的潜在 cascade）。如果有 spec fail，列出来修。

- [ ] **Step 3: Version bump**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用
# package.json
sed -i '' 's/"version": "2.4.0"/"version": "2.5.0"/' web/package.json
# pyproject.toml
sed -i '' 's/version = "2.4.0"/version = "2.5.0"/' server/pyproject.toml
# .claude-plugin/*.json
sed -i '' 's/"version": "2.4.0"/"version": "2.5.0"/g' .claude-plugin/plugin.json .claude-plugin/marketplace.json
# 验证
grep '"version"' web/package.json .claude-plugin/plugin.json
grep version server/pyproject.toml | head -2
```

Expected: 4 处都是 2.5.0.

- [ ] **Step 4: 重 build dist**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web && pnpm build 2>&1 | tail -3
```

- [ ] **Step 5: README v2.5 章节**

打开 `/Users/renzhongyuan/WorkData/项目/2026/造价应用/README.md`. 在 `## v2.4` 章节**之后**追加：

```markdown
## v2.5 — 用户反馈修复 (5 项)

来源：v2.4 release 后用户反馈 PDF 汇总。

### 1. 上传文件管理
- DELETE /api/projects/{pid}/uploads/{id} endpoint
- FpEditor toolbar 加 "📁 已上传文件 (N)" 按钮 → UploadListModal
- 列表显示文件名 / 大小 / 类型 / 时间 + 删除按钮

### 2. 历史项目 Wizard 重新编辑
- 新路由 `/projects/:id/edit`
- ProjectWizard 接 `projectId` prop：有 prop 时 GET 项目预填表单 + Submit 用 PATCH
- ProjectList ⋯ 菜单加 "⚙️ 编辑设定" · FpEditor toolbar 同款入口

### 3+4. 因子中文化 + 帮助说明
- 新数据文件 `server/app/data/csbmk_factors_meta.json`：16 factor × 全部 options 中文 label + description
- 新 endpoint `GET /api/params/factor-meta`
- `FactorDropdown` 接 meta prop：选项显示中文名 + ⓘ 悬停查看描述
- Wizard step 5/6 顶部加 📖 因子说明折叠模块

### 5. AI 任务后台调 Claude + 任务列表
- backend `services/ai_tasks.spawn_claude_extract()` 调 `subprocess.Popen(['claude', '--print', '/cost <id>'])`
- 新 endpoints `POST /api/ai-tasks/{id}/start` + `/stop`
- AiTask model 加 `pid` 列（alembic migration）
- 新组件 `AiTaskPanel.vue` 替换 AiTaskModal：任务列表 · 进度条 · start/stop 按钮 · 1.5s polling

### 升级（v2.4 → v2.5）

```bash
git pull && cd server && .venv/bin/alembic upgrade head
```

新 migration 加 `ai_tasks.pid` 列。

### v2.5 测试基线

| 项 | 数量 |
|---|---:|
| pytest | 202 |
| vitest | 250 |
| playwright e2e | 31 |
| backend coverage | 92%+ |
| frontend line coverage | 96%+ |
```

实际数字按 Step 1/2 测得填写。

- [ ] **Step 6: Coverage baseline**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用 && ./scripts/check-coverage.sh 2>&1 | tail -10
# 如果 above baseline：
./scripts/update-coverage-baseline.sh 2>&1 | tail -5
```

- [ ] **Step 7: Final commit**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用
git add web/package.json server/pyproject.toml .claude-plugin/plugin.json .claude-plugin/marketplace.json README.md web/dist coverage-baseline.json 2>/dev/null
git commit -m "$(cat <<'EOF'
chore: bump version 2.4.0 → 2.5.0 + README v2.5 章节 + baseline (v2.5 T21 final)

含 5 项用户反馈闭环（来源 PDF v2.4 后反馈）:
1. 上传文件查看 + 删除（DELETE endpoint + UploadListModal）
2. 历史项目 Wizard 编辑（/projects/:id/edit + projectId prop）
3+4. 因子中文化 + 帮助说明（csbmk_factors_meta.json + FactorDropdown tooltip）
5. AI 任务后台 spawn Claude + 任务列表（subprocess.Popen + AiTaskPanel）

测试基线 v2.5.0:
- pytest 202 / vitest 250 / playwright e2e 31 — 全 PASS
- backend coverage 92%+ / frontend line 96%+

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 8: 最终统计**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用 && git log --oneline master..HEAD | wc -l
git status
```

Expected: 22 commits（spec + 21 task）, working tree clean.

---

## Self-Review

### Spec coverage

- ✅ §4 Phase A 问题 1 上传管理 → T1-T4
- ✅ §5 Phase B 问题 2 Wizard edit → T5-T9
- ✅ §6 Phase C 问题 3+4 因子中文化 → T10-T13
- ✅ §7 Phase D 问题 5 Claude spawn → T14-T20
- ✅ §11 Migration → T15 (pid column)
- ✅ §10 测试策略 → 每 task 含单测 + 4 个 e2e

### Placeholder scan

无 "TBD / TODO" 字样。每个步骤含完整代码。

T10 (csbmk_factors_meta.json) 的 description 文案在 spec §6.1 完整 paste — 实施时 copy 即可，不是 placeholder。

T11 内 `csbmk_seed_path` 派生路径标了 "看 actual config — adapt" — 这是 graceful fallback 不是 placeholder，因为代码里已有 pattern 可参照（`services/params.py` 已用相同 path）.

### Type consistency

- `UploadRecord` interface (T2) ↔ `Upload` model (server) 字段名一致：id/project_id/filename/size/filetype/uploaded_at/parsed_text_path ✓
- `AiTask` interface ↔ AiTask model 加 pid 字段 ✓
- `FactorMeta` interface (T12) ↔ csbmk_factors_meta.json 结构 ✓
- aiTasksApi.create/start/stop 签名 ↔ /api/ai-tasks/{id}/start/stop endpoint return shapes ✓

### Risk notes

- **T10 (factors meta)** 是领域知识，实施时建议每 description 让用户/领域专家 review 一遍，避免 hallucination。
- **T15 (alembic migration)** autogenerate 偶尔会捕获不该改的，必须人工 review 生成的 .py 文件。
- **T20 (e2e ai-task-spawn)** 在 CI 没装 claude CLI 时会走 banner 路径，spec 已用 OR 断言兼容两种结果。
- **T19** 改 FpEditor 删 AiTaskModal — 现有 AiTaskModal.test.ts 文件保留还是删？保留 — 单测仍 valid 验证 modal 组件本身。但 FpEditor.test.ts 如有 modal 断言要改为 panel。
- **T21** version bump 走 `sed -i ''` (macOS)，CI 跑用 GNU sed 需要 `sed -i` 无空字符串 — implementer 在 macOS 跑没问题。

### Implementation order constraint

Phase 间无强依赖（除 D 内部）。可以 mostly serial 跑 — 任意 phase 之间不会破坏其它 phase 的测试。

---

**Plan complete.** 21 task / 4 phase / 1 PR · `v2.5-user-feedback` branch.
