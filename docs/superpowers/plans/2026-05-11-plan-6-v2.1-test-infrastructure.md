# 软件造价系统 · Plan 6 · v2.1 测试基础设施升级 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修 v2.0 留存的 3 项测试可信度遗留：fixture race（25 integration fixture 切到 in-memory engine）+ e2e 扩到 13 个（覆盖 v2.0 全部新入口 + 错误路径）+ coverage baseline 防退化。

**Architecture:** Phase A 用 SQLAlchemy in-memory + StaticPool + FastAPI `app.dependency_overrides[get_db]` 替代 v2.0 的 `importlib.reload` race-prone 模式。Phase B 升级 coverage 报告格式（pytest-cov + vitest v8 已配置，加 json/html 输出）。Phase C 新增 10 个 Playwright spec 走真实浏览器。Phase D 加 baseline JSON + shell scripts 防覆盖率退化。

**Tech Stack:** pytest 8 / pytest-cov 4 / SQLAlchemy 2.0 StaticPool / FastAPI dependency_overrides / vitest v8 / Playwright

**对应 Spec：** `docs/superpowers/specs/2026-05-11-v2.1-test-infrastructure-design.md` (commit 0a5af03)
**Branch：** v2.1-test-infrastructure（已 commit spec）

---

## 现状盘点（实施前 baseline）

| 项 | 状态 |
|---|---|
| 25 个 integration fixture | 用 `importlib.reload` + on-disk SQLite，单文件隔离运行偶发 `Table 'projects' is already defined` |
| pytest-cov | **已装** (server/pyproject.toml dep + tool.coverage.run/report 配置，fail_under=80) |
| 前端 vitest v8 coverage | **已配** (web/vitest.config.ts，thresholds 80% 全 4 项) |
| Coverage baseline 实测 | backend **92.41%** / frontend **~94%** (远高于 spec 估的 87.4 / 74.2) |
| E2E | 3 spec：forward / reverse / v2-wizard-flow |
| baseline.json + check script | 不存在 |

---

## 文件结构

```
server/
├── tests/
│   ├── conftest.py                          # Task 1 (改造)
│   └── integration/
│       ├── test_v2_audit_log_api.py        # Task 4-5 (改造)
│       ├── test_v2_param_snapshots_api.py  # Task 4-5
│       ├── test_v2_project_copy.py         # Task 4-5
│       ├── test_v2_project_list_query.py   # Task 4-5
│       ├── test_v2_calc_uses_project_factors.py
│       ├── ... 其余 20 个 fixture           # Task 4-5
│       └── (新) test_isolation_smoke.py    # Task 6 (验证单文件隔离)
└── app/services/params.py                   # Task 2 (seed_from_csbmk 加 db 参数)

web/
├── vitest.config.ts                          # Task 7 (加 json-summary reporter)
├── .gitignore                                # Task 7 (加 coverage/)
└── tests/e2e/
    ├── forward.spec.ts                       # (existing)
    ├── reverse.spec.ts                       # (existing)
    ├── v2-wizard-flow.spec.ts                # (existing)
    ├── param-manager-6tab.spec.ts            # Task 8 (NEW)
    ├── audit-view.spec.ts                    # Task 9 (NEW)
    ├── project-copy.spec.ts                  # Task 9 (NEW)
    ├── reverse-allocator.spec.ts             # Task 10 (NEW)
    ├── wizard-dev-and-ops.spec.ts            # Task 11 (NEW)
    ├── reverse-validation.spec.ts            # Task 11 (NEW)
    ├── param-snapshot-restore.spec.ts        # Task 12 (NEW)
    ├── stale-result-banner.spec.ts           # Task 13 (NEW)
    ├── bulk-snapshot.spec.ts                 # Task 13 (NEW)
    └── audit-cursor-pagination.spec.ts       # Task 13 (NEW)

(项目根)
├── coverage-baseline.json                    # Task 14 (NEW)
├── scripts/
│   ├── check-coverage.sh                     # Task 14 (NEW)
│   ├── update-coverage-baseline.sh           # Task 14 (NEW)
│   └── _compare-coverage.py                  # Task 14 (NEW)
└── README.md                                 # Task 15 (加覆盖率验证章节)
```

---

# Phase A — Per-test Ephemeral Engine（fixture race 修复）

## Task 1: conftest.py 加 3 个 fixture

**Files:**
- Modify: `server/tests/conftest.py`

- [ ] **Step 1: 读现有 conftest.py 确认空间**

```bash
cat /Users/renzhongyuan/WorkData/项目/2026/造价应用/server/tests/conftest.py
```

预期：现有约 30 行，含 app / client fixture（lazy import）。新加的 fixture 放在文件末尾。

- [ ] **Step 2: 加 3 个新 fixture 到 conftest.py 末尾**

```python
# Append to server/tests/conftest.py

import asyncio
import os
from typing import Any, Iterator

from sqlalchemy import create_engine
from sqlalchemy.orm import Session, sessionmaker
from sqlalchemy.pool import StaticPool


@pytest.fixture
def db_engine() -> Iterator[Any]:
    """v2.1 — 每 test 一个 in-memory SQLite engine。

    StaticPool 让 fixture 创建的多个 session（如果有）共享同一连接，
    所以建在内存里的表对所有 session 可见。check_same_thread=False
    让 FastAPI 的异步 endpoint 在不同 thread 上访问也行。

    yield 后 dispose — 内存表跟着 GC，零 disk 残留。
    """
    from app.db.session import Base
    engine = create_engine(
        "sqlite:///:memory:",
        connect_args={"check_same_thread": False},
        poolclass=StaticPool,
    )
    Base.metadata.create_all(bind=engine)
    yield engine
    engine.dispose()


@pytest.fixture
def db_session(db_engine) -> Iterator[Session]:
    """每 test 一个 Session，绑到 db_engine。"""
    SessionLocal = sessionmaker(
        bind=db_engine, autoflush=False, autocommit=False,
    )
    session = SessionLocal()
    try:
        yield session
    finally:
        session.close()


@pytest.fixture
def client_factory(db_session, monkeypatch):
    """工厂函数 fixture — 返回一个 callable，用来构造 AsyncClient。

    用法（async test）::

        async def test_xxx(client_factory):
            async with await client_factory() as c:
                r = await c.get('/api/...')

    fixture 内部：注入 db_session 替代 FastAPI 的 get_db；按需 seed CSBMK；
    auth token 用 'test-secret-token-xyz'。
    """
    monkeypatch.setenv("COST_AUTH_TOKEN", "test-secret-token-xyz")

    async def _make(seed_csbmk: bool = True):
        from app.db.session import get_db
        from app.main import create_app
        from app.services import params as ps
        from httpx import ASGITransport, AsyncClient

        if seed_csbmk:
            ps.seed_from_csbmk(db=db_session)
            db_session.commit()

        app = create_app()
        app.dependency_overrides[get_db] = lambda: db_session
        transport = ASGITransport(app=app)
        return AsyncClient(transport=transport, base_url="http://test")

    return _make
```

- [ ] **Step 3: 跑现有全套 pytest 确认新 fixture 不破坏旧测试**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/server
.venv/bin/pytest -q 2>&1 | tail -3
```

Expected: 170 passed（仍是原 170，新 fixture 还没被任何 test 引用，纯加法）。

- [ ] **Step 4: Commit**

```bash
git add server/tests/conftest.py
git commit -m "feat(test): conftest 加 db_engine + db_session + client_factory fixture (v2.1 A1)"
```

---

## Task 2: seed_from_csbmk 加 db 可选参数

**Files:**
- Modify: `server/app/services/params.py`

- [ ] **Step 1: 读现有签名**

```bash
grep -A 20 "^def seed_from_csbmk" /Users/renzhongyuan/WorkData/项目/2026/造价应用/server/app/services/params.py | head -22
```

当前：`def seed_from_csbmk() -> None:` — 内部用 module-level `SessionLocal()`。

- [ ] **Step 2: 改签名 + 函数体**

把 server/app/services/params.py 第 31-50 行（`def seed_from_csbmk`）替换为：

```python
def seed_from_csbmk(db: Session | None = None) -> None:
    """从 CSBMK seed JSON 文件批量写 ParamGlobal 行。

    Args:
        db: 可选 Session。None 时用 module-level SessionLocal（v2.0 行为，
            兼容现有 bootstrap.py / api 调用方）；传入时复用调用方的 session
            （v2.1+ 测试用 in-memory engine 必须经此路径，否则 seed 写到
            另一个 disk engine 看不见）。
    """
    raw = json.loads(settings.csbmk_seed_path.read_text(encoding="utf-8"))
    version = raw.get("version", "CSBMK®-unknown")
    flat: dict = {}
    _flatten("", raw, flat)
    owns_session = db is None
    if owns_session:
        db = SessionLocal()
    try:
        existing = {p.key for p in db.query(ParamGlobal).all()}
        for k, v in flat.items():
            if k in existing:
                continue
            db.add(ParamGlobal(
                key=k,
                value=json.dumps(v, ensure_ascii=False),
                basis_version=version,
                modified=False,
            ))
        db.commit()
    finally:
        if owns_session:
            db.close()
```

- [ ] **Step 3: 验全套测试 + 验新签名可用**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/server
.venv/bin/pytest -q 2>&1 | tail -3
# 验新签名手动可调
.venv/bin/python -c "
import sys; sys.path.insert(0, '.')
from app.services.params import seed_from_csbmk
import inspect
sig = inspect.signature(seed_from_csbmk)
print('参数:', list(sig.parameters.keys()))
print('默认值:', sig.parameters['db'].default)
"
```

Expected: 170 passed；参数 `['db']`，默认 None。

- [ ] **Step 4: Commit**

```bash
git add server/app/services/params.py
git commit -m "feat(server): seed_from_csbmk 加 db 可选参数，兼容 in-memory engine (v2.1 A2)"
```

---

## Task 3: 改造前 6 个 v2 integration fixture (audit / snapshots / copy / list / factors / project_factors_column)

**Files (6 个 v2 测试文件)：**
- Modify: `server/tests/integration/test_v2_audit_log_api.py`
- Modify: `server/tests/integration/test_v2_audit_log_model.py`
- Modify: `server/tests/integration/test_v2_param_snapshots_api.py`
- Modify: `server/tests/integration/test_v2_param_snapshots_model.py`
- Modify: `server/tests/integration/test_v2_project_copy.py`
- Modify: `server/tests/integration/test_v2_project_list_query.py`

- [ ] **Step 1: 用统一模式改造每个文件**

对每个文件做相同操作：
1. 删除文件顶部的 local fixture（含 `importlib.reload` for-loop）
2. 删除文件顶部的 `H = {...}` 常量（统一放到 fixture 里）
3. 测试函数签名从 `async def test_xxx(client):` 改为 `async def test_xxx(client_factory):`
4. test 内部用 `async with await client_factory() as c:` 包住原 `client` 调用

**示例 — `test_v2_audit_log_api.py`** 现状（删除）：

```python
H = {"X-Auth-Token": "test-secret-token-xyz", "Origin": "http://127.0.0.1:8788"}


@pytest.fixture
async def client(monkeypatch, tmp_path):
    monkeypatch.setenv("COST_AUTH_TOKEN", "test-secret-token-xyz")
    monkeypatch.setenv("COST_DB_PATH", f"/tmp/cost-test-{uuid.uuid4()}.sqlite")
    monkeypatch.setenv("COST_DATA_DIR", str(tmp_path))
    import importlib
    for m in [...]:
        importlib.reload(importlib.import_module(m))
    from app.db.session import Base, engine
    Base.metadata.create_all(bind=engine)
    import app.services.params
    app.services.params.seed_from_csbmk()
    import app.main
    transport = ASGITransport(app=app.main.create_app())
    async with AsyncClient(transport=transport, base_url="http://test") as c:
        yield c
```

替换为（保留 H 常量在文件顶部供 test 使用）：

```python
H = {"X-Auth-Token": "test-secret-token-xyz", "Origin": "http://127.0.0.1:8788"}

# v2.1 — fixture 改用 conftest 的 client_factory（per-test in-memory engine）
```

Test 函数从：

```python
async def test_project_create_writes_audit_log(client):
    pid = await _make_project(client)
    rows = (await client.get(f"/api/projects/{pid}/audit", headers=H)).json()["data"]
    assert any(r["action"] == "project.create" for r in rows)
```

改为：

```python
async def test_project_create_writes_audit_log(client_factory):
    async with await client_factory() as client:
        pid = await _make_project(client)
        rows = (await client.get(f"/api/projects/{pid}/audit", headers=H)).json()["data"]
        assert any(r["action"] == "project.create" for r in rows)
```

helper `_make_project(client)` 函数不变（仍接 client 参数）。

- [ ] **Step 2: 改造 6 个文件**

对 6 个文件逐个 sed / 编辑。每个文件删除 local fixture，改 test 函数签名 + body。

- [ ] **Step 3: 跑 6 个文件验证**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/server
.venv/bin/pytest tests/integration/test_v2_audit_log_api.py tests/integration/test_v2_audit_log_model.py tests/integration/test_v2_param_snapshots_api.py tests/integration/test_v2_param_snapshots_model.py tests/integration/test_v2_project_copy.py tests/integration/test_v2_project_list_query.py -v 2>&1 | tail -15
```

Expected: 全部 PASS（v2 audit/snapshot/copy/list 共约 30 个测试）。

- [ ] **Step 4: 跑全套确认未破坏其他测试**

```bash
.venv/bin/pytest -q 2>&1 | tail -3
```

Expected: 170 passed。

- [ ] **Step 5: Commit**

```bash
git add server/tests/integration/test_v2_*.py
git commit -m "refactor(test): 6 个 v2 fixture 切到 client_factory (v2.1 A3)"
```

---

## Task 4: 改造剩余 v2 fixture (calc factors)

**Files:**
- Modify: `server/tests/integration/test_v2_calc_uses_project_factors.py`
- Modify: `server/tests/integration/test_v2_project_factors_column.py`

- [ ] **Step 1: 按 Task 3 同样模式改造这 2 个文件**

注意 `test_v2_project_factors_column.py` 用的是 sync `db` fixture（不是 async client），改造为：

```python
def test_project_has_factors_columns(db_session):
    from app.db.models import Project
    p = Project(
        id="p1", name="T", project_type="dev_only", phase="bidding",
        city="北京", industry="电子政务", mode="forward",
        basis_data_ver="CSBMK®-202510",
        factors_dev_json=json.dumps({"app_type": "OLTP"}),
        factors_ops_json=None,
    )
    db_session.add(p)
    db_session.commit()
    db_session.refresh(p)
    assert json.loads(p.factors_dev_json) == {"app_type": "OLTP"}
    assert p.factors_ops_json is None
```

直接用 conftest 的 `db_session` fixture，不用 client。

- [ ] **Step 2: 跑这 2 个文件 + 全套**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/server
.venv/bin/pytest tests/integration/test_v2_calc_uses_project_factors.py tests/integration/test_v2_project_factors_column.py -v 2>&1 | tail -10
.venv/bin/pytest -q 2>&1 | tail -3
```

Expected: 全部 PASS，170 passed。

- [ ] **Step 3: Commit**

```bash
git add server/tests/integration/test_v2_calc_uses_project_factors.py server/tests/integration/test_v2_project_factors_column.py
git commit -m "refactor(test): v2 calc/factors fixture 切到 db_session (v2.1 A4)"
```

---

## Task 5: 改造剩余 17 个非-v2 integration fixture

**Files (17 个):**
- `test_calc_api.py` / `test_calc_no_fps.py` / `test_config_paths.py` / `test_excel_injection.py` / `test_functions_api.py` / `test_functions_restore_api.py` / `test_input_finite_nonneg.py` / `test_params_api.py` / `test_params_effective_api.py` / `test_phase1_hardening.py` / `test_projects_api.py` / `test_projects_cascade_delete.py` / `test_reports_api.py` / `test_review_round_fixes.py` / `test_static_serving.py` / `test_uploads_no_collision.py` / `test_uploads_api.py`

- [ ] **Step 1: 批量改造**

对每个文件做与 Task 3 相同的改造模式。这是 mechanical search-replace，但需要逐个验证每个 fixture 的特殊行为是否需要保留（例如 `test_static_serving.py` 可能需要设 `COST_WEB_DIST_DIR`，`test_uploads_*` 需要 tmp_path 作为上传目录）。

**特殊保留**：
- `tmp_path` 仍可用（pytest builtin）— 上传 / 静态文件测试继续注 monkeypatch.setenv("COST_DATA_DIR", str(tmp_path))
- `COST_WEB_DIST_DIR` — `test_static_serving.py` 显式 monkeypatch.setenv
- `test_projects_cascade_delete.py` 测 FK cascade — 验证 in-memory engine 也走 PRAGMA foreign_keys=ON（已在 db/session.py 配置）

- [ ] **Step 2: 跑每个文件单独验证**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/server
for f in test_calc_api test_calc_no_fps test_functions_api test_params_api test_projects_api test_projects_cascade_delete test_uploads_api test_uploads_no_collision; do
  echo "=== $f ==="
  .venv/bin/pytest tests/integration/$f.py -q 2>&1 | tail -3
done
```

Expected: 每个文件全部 pass。

- [ ] **Step 3: 跑全套**

```bash
.venv/bin/pytest -q 2>&1 | tail -3
```

Expected: 170 passed。

- [ ] **Step 4: Commit**

```bash
git add server/tests/integration/test_*.py
git commit -m "refactor(test): 17 个 integration fixture 切到 client_factory (v2.1 A5)"
```

---

## Task 6: 单文件隔离运行验证 + 加 smoke test

**Files:**
- Create: `server/tests/integration/test_isolation_smoke.py`

- [ ] **Step 1: 单文件隔离运行 5 个之前易撞 race 的文件**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/server
for f in test_v2_audit_log_api test_v2_param_snapshots_api test_v2_project_copy test_v2_audit_log_model test_v2_calc_uses_project_factors; do
  echo "=== isolation: $f ==="
  .venv/bin/pytest tests/integration/$f.py -q 2>&1 | tail -3
done
```

Expected: 每个文件全部 pass（无 `Table 'projects' is already defined`）。

- [ ] **Step 2: 创建 isolation smoke test**

`server/tests/integration/test_isolation_smoke.py`：

```python
"""v2.1 A6 — isolation regression test.

v2.0 → v2.1 修复前：单文件运行某些 v2 integration test 报
`Table 'projects' is already defined for this MetaData instance`。
原因是 fixture 用 `importlib.reload`，conftest 之前 import 的旧 metadata
里已有 projects 表，reload 后 class Project(Base) 又试图注册到同一 metadata。

修复后用 per-test in-memory engine + StaticPool，每 test 独立 metadata，
不再共享 disk DB。这个 smoke test 锁定回归：连续 3 个 test 都建项目，
确认 metadata 隔离 + cascade delete 工作。
"""
import pytest


pytestmark = pytest.mark.asyncio

H = {"X-Auth-Token": "test-secret-token-xyz", "Origin": "http://127.0.0.1:8788"}


async def _create(c, name: str) -> str:
    r = await c.post("/api/projects", headers=H, json={
        "name": name, "project_type": "dev_only", "phase": "bidding",
        "city": "北京", "industry": "电子政务", "mode": "forward",
        "basis_data_ver": "CSBMK®-202510",
    })
    assert r.status_code == 201
    return r.json()["data"]["id"]


async def test_isolation_first_engine_clean(client_factory):
    """第一个 test 建项目，确认 in-memory engine 全新。"""
    async with await client_factory() as c:
        before = (await c.get("/api/projects", headers=H)).json()
        assert before["meta"]["total"] == 0
        pid = await _create(c, "isolation-1")
        after = (await c.get("/api/projects", headers=H)).json()
        assert after["meta"]["total"] == 1
        assert after["data"][0]["id"] == pid


async def test_isolation_second_engine_clean(client_factory):
    """第二个 test 也建项目，但拿到的是 fresh engine（看不到 test 1 的数据）。"""
    async with await client_factory() as c:
        before = (await c.get("/api/projects", headers=H)).json()
        # 关键 assertion：第二个 test 看到 0 个项目，证明 engine 隔离
        assert before["meta"]["total"] == 0
        pid = await _create(c, "isolation-2")
        assert pid is not None


async def test_isolation_third_engine_clean(client_factory):
    """第三个 test 同理 — fresh engine。"""
    async with await client_factory() as c:
        before = (await c.get("/api/projects", headers=H)).json()
        assert before["meta"]["total"] == 0
```

- [ ] **Step 3: 跑 isolation smoke**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/server
.venv/bin/pytest tests/integration/test_isolation_smoke.py -v 2>&1 | tail -8
# 也单文件跑（这是 main verification）
.venv/bin/pytest tests/integration/test_isolation_smoke.py::test_isolation_first_engine_clean -v 2>&1 | tail -5
```

Expected: 3 个 test 全部 pass；单测试 isolation 跑也通过。

- [ ] **Step 4: 跑全套确认无回归**

```bash
.venv/bin/pytest -q 2>&1 | tail -3
```

Expected: 173 passed（原 170 + isolation smoke 3）。

- [ ] **Step 5: Commit**

```bash
git add server/tests/integration/test_isolation_smoke.py
git commit -m "test(server): isolation smoke 锁定 fixture race 修复 (v2.1 A6)"
```

---

# Phase B — Coverage 报告升级（仅 1 task，多数已就位）

## Task 7: 升级 coverage 报告格式 + .gitignore

**Files:**
- Modify: `server/pyproject.toml`
- Modify: `web/vitest.config.ts`
- Modify: `web/.gitignore`
- Modify: `web/package.json` (加 test:coverage script，如缺)

- [ ] **Step 1: 读现有 pyproject.toml + vitest.config.ts + .gitignore**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用
grep -A 5 "tool.coverage" server/pyproject.toml
grep "coverage" web/.gitignore || echo "(missing coverage in .gitignore)"
grep "test:coverage" web/package.json || echo "(missing test:coverage script)"
```

- [ ] **Step 2: 改 vitest.config.ts 加 json-summary reporter**

把 `reporter: ["text", "html"]` 改成 `reporter: ["text", "html", "json-summary"]`：

```typescript
// web/vitest.config.ts (test.coverage section)
coverage: {
  provider: "v8",
  reporter: ["text", "html", "json-summary"],  // v2.1 — json-summary 给 baseline check 用
  reportsDirectory: "./coverage",               // v2.1 — 显式目录
  include: ["src/**/*.{ts,vue}"],
  exclude: [
    "src/main.ts",
    "src/api/types.ts",
    "src/**/*.d.ts",
    "src/__tests__/**",
    "**/node_modules/**",
    "**/dist/**",
  ],
  thresholds: { lines: 80, statements: 80, functions: 80, branches: 80 },
},
```

- [ ] **Step 3: package.json 加 test:coverage script**

`web/package.json` scripts 加（如缺）：

```json
"scripts": {
  ...
  "test:coverage": "vitest run --coverage"
}
```

- [ ] **Step 4: web/.gitignore 加 coverage/**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web
grep -q "^coverage/" .gitignore || echo "coverage/" >> .gitignore
```

- [ ] **Step 5: 跑 coverage 两边**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用
# 后端 — 验 json + html 输出
(cd server && .venv/bin/pytest --cov=app --cov-report=html --cov-report=json:coverage.json -q 2>&1 | tail -3)
ls server/htmlcov/index.html server/coverage.json
# 前端 — 验 v8 + json-summary
(cd web && pnpm test:coverage --reporter=dot 2>&1 | tail -3)
ls web/coverage/index.html web/coverage/coverage-summary.json
```

Expected: 4 个文件都存在。后端 ~92%，前端 ~80%（vitest 自带 thresholds 80% 通过）。

- [ ] **Step 6: server/.gitignore 加 htmlcov/ + coverage.json**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/server
grep -q "^htmlcov/" .gitignore 2>/dev/null || echo "htmlcov/" >> .gitignore
grep -q "^coverage.json$" .gitignore 2>/dev/null || echo "coverage.json" >> .gitignore
```

- [ ] **Step 7: Commit**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用
git add web/vitest.config.ts web/package.json web/.gitignore server/.gitignore
git commit -m "feat(test): coverage 报告升级 html + json-summary (v2.1 B1)"
```

---

# Phase C — E2E 扩展（10 个新 spec）

每个 E2E task 包含：写 spec → 启 server + dev server → 跑 spec → commit。

## Task 8: param-manager-6tab e2e

**Files:**
- Create: `web/tests/e2e/param-manager-6tab.spec.ts`

- [ ] **Step 1: 写 spec**

`web/tests/e2e/param-manager-6tab.spec.ts`：

```typescript
import { test, expect } from "./fixtures";

const TOKEN = process.env.E2E_AUTH_TOKEN ?? "e2e-token";

test("ParamManager 6 tab 全部可访问且非 stub", async ({ page, baseURL, request }) => {
  // 1. 建项目
  const create = await request.post(`${baseURL}/api/projects`, {
    headers: { "X-Auth-Token": TOKEN, "Content-Type": "application/json" },
    data: {
      name: `e2e-6tab-${Date.now()}`,
      project_type: "dev_only", phase: "bidding",
      city: "北京", industry: "电子政务", mode: "forward",
      basis_data_ver: "CSBMK®-202510",
    },
  });
  const pid = (await create.json()).data.id;

  try {
    await page.goto(`${baseURL}/projects/${pid}/parameters?t=${TOKEN}`);
    await page.waitForLoadState("networkidle").catch(() => {});

    // 验 6 个 tab 都存在
    const tabs = ["费率", "生产率", "开发因子", "运维因子", "规模变更", "快照"];
    for (const tab of tabs) {
      const btn = page.locator(`[role="tab"]:has-text("${tab}")`);
      await expect(btn).toBeVisible();
    }

    // 切到费率 → 看到城市行（如北京）
    await page.locator(`[role="tab"]:has-text("费率")`).click();
    await expect(page.locator('[data-testid="city-rate-row"]').first()).toBeVisible();

    // 切到开发因子 → 看到 app_type 因子卡
    await page.locator(`[role="tab"]:has-text("开发因子")`).click();
    await page.waitForTimeout(200);
    await expect(page.locator('[data-factor="app_type"]').first()).toBeVisible();

    // 切到规模变更 → 看到"新增"行
    await page.locator(`[role="tab"]:has-text("规模变更")`).click();
    await page.waitForTimeout(200);
    await expect(page.locator("text=新增").first()).toBeVisible();

    // 切到快照 → 看到"立即快照"按钮
    await page.locator(`[role="tab"]:has-text("快照")`).click();
    await page.waitForTimeout(200);
    await expect(page.locator('button:has-text("立即快照")')).toBeVisible();
  } finally {
    await request.delete(`${baseURL}/api/projects/${pid}`, {
      headers: { "X-Auth-Token": TOKEN },
    }).catch(() => {});
  }
});
```

- [ ] **Step 2: 启 server + 跑 spec**

确保后端运行（用 `COST_WEB_DIST_DIR` 挂载 dist），然后：

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web
NO_PROXY=127.0.0.1,localhost E2E_AUTH_TOKEN=qa-token-2026 \
  pnpm exec playwright test tests/e2e/param-manager-6tab.spec.ts --reporter=list 2>&1 | tail -5
```

Expected: 1 passed。

- [ ] **Step 3: Commit**

```bash
git add web/tests/e2e/param-manager-6tab.spec.ts
git commit -m "test(e2e): ParamManager 6 tab 切换覆盖 (v2.1 C1)"
```

---

## Task 9: audit-view + project-copy e2e

**Files:**
- Create: `web/tests/e2e/audit-view.spec.ts`
- Create: `web/tests/e2e/project-copy.spec.ts`

- [ ] **Step 1: 写 audit-view.spec.ts**

```typescript
import { test, expect } from "./fixtures";

const TOKEN = process.env.E2E_AUTH_TOKEN ?? "e2e-token";

test("AuditView — 项目操作后审计页有记录", async ({ page, baseURL, request }) => {
  const create = await request.post(`${baseURL}/api/projects`, {
    headers: { "X-Auth-Token": TOKEN, "Content-Type": "application/json" },
    data: {
      name: `e2e-audit-${Date.now()}`,
      project_type: "dev_only", phase: "bidding",
      city: "北京", industry: "电子政务", mode: "forward",
      basis_data_ver: "CSBMK®-202510",
    },
  });
  const pid = (await create.json()).data.id;

  try {
    // 改名（PATCH） — 制造一条 project.update 审计
    await request.patch(`${baseURL}/api/projects/${pid}`, {
      headers: { "X-Auth-Token": TOKEN, "Content-Type": "application/json" },
      data: { name: "renamed-by-e2e" },
    });

    await page.goto(`${baseURL}/projects/${pid}/audit?t=${TOKEN}`);
    await page.waitForLoadState("networkidle").catch(() => {});

    // 验至少 2 行 audit（create + update）
    const rows = page.locator('[data-testid="audit-row"]');
    await expect(rows).toHaveCount(2);
    // 验内容包含中文标签
    await expect(page.locator("text=创建项目")).toBeVisible();
    await expect(page.locator("text=修改项目")).toBeVisible();
  } finally {
    await request.delete(`${baseURL}/api/projects/${pid}`, {
      headers: { "X-Auth-Token": TOKEN },
    }).catch(() => {});
  }
});
```

- [ ] **Step 2: 写 project-copy.spec.ts**

```typescript
import { test, expect } from "./fixtures";

const TOKEN = process.env.E2E_AUTH_TOKEN ?? "e2e-token";

test("项目复制 — ⋯ 菜单 → 副本含 FP + diff_json", async ({ page, baseURL, request }) => {
  // 建项目 + 加 1 个 FP
  const create = await request.post(`${baseURL}/api/projects`, {
    headers: { "X-Auth-Token": TOKEN, "Content-Type": "application/json" },
    data: {
      name: `e2e-copy-${Date.now()}`,
      project_type: "dev_only", phase: "bidding",
      city: "北京", industry: "电子政务", mode: "forward",
      basis_data_ver: "CSBMK®-202510",
    },
  });
  const pid = (await create.json()).data.id;
  await request.post(`${baseURL}/api/projects/${pid}/functions`, {
    headers: { "X-Auth-Token": TOKEN, "Content-Type": "application/json" },
    data: { name: "fp1", category: "EI", complexity: "low", ufp: 3, us: 3, source: "manual" },
  });

  let copyPid: string | null = null;
  try {
    // 通过 API 直接 copy（UI 用 window.prompt 不易在 e2e 自动）
    const copy = await request.post(`${baseURL}/api/projects/${pid}/copy`, {
      headers: { "X-Auth-Token": TOKEN, "Content-Type": "application/json" },
      data: { name: "copy-by-e2e" },
    });
    expect(copy.status()).toBe(201);
    copyPid = (await copy.json()).data.id;
    expect(copyPid).not.toBe(pid);

    // 验副本含 1 个 FP
    const fps = await request.get(`${baseURL}/api/projects/${copyPid}/functions`, {
      headers: { "X-Auth-Token": TOKEN },
    });
    expect((await fps.json()).data.length).toBe(1);

    // 验副本 audit 含 copied_from
    const audit = await request.get(`${baseURL}/api/projects/${copyPid}/audit`, {
      headers: { "X-Auth-Token": TOKEN },
    });
    const rows = (await audit.json()).data;
    expect(rows.length).toBeGreaterThanOrEqual(1);
    const createEntry = rows.find((r: any) => r.action === "project.create");
    expect(createEntry).toBeDefined();
    expect(createEntry.diff_json).toContain("copied_from");
  } finally {
    await request.delete(`${baseURL}/api/projects/${pid}`, {
      headers: { "X-Auth-Token": TOKEN },
    }).catch(() => {});
    if (copyPid) {
      await request.delete(`${baseURL}/api/projects/${copyPid}`, {
        headers: { "X-Auth-Token": TOKEN },
      }).catch(() => {});
    }
  }
});
```

- [ ] **Step 3: 跑两个 spec**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web
NO_PROXY=127.0.0.1,localhost E2E_AUTH_TOKEN=qa-token-2026 \
  pnpm exec playwright test tests/e2e/audit-view.spec.ts tests/e2e/project-copy.spec.ts --reporter=list 2>&1 | tail -8
```

Expected: 2 passed。

- [ ] **Step 4: Commit**

```bash
git add web/tests/e2e/audit-view.spec.ts web/tests/e2e/project-copy.spec.ts
git commit -m "test(e2e): AuditView 时间线 + 项目复制流程 (v2.1 C2)"
```

---

## Task 10: reverse-allocator e2e

**Files:**
- Create: `web/tests/e2e/reverse-allocator.spec.ts`

- [ ] **Step 1: 写 spec**

```typescript
import { test, expect } from "./fixtures";

const TOKEN = process.env.E2E_AUTH_TOKEN ?? "e2e-token";

test("反向 + AI 模块分摊 — calc → allocator 渲染分摊结果", async ({ page, baseURL, request }) => {
  // 建反向项目
  const create = await request.post(`${baseURL}/api/projects`, {
    headers: { "X-Auth-Token": TOKEN, "Content-Type": "application/json" },
    data: {
      name: `e2e-alloc-${Date.now()}`,
      project_type: "dev_only", phase: "budget",
      city: "上海", industry: "金融", mode: "reverse",
      target_cost: 5000000,
      basis_data_ver: "CSBMK®-202510",
    },
  });
  const pid = (await create.json()).data.id;

  try {
    // 反向 calc
    await request.post(`${baseURL}/api/calc/reverse`, {
      headers: { "X-Auth-Token": TOKEN, "Content-Type": "application/json" },
      data: { project_id: pid, target_total: 5000000 },
    });

    // 调 allocate（手动给 drafts JSON 模拟 AI 输出）
    const alloc = await request.post(`${baseURL}/api/calc/allocate`, {
      headers: { "X-Auth-Token": TOKEN, "Content-Type": "application/json" },
      data: {
        project_id: pid,
        target_us: 100,
        drafts: [
          { name: "前端门户", weight: 1.0 },
          { name: "后台管理", weight: 2.0 },
        ],
        cf: 1.21,
      },
    });
    expect(alloc.status()).toBe(200);
    const allocData = await alloc.json();
    expect(allocData.data).toHaveLength(2);

    // 验返回值 us 字段（按 weight 比例分摊；2:1 比例下后台应该 ≈ 2 * 前端）
    const frontend = allocData.data.find((x: any) => x.name === "前端门户");
    const backend = allocData.data.find((x: any) => x.name === "后台管理");
    expect(backend.us).toBeGreaterThan(frontend.us);
  } finally {
    await request.delete(`${baseURL}/api/projects/${pid}`, {
      headers: { "X-Auth-Token": TOKEN },
    }).catch(() => {});
  }
});
```

- [ ] **Step 2: 跑 + commit**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web
NO_PROXY=127.0.0.1,localhost E2E_AUTH_TOKEN=qa-token-2026 \
  pnpm exec playwright test tests/e2e/reverse-allocator.spec.ts --reporter=list 2>&1 | tail -5
git add web/tests/e2e/reverse-allocator.spec.ts
git commit -m "test(e2e): 反向 + AI 模块分摊 (v2.1 C3)"
```

Expected: 1 passed。

---

## Task 11: wizard-dev-and-ops + reverse-validation e2e

**Files:**
- Create: `web/tests/e2e/wizard-dev-and-ops.spec.ts`
- Create: `web/tests/e2e/reverse-validation.spec.ts`

- [ ] **Step 1: 写 wizard-dev-and-ops.spec.ts**

```typescript
import { test, expect } from "./fixtures";

const TOKEN = process.env.E2E_AUTH_TOKEN ?? "e2e-token";

test("Wizard dev_and_ops — α 滑块 + ops 因子可见", async ({ page, baseURL, request }) => {
  await page.goto(`${baseURL}/projects/new?t=${TOKEN}`);
  await page.waitForLoadState("networkidle").catch(() => {});

  const name = `e2e-d+o-${Date.now()}`;
  await page.locator('input[name="name"]').fill(name);
  await page.locator('[data-test="wizard-next"]').click();
  await page.waitForTimeout(150);

  // Step 2: 选 dev_and_ops
  await page.locator('input[value="dev_and_ops"]').click();
  // 验 α 滑块出现
  await expect(page.locator('input[type="range"]')).toBeVisible();
  // 验 include_ops 自动选中
  await expect(page.locator('input[type="checkbox"]:checked').first()).toBeVisible();

  // 拉滑块到 0.85
  await page.locator('input[type="range"]').fill("0.85");
  await page.locator('[data-test="wizard-next"]').click(); // step 3
  await page.locator('[data-test="wizard-next"]').click(); // step 4
  await page.locator('[data-test="wizard-next"]').click(); // step 5
  await page.locator('[data-test="wizard-next"]').click(); // step 6 (ops factors)

  // step 6 应该有 ops factor dropdowns（>1 个 select）
  await page.waitForTimeout(150);
  const opsSelects = page.locator('select');
  const count = await opsSelects.count();
  expect(count).toBeGreaterThan(0); // ops dropdowns 存在

  let createdPid: string | null = null;
  try {
    await page.locator('[data-test="wizard-next"]').click(); // step 7
    await page.locator('button:has-text("创建项目")').click();
    await page.waitForTimeout(800);
    const url = page.url();
    createdPid = url.match(/projects\/([^/]+)/)?.[1] ?? null;
    expect(createdPid).toBeTruthy();
  } finally {
    if (createdPid) {
      await request.delete(`${baseURL}/api/projects/${createdPid}`, {
        headers: { "X-Auth-Token": TOKEN },
      }).catch(() => {});
    }
  }
});
```

- [ ] **Step 2: 写 reverse-validation.spec.ts**

```typescript
import { test, expect } from "./fixtures";

const TOKEN = process.env.E2E_AUTH_TOKEN ?? "e2e-token";

test("Wizard reverse 模式 target_total=0 → 下一步 disabled", async ({ page, baseURL }) => {
  await page.goto(`${baseURL}/projects/new?t=${TOKEN}`);
  await page.waitForLoadState("networkidle").catch(() => {});

  await page.locator('input[name="name"]').fill("validation-test");
  await page.locator('[data-test="wizard-next"]').click(); // → step 2
  await page.locator('[data-test="wizard-next"]').click(); // → step 3
  await page.locator('[data-test="wizard-next"]').click(); // → step 4

  // Step 4: 选 reverse
  await page.locator('input[value="reverse"]').click();
  await page.waitForTimeout(100);

  // target_total 默认 0 — next 应 disabled
  const nextBtn = page.locator('[data-test="wizard-next"]');
  await expect(nextBtn).toBeDisabled();

  // 填正数 → enabled
  await page.locator('input[name="target_total"]').fill("1000");
  await page.waitForTimeout(50);
  await expect(nextBtn).toBeEnabled();
});
```

- [ ] **Step 3: 跑 + commit**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web
NO_PROXY=127.0.0.1,localhost E2E_AUTH_TOKEN=qa-token-2026 \
  pnpm exec playwright test tests/e2e/wizard-dev-and-ops.spec.ts tests/e2e/reverse-validation.spec.ts --reporter=list 2>&1 | tail -6
git add web/tests/e2e/wizard-dev-and-ops.spec.ts web/tests/e2e/reverse-validation.spec.ts
git commit -m "test(e2e): Wizard dev_and_ops + reverse validation (v2.1 C4)"
```

Expected: 2 passed。

---

## Task 12: param-snapshot-restore e2e

**Files:**
- Create: `web/tests/e2e/param-snapshot-restore.spec.ts`

- [ ] **Step 1: 写 spec**

```typescript
import { test, expect } from "./fixtures";

const TOKEN = process.env.E2E_AUTH_TOKEN ?? "e2e-token";

test("Snapshot 创建 + restore 恢复全局参数", async ({ request, baseURL }) => {
  // 1. 改 hours_per_pm 让它偏离默认
  await request.patch(`${baseURL}/api/params/global`, {
    headers: { "X-Auth-Token": TOKEN, "Content-Type": "application/json" },
    data: { key: "hours_per_pm", value: 200 },
  });

  // 2. 拍 snapshot
  const create = await request.post(`${baseURL}/api/params/snapshots`, {
    headers: { "X-Auth-Token": TOKEN, "Content-Type": "application/json" },
    data: { scope: "global", label: "before-second-edit" },
  });
  expect(create.status()).toBe(201);
  const snapId = (await create.json()).data.id;

  // 3. 再改 hours_per_pm → 300
  await request.patch(`${baseURL}/api/params/global`, {
    headers: { "X-Auth-Token": TOKEN, "Content-Type": "application/json" },
    data: { key: "hours_per_pm", value: 300 },
  });

  // 4. Restore snapshot
  const restore = await request.post(`${baseURL}/api/params/snapshots/${snapId}/restore`, {
    headers: { "X-Auth-Token": TOKEN },
  });
  expect(restore.status()).toBe(200);

  // 5. 验 hours_per_pm 恢复到 200
  const eff = await request.get(`${baseURL}/api/params/effective`, {
    headers: { "X-Auth-Token": TOKEN },
  });
  const data = (await eff.json()).data;
  expect(data.hours_per_pm).toBe(200);

  // 6. 清理 — 删 snapshot + reset
  await request.delete(`${baseURL}/api/params/snapshots/${snapId}`, {
    headers: { "X-Auth-Token": TOKEN },
  }).catch(() => {});
});
```

- [ ] **Step 2: 跑 + commit**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web
NO_PROXY=127.0.0.1,localhost E2E_AUTH_TOKEN=qa-token-2026 \
  pnpm exec playwright test tests/e2e/param-snapshot-restore.spec.ts --reporter=list 2>&1 | tail -5
git add web/tests/e2e/param-snapshot-restore.spec.ts
git commit -m "test(e2e): param snapshot 创建 + restore 恢复 (v2.1 C5)"
```

Expected: 1 passed。

---

## Task 13: stale-banner + bulk-snapshot + cursor-pagination e2e

**Files:**
- Create: `web/tests/e2e/stale-result-banner.spec.ts`
- Create: `web/tests/e2e/bulk-snapshot.spec.ts`
- Create: `web/tests/e2e/audit-cursor-pagination.spec.ts`

- [ ] **Step 1: 写 stale-result-banner.spec.ts**

```typescript
import { test, expect } from "./fixtures";

const TOKEN = process.env.E2E_AUTH_TOKEN ?? "e2e-token";

test("改 override 后结果页显示 stale banner", async ({ page, baseURL, request }) => {
  const create = await request.post(`${baseURL}/api/projects`, {
    headers: { "X-Auth-Token": TOKEN, "Content-Type": "application/json" },
    data: {
      name: `e2e-stale-${Date.now()}`,
      project_type: "dev_only", phase: "bidding",
      city: "北京", industry: "电子政务", mode: "forward",
      basis_data_ver: "CSBMK®-202510",
    },
  });
  const pid = (await create.json()).data.id;

  try {
    // 加 FP + 跑 forward
    await request.post(`${baseURL}/api/projects/${pid}/functions`, {
      headers: { "X-Auth-Token": TOKEN, "Content-Type": "application/json" },
      data: { name: "fp1", category: "EI", complexity: "low", ufp: 3, us: 3, source: "manual" },
    });
    await request.post(`${baseURL}/api/calc/forward`, {
      headers: { "X-Auth-Token": TOKEN, "Content-Type": "application/json" },
      data: { project_id: pid },
    });

    // 改 project-level override（让 Result.is_stale=True）
    await request.patch(`${baseURL}/api/projects/${pid}/params/override`, {
      headers: { "X-Auth-Token": TOKEN, "Content-Type": "application/json" },
      data: { "hours_per_pm": 160 },
    });

    // 打开 result 页 — 应有 stale banner
    await page.goto(`${baseURL}/projects/${pid}/result?t=${TOKEN}`);
    await page.waitForLoadState("networkidle").catch(() => {});
    await page.waitForTimeout(300);
    // banner 文字含"重新计算"或"已变"
    const banner = page.locator('text=/重新计算|参数.*变|stale/i');
    await expect(banner.first()).toBeVisible({ timeout: 3000 });
  } finally {
    await request.delete(`${baseURL}/api/projects/${pid}`, {
      headers: { "X-Auth-Token": TOKEN },
    }).catch(() => {});
  }
});
```

- [ ] **Step 2: 写 bulk-snapshot.spec.ts**

```typescript
import { test, expect } from "./fixtures";

const TOKEN = process.env.E2E_AUTH_TOKEN ?? "e2e-token";

test("bulk_write 后 fp_snapshot 自动产生", async ({ request, baseURL }) => {
  const create = await request.post(`${baseURL}/api/projects`, {
    headers: { "X-Auth-Token": TOKEN, "Content-Type": "application/json" },
    data: {
      name: `e2e-bulk-${Date.now()}`,
      project_type: "dev_only", phase: "bidding",
      city: "北京", industry: "电子政务", mode: "forward",
      basis_data_ver: "CSBMK®-202510",
    },
  });
  const pid = (await create.json()).data.id;

  try {
    // 先单独 POST 一个 FP
    await request.post(`${baseURL}/api/projects/${pid}/functions`, {
      headers: { "X-Auth-Token": TOKEN, "Content-Type": "application/json" },
      data: { name: "manual-fp", category: "EI", complexity: "low", ufp: 3, us: 3, source: "manual" },
    });

    // 然后 bulk_write replace=true — 应触发 pre_bulk_replace snapshot
    await request.post(`${baseURL}/api/projects/${pid}/functions/bulk`, {
      headers: { "X-Auth-Token": TOKEN, "Content-Type": "application/json" },
      data: {
        items: [
          { name: "bulk-fp", category: "EQ", complexity: "average", ufp: 4, us: 4, source: "manual" },
        ],
        replace: true,
      },
    });

    // 验 snapshot 列表至少 1 行 pre_bulk_replace
    const snaps = await request.get(`${baseURL}/api/projects/${pid}/functions/snapshots`, {
      headers: { "X-Auth-Token": TOKEN },
    });
    const rows = (await snaps.json()).data;
    const preBulk = rows.find((s: any) => s.reason === "pre_bulk_replace");
    expect(preBulk).toBeDefined();
  } finally {
    await request.delete(`${baseURL}/api/projects/${pid}`, {
      headers: { "X-Auth-Token": TOKEN },
    }).catch(() => {});
  }
});
```

- [ ] **Step 3: 写 audit-cursor-pagination.spec.ts**

```typescript
import { test, expect } from "./fixtures";

const TOKEN = process.env.E2E_AUTH_TOKEN ?? "e2e-token";

test("Audit cursor 分页 — before_id 正确", async ({ request, baseURL }) => {
  const create = await request.post(`${baseURL}/api/projects`, {
    headers: { "X-Auth-Token": TOKEN, "Content-Type": "application/json" },
    data: {
      name: `e2e-cursor-${Date.now()}`,
      project_type: "dev_only", phase: "bidding",
      city: "北京", industry: "电子政务", mode: "forward",
      basis_data_ver: "CSBMK®-202510",
    },
  });
  const pid = (await create.json()).data.id;

  try {
    // 制造 6 条 audit（5 个 PATCH + 1 个 create 已有 = 6 行）
    for (let i = 0; i < 5; i++) {
      await request.patch(`${baseURL}/api/projects/${pid}`, {
        headers: { "X-Auth-Token": TOKEN, "Content-Type": "application/json" },
        data: { name: `rename-${i}` },
      });
    }

    // limit=3 第一页
    const page1 = await request.get(`${baseURL}/api/projects/${pid}/audit?limit=3`, {
      headers: { "X-Auth-Token": TOKEN },
    });
    const rows1 = (await page1.json()).data;
    expect(rows1.length).toBe(3);

    // limit=3 第二页（before_id = 第一页最后一行的 id）
    const lastId = rows1[rows1.length - 1].id;
    const page2 = await request.get(
      `${baseURL}/api/projects/${pid}/audit?limit=3&before_id=${lastId}`,
      { headers: { "X-Auth-Token": TOKEN } }
    );
    const rows2 = (await page2.json()).data;
    expect(rows2.length).toBeGreaterThan(0);
    // 第二页所有 id 都应 < lastId
    for (const r of rows2) {
      expect(r.id).toBeLessThan(lastId);
    }
  } finally {
    await request.delete(`${baseURL}/api/projects/${pid}`, {
      headers: { "X-Auth-Token": TOKEN },
    }).catch(() => {});
  }
});
```

- [ ] **Step 4: 跑 3 个 spec + 全套 e2e**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web
NO_PROXY=127.0.0.1,localhost E2E_AUTH_TOKEN=qa-token-2026 \
  pnpm exec playwright test \
    tests/e2e/stale-result-banner.spec.ts \
    tests/e2e/bulk-snapshot.spec.ts \
    tests/e2e/audit-cursor-pagination.spec.ts \
    --reporter=list 2>&1 | tail -8

echo "=== 全套 e2e（13 spec）==="
NO_PROXY=127.0.0.1,localhost E2E_AUTH_TOKEN=qa-token-2026 \
  pnpm exec playwright test --reporter=list 2>&1 | tail -20
```

Expected: 13 passed（3 旧 + 10 新）。

- [ ] **Step 5: Commit**

```bash
git add web/tests/e2e/stale-result-banner.spec.ts web/tests/e2e/bulk-snapshot.spec.ts web/tests/e2e/audit-cursor-pagination.spec.ts
git commit -m "test(e2e): stale banner + bulk snapshot + audit cursor (v2.1 C6)"
```

---

# Phase D — Baseline + No-regression hook

## Task 14: coverage-baseline.json + 3 个 scripts

**Files:**
- Create: `coverage-baseline.json`
- Create: `scripts/check-coverage.sh`
- Create: `scripts/update-coverage-baseline.sh`
- Create: `scripts/_compare-coverage.py`

- [ ] **Step 1: 跑当前 coverage 拿实际 baseline 数字**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用
(cd server && .venv/bin/pytest --cov=app --cov-report=json:coverage.json -q 2>&1 | tail -3)
BACKEND_PCT=$(python3 -c "import json; d=json.load(open('server/coverage.json')); print(round(d['totals']['percent_covered'], 1))")
echo "Backend line coverage: $BACKEND_PCT%"

(cd web && pnpm test:coverage --reporter=dot 2>&1 | tail -3)
FRONTEND_PCT=$(python3 -c "import json; d=json.load(open('web/coverage/coverage-summary.json')); print(d['total']['lines']['pct'])")
echo "Frontend line coverage: $FRONTEND_PCT%"
```

记录两个百分比，下一步写进 baseline 文件。

- [ ] **Step 2: 创建 coverage-baseline.json**

把上一步的数字填进去：

```json
{
  "$schema": "本地 baseline — scripts/check-coverage.sh 用来防止 PR 让覆盖率退化超过 tolerance",
  "captured_at": "2026-05-11",
  "captured_at_commit": "<填入 git rev-parse --short HEAD 的值>",
  "tolerance_pct": 0.5,
  "backend": {
    "line_coverage_pct": <BACKEND_PCT>,
    "source": "server/coverage.json -> totals.percent_covered"
  },
  "frontend": {
    "line_coverage_pct": <FRONTEND_PCT>,
    "source": "web/coverage/coverage-summary.json -> total.lines.pct"
  }
}
```

填具体数字（例如 92.4 / 80.0），commit SHA 用 `git rev-parse --short HEAD`。

- [ ] **Step 3: 创建 scripts/_compare-coverage.py**

```python
#!/usr/bin/env python3
"""Compare current coverage against baseline. Exit 1 if regression > tolerance.

Used by scripts/check-coverage.sh. Reads coverage-baseline.json from repo root.
"""
from __future__ import annotations

import json
import os
import sys
from pathlib import Path


def main() -> int:
    if len(sys.argv) != 3:
        print(
            "Usage: _compare-coverage.py <backend_pct> <frontend_pct>",
            file=sys.stderr,
        )
        return 2
    try:
        current_backend = float(sys.argv[1])
        current_frontend = float(sys.argv[2])
    except ValueError:
        print("Coverage values must be numeric", file=sys.stderr)
        return 2

    repo_root = Path(__file__).resolve().parent.parent
    baseline_path = repo_root / "coverage-baseline.json"
    if not baseline_path.exists():
        print(
            f"ERROR: {baseline_path} not found. "
            "Run scripts/update-coverage-baseline.sh once to establish baseline.",
            file=sys.stderr,
        )
        return 1
    baseline = json.loads(baseline_path.read_text())
    tolerance = float(baseline.get("tolerance_pct", 0.5))

    issues: list[str] = []
    improvements: list[str] = []

    for label, key, current in [
        ("Backend", "backend", current_backend),
        ("Frontend", "frontend", current_frontend),
    ]:
        baseline_pct = float(baseline[key]["line_coverage_pct"])
        delta = current - baseline_pct
        if delta < -tolerance:
            issues.append(
                f"  ✗ {label}: {current:.2f}% (baseline {baseline_pct:.2f}%, "
                f"delta {delta:+.2f}, tolerance {tolerance})"
            )
        elif delta > tolerance:
            improvements.append(
                f"  ↑ {label}: {current:.2f}% (baseline {baseline_pct:.2f}%, "
                f"delta {delta:+.2f})"
            )
        else:
            print(f"  ✓ {label}: {current:.2f}% (baseline {baseline_pct:.2f}%)")

    if improvements:
        print("\n覆盖率上升：")
        for line in improvements:
            print(line)
        print("\n建议：scripts/update-coverage-baseline.sh 锁定新 baseline。")

    if issues:
        print("\n✗ 覆盖率退化超过 tolerance：", file=sys.stderr)
        for line in issues:
            print(line, file=sys.stderr)
        return 1

    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 4: 创建 scripts/check-coverage.sh**

```bash
#!/usr/bin/env bash
# 验证当前 coverage 不退化于 coverage-baseline.json。
# 用法：scripts/check-coverage.sh
set -euo pipefail

REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$REPO_ROOT"

echo "→ 跑 backend pytest --cov ..."
(cd server && .venv/bin/pytest --cov=app --cov-report=json:coverage.json -q > /tmp/cov-backend.log 2>&1) || {
    echo "✗ Backend tests 失败 — 详见 /tmp/cov-backend.log" >&2
    exit 1
}
BACKEND_PCT=$(python3 -c "import json; d=json.load(open('server/coverage.json')); print(round(d['totals']['percent_covered'], 2))")

echo "→ 跑 frontend vitest --coverage ..."
(cd web && pnpm test:coverage --reporter=dot > /tmp/cov-frontend.log 2>&1) || {
    echo "✗ Frontend tests 失败 — 详见 /tmp/cov-frontend.log" >&2
    exit 1
}
FRONTEND_PCT=$(python3 -c "import json; d=json.load(open('web/coverage/coverage-summary.json')); print(d['total']['lines']['pct'])")

echo ""
echo "→ 比对 coverage-baseline.json ..."
python3 scripts/_compare-coverage.py "$BACKEND_PCT" "$FRONTEND_PCT"
```

- [ ] **Step 5: 创建 scripts/update-coverage-baseline.sh**

```bash
#!/usr/bin/env bash
# 重写 coverage-baseline.json 为当前覆盖率。
# 用户手动调用 — 在覆盖率上升后锁定新 baseline。
set -euo pipefail

REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$REPO_ROOT"

echo "→ 跑 backend pytest --cov ..."
(cd server && .venv/bin/pytest --cov=app --cov-report=json:coverage.json -q > /dev/null)
BACKEND_PCT=$(python3 -c "import json; d=json.load(open('server/coverage.json')); print(round(d['totals']['percent_covered'], 2))")

echo "→ 跑 frontend vitest --coverage ..."
(cd web && pnpm test:coverage --reporter=dot > /dev/null)
FRONTEND_PCT=$(python3 -c "import json; d=json.load(open('web/coverage/coverage-summary.json')); print(d['total']['lines']['pct'])")

COMMIT=$(git rev-parse --short HEAD)
TODAY=$(date +%Y-%m-%d)
TOLERANCE=$(python3 -c "import json; d=json.load(open('coverage-baseline.json')); print(d.get('tolerance_pct', 0.5))")

python3 - <<EOF
import json
baseline = {
    "\$schema": "本地 baseline — scripts/check-coverage.sh 用来防止 PR 让覆盖率退化超过 tolerance",
    "captured_at": "$TODAY",
    "captured_at_commit": "$COMMIT",
    "tolerance_pct": $TOLERANCE,
    "backend": {
        "line_coverage_pct": $BACKEND_PCT,
        "source": "server/coverage.json -> totals.percent_covered",
    },
    "frontend": {
        "line_coverage_pct": $FRONTEND_PCT,
        "source": "web/coverage/coverage-summary.json -> total.lines.pct",
    },
}
with open("coverage-baseline.json", "w") as f:
    json.dump(baseline, f, indent=2, ensure_ascii=False)
    f.write("\n")
print(f"✓ baseline 已更新：backend {$BACKEND_PCT}% / frontend {$FRONTEND_PCT}% (commit $COMMIT)")
EOF
```

- [ ] **Step 6: 加可执行权限 + 验证 check 跑通**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用
chmod +x scripts/check-coverage.sh scripts/update-coverage-baseline.sh
chmod +x scripts/_compare-coverage.py

# 第一次跑：用 update 锁定 baseline
./scripts/update-coverage-baseline.sh
cat coverage-baseline.json

# 第二次：check 应通过（数字没变）
./scripts/check-coverage.sh
```

Expected: update 输出新 baseline；check 输出三个绿色 ✓。

- [ ] **Step 7: Commit**

```bash
git add scripts/check-coverage.sh scripts/update-coverage-baseline.sh scripts/_compare-coverage.py coverage-baseline.json
git commit -m "feat(test): coverage baseline + no-regression hook scripts (v2.1 D1)"
```

---

## Task 15: README 覆盖率验证章节 + 完工

**Files:**
- Modify: `README.md`

- [ ] **Step 1: 加章节到 README.md**

读 README 现状找合适位置（"## 版本历史" 之前 或 v2.0 章节末尾），加：

```markdown
## 覆盖率验证（v2.1+）

项目维护 `coverage-baseline.json` 防止覆盖率退化。本地验证：

```bash
# 跑全套测试 + 与 baseline 比对（>0.5% 退化时退出 1）
./scripts/check-coverage.sh

# 覆盖率上升时锁定新 baseline
./scripts/update-coverage-baseline.sh
```

可选 pre-commit hook（自决）：

```bash
ln -sf ../../scripts/check-coverage.sh .git/hooks/pre-commit
```

测试基线（v2.1）：

| 项 | 数量 | line coverage |
|---|---:|---:|
| Backend pytest | 173+ | ≥ 92% |
| Frontend vitest | 220+ | ≥ 80% |
| Playwright e2e | 13 | n/a |
```

- [ ] **Step 2: 跑全套最终验证**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用
echo "=== Backend ==="
(cd server && .venv/bin/pytest -q 2>&1 | tail -3)
echo "=== Frontend ==="
(cd web && pnpm vitest run --reporter=dot 2>&1 | tail -3)
echo "=== E2E ==="
(cd web && NO_PROXY=127.0.0.1,localhost E2E_AUTH_TOKEN=qa-token-2026 pnpm exec playwright test --reporter=list 2>&1 | tail -5)
echo "=== type-check + lint ==="
(cd web && pnpm type-check 2>&1 | tail -2)
(cd web && pnpm lint 2>&1 | tail -2)
echo "=== coverage hook ==="
./scripts/check-coverage.sh
```

Expected:
- Backend pytest: 173 passed（170 旧 + 3 isolation smoke）
- Frontend vitest: 220 passed + 1 skip
- E2E: 13 passed
- type-check / lint: clean
- coverage hook: 三个绿色 ✓

- [ ] **Step 3: Commit**

```bash
git add README.md
git commit -m "docs: README 加覆盖率验证章节 (v2.1 D2)"
```

---

# Self-Review checklist（写完 plan 必跑）

- [ ] **Spec coverage:** 每节 spec 是否都有 task？

| Spec 章节 | 对应 task |
|---|---|
| §3.1 Per-test ephemeral engine（A） | T1 conftest / T2 seed signature / T3-5 fixture 改造 / T6 isolation smoke |
| §3.2 13 E2E specs（C） | T8-13（10 个新 + 3 已有保留）|
| §3.3 Coverage 双引擎（B） | T7 升级 reporter |
| §3.3 No-regression（D） | T14 baseline + scripts / T15 README |
| §6 测试计划 | 每个 phase 的 Step 都有 verification |
| §7 范围外 | 无需 task |
| §8 默认决策 | 已在 T2/T4/T14 中体现（None 默认、API setup data、baseline 0.5%） |

✅ 全覆盖。

- [ ] **Placeholder scan:** plan 内已搜过 "TBD" "TODO" "implement later" "fill in" — 0 命中（除 task 14 step 2 让用户填实际 baseline 数字这一处属于运行时数据，不是 placeholder）。

- [ ] **Type consistency:**
  - `client_factory` 签名 `(seed_csbmk: bool = True) -> async callable` — T1 定义，T3-5 + isolation smoke (T6) 都用 `async with await client_factory() as c` ✓
  - `db_session` fixture — T1 定义 sync session，T4 直接用 ✓
  - `seed_from_csbmk(db=None)` — T2 定义，T1 conftest 用 `seed_from_csbmk(db=db_session)` ✓
  - coverage baseline json schema — T14 / T15 一致

✅ 无差异。

---

# 执行注意

1. **顺序敏感**：A 必须先于 C/D（fixture race 不修，后续 e2e 跑起来仍可能撞 SQLite lock）；B 可以与 A 并行。
2. **Phase A 第 3-5 task 是 mechanical search-replace**：可以让 subagent 用 sed 批量处理，但每个文件需要 verify。
3. **E2E 跑前需启 server + dist mount**：`COST_WEB_DIST_DIR=/Users/.../web/dist .venv/bin/uvicorn app.main:app --port 8788 &`
4. **Baseline 数字以实测为准**：spec 估的 87.4/74.2 是错的（实际 92.4/80+），T14 step 1 现场取值，不要照搬 spec。
5. **预计 CC 执行 4-6 小时**；人工执行 ~2 天。

---

**Plan 完。** 15 task，4 phase，~16 commits。
