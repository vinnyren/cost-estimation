# v2.4 反算路径视觉升级 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 ResultView.vue 的 reverse 分支视觉升级到 v2.2 design import token 体系，与 forward 保持一致 — 4 列 grid 反算输入 + ResultTrio FP 卡片 + 上移 download 按钮到 page-header。

**Architecture:** 4 task / 1 phase / 1 PR · 纯前端纯视觉。不动 calc 算法、不动 schema、不动 API。复用 v2.2 已有 ResultTrio (unit="fp" 分支) 和 v2.3 AllocatorPanel。

**Tech Stack:** Vue 3.5 + TypeScript + vitest + Playwright.

**Branch:** `v2.4-reverse-visual`（off `v2.3-closure` HEAD `0d2a038`，spec 在 `281d95e`）。

---

## Task 1: ResultView.vue reverse 分支 script + template 重写

**Files:**
- Modify: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/web/src/views/ResultView.vue`

### 现状

- script (line 41-128)：已有 `reverseResult / targetTotal / otherCost / reverseCalc / hasReverse`
- template (line 264-329)：用 `<fieldset class="card reverse-card">` 旧样式 + `<ResultCard>` × 3
- CSS (line 416-451)：`.reverse / .reverse-card / .reverse-card legend / .reverse-actions`

### 目标

- script: 加 `availableBudget` + `reverseTiers` computed，加 `ResultTrio` import
- template (264-329) 整段重写：v2.2 utility class `.card / .field-row / .field-input` + `<ResultTrio>` 替代 `<ResultCard>` × 3
- CSS: 删 `.reverse-card / .reverse-card legend / .reverse-actions`（不再使用）；保留 `.reverse / .field / .field-label`（field-label 已被 forward / 其它视图用，不动）

### 步骤

- [ ] **Step 1: 加 ResultTrio import 到 script setup**

打开 `web/src/views/ResultView.vue`. 找到现有 `import ResultTrio` 行（forward path 已 import）。如果已 import，跳过此 step。如果没有，在 imports 段加：

```ts
import ResultTrio from "@/components/result/ResultTrio.vue";
```

通过 grep 验证：

```bash
grep -n "ResultTrio" web/src/views/ResultView.vue
```

应至少有 import + forward 用法 2 处。

- [ ] **Step 2: 加 availableBudget computed**

在 script setup 内，紧跟在 `const otherCost = ref(0);` 之后（约 line 50）加：

```ts
const availableBudget = computed(() => {
  const avail = Math.max(0, targetTotal.value - otherCost.value);
  return avail.toLocaleString("zh-CN");
});
```

确认 `computed` 已 import — 文件顶部应有 `import { computed, ref, ... } from "vue";`。

- [ ] **Step 3: 加 reverseTiers computed**

紧跟 `availableBudget` 之后加：

```ts
const reverseTiers = computed(() => {
  if (!reverseResult.value) return [];
  const r = reverseResult.value;
  return (["P10", "P50", "P90"] as const).map((k) => ({
    key: k,
    label: k === "P10"
      ? "乐观 · 最大可承载"
      : k === "P50"
      ? "中位 · 推荐采纳"
      : "保守 · 最少可保证",
    cost: 0, // unit="fp" 分支 ResultTrio 不用 cost
    fp: r.scale_adjusted_bands[k],
    recommended: r.recommended_band === k,
    unit: "fp" as const,
    extras: [
      ["未调整规模 US", `${r.scale_unadjusted_bands[k].toFixed(2)} FP`],
    ] as Array<[string, string]>,
  }));
});
```

- [ ] **Step 4: Template — 替换 reverse 分支整段**

定位 `<div v-else-if="project?.mode === 'reverse'" class="reverse">` 块（约 line 264-329）。**整段** 替换为：

```vue
    <div
      v-else-if="project?.mode === 'reverse'"
      class="reverse"
    >
      <div
        class="card"
        style="padding: 20px; margin-bottom: 16px"
      >
        <div
          class="section-title"
          style="margin-bottom: 14px"
        >
          反算输入
        </div>
        <div style="display: grid; grid-template-columns: repeat(4, 1fr); gap: 16px">
          <div
            class="field"
            style="margin-bottom: 0"
          >
            <label class="field-label">目标总造价 (元)</label>
            <input
              v-model.number="targetTotal"
              class="field-input mono"
              type="number"
              min="0"
            >
          </div>
          <div
            class="field"
            style="margin-bottom: 0"
          >
            <label class="field-label">其他费用 (元)</label>
            <input
              v-model.number="otherCost"
              class="field-input mono"
              type="number"
              min="0"
            >
          </div>
          <div
            class="field"
            style="margin-bottom: 0"
          >
            <label class="field-label">可用预算 (元)</label>
            <input
              class="field-input mono"
              :value="availableBudget"
              disabled
              style="background: var(--surface-sunken)"
            >
          </div>
          <div
            class="field"
            style="margin-bottom: 0"
          >
            <label class="field-label">α 开发占比</label>
            <input
              class="field-input mono"
              :value="(project?.alpha_dev ?? 1.0).toFixed(3)"
              disabled
              style="background: var(--surface-sunken)"
            >
          </div>
        </div>
        <div style="margin-top: 14px; display: flex; gap: 8px">
          <button
            type="button"
            class="btn btn-primary"
            @click="reverseCalc"
          >
            反算
          </button>
        </div>
      </div>

      <ResultTrio
        v-if="hasReverse"
        :tiers="reverseTiers"
      />

      <AllocatorPanel
        v-if="reverseResult && project"
        :reverse-result="reverseResult"
        :project-id="projectId"
        @allocated="onAllocated"
      />
    </div>
```

注意：
- 删除原 `<fieldset class="card reverse-card">` 包装
- 删除原 `<ResultCard>` × 3 块（替换为单个 `<ResultTrio>`）
- **保留** `<AllocatorPanel>` 块 (v2.3 不动)

- [ ] **Step 5: 删旧 CSS**

定位 `<style scoped>` 段中下列 selectors，**整块删除**：

```css
.reverse-card {
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  background: var(--color-bg-elevated);
  display: flex;
  flex-direction: column;
  gap: var(--space-3);
  padding: var(--space-5);
}
.reverse-card legend {
  font-weight: 600;
  font-size: var(--font-size-md);
  color: var(--color-text-title);
  padding: 0 var(--space-2);
}
.reverse-actions {
  display: flex;
  justify-content: flex-end;
  margin-top: var(--space-2);
}
```

**保留**：
- `.reverse { display: flex; flex-direction: column; gap: var(--space-5); }` — 容器布局仍用
- `.field` 和 `.field-label` — 这些在 global.css v2.2 已定义，scoped 这版冗余但不动避免回归

- [ ] **Step 6: vitest sanity（部分 test 会 fail，预期）**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web && pnpm vitest run src/__tests__/views/ResultView.test.ts 2>&1 | tail -25
```

Expected: 某个 reverse mode test fail（断言 `<fieldset>` 不存在了），这是预期的；T3 会修。其它 ResultView test 应仍 PASS。

- [ ] **Step 7: vue-tsc 验证**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web && pnpm vue-tsc --noEmit --pretty false 2>&1 | tail -10
```

Expected: 0 errors。

- [ ] **Step 8: Commit**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用 && git add web/src/views/ResultView.vue && git commit -m "feat(result): reverse 分支视觉升级 — 4 列 grid + ResultTrio + 删旧 fieldset (v2.4 T1)"
```

---

## Task 2: Page header 升级 — hero-bg + reverse download 按钮

**Files:**
- Modify: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/web/src/views/ResultView.vue`

### 现状

- Template 顶层 `<section class="page" aria-labelledby="title">` (line 168)
- `<header class="page-header">` 含 `<h1>` + 单个 `back` 按钮
- 底部 `<footer class="dl-bar">` (line 332-350) 显示 download 按钮 + error，对 forward 和 reverse 都显示

### 目标

- 给 `.page` 加 `hero-bg` class（v2.2 已加的 utility，accent gradient 顶端）
- 在 page-header 加 reverse mode 专用的 [↻ 重新反算] + [下载 Excel] 右对齐按钮
- 减少底部 `<footer class="dl-bar">` 触发：仅 forward path 显示

### 步骤

- [ ] **Step 1: 给 `<section class="page">` 加 hero-bg class**

定位 line 168 的：

```vue
<section
  class="page"
  aria-labelledby="title"
>
```

改为：

```vue
<section
  class="page hero-bg"
  aria-labelledby="title"
>
```

`.hero-bg` 在 `web/src/styles/global.css` v2.2 已定义（accent-soft 渐变到 bg）。

- [ ] **Step 2: page-header 加 reverse-only 按钮组**

定位 `<header class="page-header">` (约 line 171-186)。当前结构是 `<h1>` + `<button @click="back">`. 整段替换为：

```vue
    <header class="page-header">
      <h1 id="title">
        评估结果（项目 #{{ projectId }} ·
        {{ project?.mode === "reverse" ? "反向" : "正向" }}）
      </h1>
      <button
        type="button"
        class="btn"
        @click="back"
      >
        返回
      </button>
      <div class="page-spacer" />
      <template v-if="project?.mode === 'reverse' && hasReverse">
        <button
          type="button"
          class="btn btn-ghost"
          :disabled="!hasReverse"
          @click="reverseCalc"
        >
          ↻ 重新反算
        </button>
        <button
          type="button"
          class="btn btn-primary"
          :disabled="downloading"
          @click="download"
        >
          {{ downloading ? "下载中…" : "下载 Excel" }}
        </button>
      </template>
    </header>
```

`.page-spacer { flex: 1 }` 在 global.css v2.2 已定义，把右侧按钮顶到 header 末端。

- [ ] **Step 3: 限制底部 footer 仅 forward 显示**

定位 line 332-350 的 `<footer v-if="hasForward || hasReverse" class="dl-bar">`. 改为：

```vue
    <footer
      v-if="hasForward"
      class="dl-bar"
    >
```

只把 `v-if="hasForward || hasReverse"` 改为 `v-if="hasForward"`. 内容保持。

注意：reverse 模式的 download error 提示也需要兼顾。Reverse 模式的 downloadError 在 header 下载按钮触发时仍会设值，但 `<footer>` 不显示。为了不丢失这个 error feedback，在 page-header `<template v-if>` 块内的 download 按钮之后**追加** error 提示：

```vue
      </template>
      <p
        v-if="downloadError && project?.mode === 'reverse'"
        class="dl-error"
        role="alert"
        style="width: 100%; margin-top: 8px"
      >
        下载失败：{{ downloadError }}
      </p>
```

放在 `</header>` 之前。

- [ ] **Step 4: 验证 dev server visual smoke（可选，subagent 可跳过）**

如果时间允许、有 dev server，可视化扫一眼。否则跳到 step 5。

- [ ] **Step 5: vitest 短跑 sanity**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web && pnpm vitest run src/__tests__/views/ResultView.test.ts 2>&1 | tail -25
```

Expected: 与 T1 后状态一致（reverse mode 少数 test fail 预期，T3 修）。

- [ ] **Step 6: vue-tsc**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web && pnpm vue-tsc --noEmit --pretty false 2>&1 | tail -10
```

Expected: 0 errors。

- [ ] **Step 7: Commit**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用 && git add web/src/views/ResultView.vue && git commit -m "feat(result): page-header + hero-bg + reverse download 上移到 header (v2.4 T2)"
```

---

## Task 3: 更新 ResultView.test.ts reverse mode 单测

**Files:**
- Modify: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/web/src/__tests__/views/ResultView.test.ts`

### 现状

`web/src/__tests__/views/ResultView.test.ts` line 95-112 的 reverse mode test 断言 `<fieldset>` 存在（T1 已删除 fieldset）。

### 目标

- 修旧 test：去除 `<fieldset>` 断言，改为断言 4 列 grid + ResultTrio
- 新增 1 个 availableBudget computed test

### 步骤

- [ ] **Step 1: 读现状**

```bash
sed -n '95,115p' web/src/__tests__/views/ResultView.test.ts
```

定位 test `"reverse 模式：显示反算输入区..."` 的内容。

- [ ] **Step 2: 替换该 test 断言**

把 line 95-112 的 test 整段替换为：

```ts
  it("reverse 模式：显示 4 列反算输入区 + 反算按钮 (v2.4)", async () => {
    vi.mocked(projectsApi.get).mockResolvedValueOnce(reverseProject);
    router.push("/projects/2/result");
    await router.isReady();
    const w = mount(ResultView, {
      props: { projectId: "p-2" },
      global: { plugins: [createPinia(), router] },
    });
    await flushPromises();
    await flushPromises();
    // v2.4 — 4 列 grid 代替原 fieldset
    expect(w.text()).toContain("反算输入");
    expect(w.text()).toContain("目标总造价");
    expect(w.text()).toContain("其他费用");
    expect(w.text()).toContain("可用预算");
    expect(w.text()).toContain("α 开发占比");
    const buttons = w.findAll("button");
    const reverseBtn = buttons.find((b) => b.text() === "反算");
    expect(reverseBtn).toBeDefined();
  });
```

- [ ] **Step 3: 增加 availableBudget computed test**

紧跟该 test 之后追加：

```ts
  it("reverse 模式：可用预算 = target - other（disabled 字段）(v2.4)", async () => {
    vi.mocked(projectsApi.get).mockResolvedValueOnce(reverseProject);
    router.push("/projects/2/result");
    await router.isReady();
    const w = mount(ResultView, {
      props: { projectId: "p-2" },
      global: { plugins: [createPinia(), router] },
    });
    await flushPromises();
    await flushPromises();
    // 模拟用户输入
    const inputs = w.findAll("input");
    const targetInput = inputs.find((i) => (i.element as HTMLInputElement).getAttribute("type") === "number");
    expect(targetInput).toBeDefined();
    await targetInput!.setValue(1500000);
    const otherInput = inputs.filter((i) =>
      (i.element as HTMLInputElement).getAttribute("type") === "number"
    )[1];
    await otherInput.setValue(60000);
    await flushPromises();
    // 可用预算应为 1,440,000（zh-CN locale 带逗号）
    expect(w.text()).toContain("1,440,000");
  });
```

- [ ] **Step 4: 增加 ResultTrio 渲染断言（替代原 ResultCard）**

在 `"reverse 模式：targetTotal>0 → 调 reverse API 并显示三档 FP"` test 末尾（line 187-220 附近的 test）加一行断言。先 grep 找位置：

```bash
grep -n "reverseResult.value\|targetTotal>0\|显示三档 FP" web/src/__tests__/views/ResultView.test.ts | head -5
```

找到该 test 末尾 `expect(calcApi.reverse).toHaveBeenCalledWith(...)` 之后。在 `})` 闭合**前**插入：

```ts
    // v2.4 — ResultTrio 渲染（替代旧 ResultCard × 3）
    await flushPromises();
    const trio = w.find(".result-trio");
    expect(trio.exists()).toBe(true);
```

注：若该 test 的 reverse API mock 没返回完整 scale_adjusted_bands / scale_unadjusted_bands / recommended_band，会导致 reverseTiers computed 出错。检查 mock 数据完整：

```bash
sed -n '187,220p' web/src/__tests__/views/ResultView.test.ts
```

确认 mock 含：

```ts
{
  budget_for_dev: ...,
  budget_for_ops: ...,
  scale_adjusted_bands: { P10: ..., P50: ..., P90: ... },
  scale_unadjusted_bands: { P10: ..., P50: ..., P90: ... },
  scale_adjusted_ops_bands: { P10: 0, P50: 0, P90: 0 },
  scale_unadjusted_ops_bands: { P10: 0, P50: 0, P90: 0 },
  cf_used: ...,
  recommended_band: "P50",
}
```

如果 mock 缺 `scale_unadjusted_bands`（新加的 ResultTrio extras 依赖），补上：

```ts
scale_unadjusted_bands: { P10: 297.7, P50: 275.0, P90: 252.4 },
```

- [ ] **Step 5: 跑改后 test**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web && pnpm vitest run src/__tests__/views/ResultView.test.ts --reporter=verbose 2>&1 | tail -25
```

Expected: 全部 PASS（新加的 + 改的）。

- [ ] **Step 6: 全套 vitest**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web && pnpm vitest run 2>&1 | tail -5
```

Expected: 237+ PASS (236 + 1 new test)。

- [ ] **Step 7: Commit**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用 && git add web/src/__tests__/views/ResultView.test.ts && git commit -m "test(web): ResultView reverse mode test 适配 v2.4 — ResultTrio + availableBudget (v2.4 T3)"
```

---

## Task 4: e2e v2.4-reverse-visual + README v2.4 章节 + final 验证

**Files:**
- Create: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/web/tests/e2e/v2.4-reverse-visual.spec.ts`
- Modify: `/Users/renzhongyuan/WorkData/项目/2026/造价应用/README.md`
- Possibly Modify: `coverage-baseline.json`

### 步骤

- [ ] **Step 1: 创建 e2e spec**

```ts
import { test, expect, request } from "@playwright/test";

const BASE_URL = "http://127.0.0.1:8788";
const AUTH_TOKEN = "e2e-token";

async function createReverseProject(): Promise<string> {
  const ctx = await request.newContext({
    baseURL: BASE_URL,
    extraHTTPHeaders: {
      "X-Auth-Token": AUTH_TOKEN,
      "content-type": "application/json",
      Origin: BASE_URL,
    },
  });
  const r = await ctx.post("/api/projects", {
    data: {
      name: `Reverse Visual E2E ${Date.now()}`,
      project_type: "dev_only",
      phase: "bidding",
      city: "北京",
      industry: "电子政务",
      mode: "reverse",
      basis_data_ver: "CSBMK-202510",
      target_cost: 1500000,
      other_cost: 60000,
    },
  });
  expect(r.status()).toBe(201);
  const body = await r.json();
  return body.data?.id ?? body.id;
}

test("v2.4 reverse: 4 列 grid + ResultTrio + hero-bg + header download", async ({ page }) => {
  const pid = await createReverseProject();
  await page.goto(`/projects/${pid}/result`);
  await page.waitForLoadState("networkidle");

  // hero-bg 应用到 .page
  await expect(page.locator(".page.hero-bg")).toBeVisible();

  // 反算输入卡片 4 列
  await expect(page.locator("label.field-label", { hasText: "目标总造价" })).toBeVisible();
  await expect(page.locator("label.field-label", { hasText: "其他费用" })).toBeVisible();
  await expect(page.locator("label.field-label", { hasText: "可用预算" })).toBeVisible();
  await expect(page.locator("label.field-label", { hasText: "α 开发占比" })).toBeVisible();

  // 可用预算 disabled
  const availableInput = page.locator("input").nth(2);
  await expect(availableInput).toBeDisabled();

  // 点反算
  await page.locator(".btn-primary", { hasText: "反算" }).click();
  await page.waitForLoadState("networkidle");

  // ResultTrio 3 卡片渲染（替代旧 ResultCard）
  await expect(page.locator(".result-trio .result-card")).toHaveCount(3);
  await expect(page.locator(".result-card.recommended")).toBeVisible();

  // FP 单位（reverse 模式 ResultTrio unit="fp"）
  await expect(page.locator(".result-card-amt").first()).toContainText("FP");

  // 卡片 label 文案
  await expect(page.locator(".result-card-name").nth(0)).toContainText("乐观");
  await expect(page.locator(".result-card-name").nth(1)).toContainText("中位");
  await expect(page.locator(".result-card-name").nth(2)).toContainText("保守");

  // page-header 含 reverse 下载按钮
  await expect(page.locator(".page-header .btn-primary", { hasText: "下载" })).toBeVisible();

  // AllocatorPanel 仍在
  await expect(page.locator(".allocator-panel")).toBeVisible();
});
```

写入 `/Users/renzhongyuan/WorkData/项目/2026/造价应用/web/tests/e2e/v2.4-reverse-visual.spec.ts`.

- [ ] **Step 2: 重建 dist**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web && pnpm build 2>&1 | tail -5
```

Expected: built in <1s, no error.

- [ ] **Step 3: 确认 server up**

```bash
curl -s --noproxy 127.0.0.1 http://127.0.0.1:8788/health
```

如果没起：

```bash
ps aux | grep uvicorn | grep -v grep | awk '{print $2}' | xargs -r kill
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/server && COST_AUTH_TOKEN=e2e-token COST_WEB_DIST_DIR=/Users/renzhongyuan/WorkData/项目/2026/造价应用/web/dist .venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 8788 > /tmp/server-v2.4.log 2>&1 &
sleep 3
curl -s --noproxy 127.0.0.1 http://127.0.0.1:8788/health
```

- [ ] **Step 4: 跑新 e2e**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web && pnpm playwright test tests/e2e/v2.4-reverse-visual.spec.ts --reporter=line 2>&1 | tail -15
```

Expected: 1 PASS.

如果 fail：
- `.page.hero-bg` 找不到 — 验证 T2 step 1 是否把 `class="page"` 改为 `class="page hero-bg"`
- ResultTrio 没 render — 可能是 reverseTiers computed 出错（mock data 缺字段；e2e 实际后端有完整 reverseResult，不应有此问题）
- "下载" header 按钮找不到 — 验证 T2 step 2 是否 v-if 条件正确

- [ ] **Step 5: 跑全套 e2e**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web && pnpm playwright test --reporter=line 2>&1 | tail -15
```

Expected: 26 PASS (25 + 1 new).

- [ ] **Step 6: 全套 vitest + lint + tsc final 验证**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web && pnpm vitest run 2>&1 | tail -5
```

Expected: 237 PASS.

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web && pnpm vue-tsc --noEmit --pretty false 2>&1 | tail -5
```

Expected: 0 errors.

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用/web && pnpm eslint --max-warnings 0 src 2>&1 | tail -3
```

Expected: clean.

- [ ] **Step 7: Coverage baseline 检查**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用 && ./scripts/check-coverage.sh 2>&1 | tail -10
```

如果 coverage 高于 baseline:

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用 && ./scripts/update-coverage-baseline.sh 2>&1 | tail -5
```

如果未变化，跳过 baseline 更新。

- [ ] **Step 8: README v2.4 章节**

Read `/Users/renzhongyuan/WorkData/项目/2026/造价应用/README.md` 末尾，找到 `## v2.3 — 闭环` 章节位置。

在 v2.3 章节**之后**追加：

```markdown
## v2.4 — 反算路径视觉升级

v2.4 把 ResultView 的 **reverse 分支**视觉对齐 v2.2 design import：

- 反算输入卡 → v2.2 `.card` + 4 列 `.field-row` grid（目标总造价 / 其他费 / 可用预算 disabled / α 开发占比 disabled）
- 三档 FP 卡片 → 用 v2.2 `ResultTrio` (unit="fp")，与 forward 同款组件
- `.page` 加 `hero-bg` accent gradient
- Reverse 下载按钮上移到 page-header 右侧（与 forward 一致体验）
- AllocatorPanel（v2.3）和反算算法保持不变

**升级（v2.3 → v2.4）：**

```bash
git pull
# 无 schema 改动，无 plugin 改动
```

**v2.4 测试基线：**

| 项 | 数量 |
|---|---:|
| pytest | 191 |
| vitest | 237+ |
| playwright e2e | 26 |
```

实际填入 step 6 测得数字。

- [ ] **Step 9: Commit + final summary**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用 && git add web/tests/e2e/v2.4-reverse-visual.spec.ts README.md coverage-baseline.json 2>/dev/null
git commit -m "test(e2e) + docs: v2.4 reverse visual + README + coverage baseline (v2.4 T4 final)"
```

如果 baseline 没改动，commit 会自动只含 e2e + README。

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用 && git log --oneline v2.3-closure..HEAD | wc -l && git status
```

Expected: 5 commits（spec + plan + T1-T4 ≈ 6 个 since plan 也是 commit），working tree clean。

---

## Self-Review

### Spec coverage

- ✅ §4 反算输入 4 列 grid → T1 step 4
- ✅ §4 ResultTrio 替代 ResultCard × 3 → T1 step 3 (computed) + step 4 (template)
- ✅ §4 Page header download 按钮上移 → T2 step 2
- ✅ §4 hero-bg → T2 step 1
- ✅ §5 文件清单 → 全部在 task 里
- ✅ §6.1-6.4 实施细节 → T1-T2 完整 cover
- ✅ §7 测试策略 → T3 + T4

### Placeholder scan

无 "TBD/TODO". 每个 step 含完整 vue / ts / shell 代码。

### Type consistency

- `availableBudget` computed 返回 string（toLocaleString）。Template 直接 `:value="availableBudget"` 显示在 input 里 ✓
- `reverseTiers` 返回 `Tier[]` 与 v2.2 P4 ResultTrio props.tiers 类型一致：包含 `key/label/cost/fp/recommended/unit/extras` ✓
- `project?.alpha_dev` 字段：从 v2.0 Project schema 已有 (default 1.0) ✓

### Risk notes

- T1 step 5 删 CSS：保留 `.field` / `.field-label` scoped — 它们 v2.2 已在 global.css 定义，删了不会破，但保留可以避免后续 e2e/snapshot 测试卡 specificity。**决定保留** 因为它们 harmless。
- T2 step 3 把 footer.dl-bar 改成 forward-only — reverse 模式 downloadError 仅在 page-header 内显示。这是 spec D 决策（download 按钮上移）。
- T3 step 4 检查 mock 是否含 `scale_unadjusted_bands` — 现有 reverse mock 可能没这个字段，确认补全。

---

**Plan complete.** 4 task · `v2.4-reverse-visual` branch.
