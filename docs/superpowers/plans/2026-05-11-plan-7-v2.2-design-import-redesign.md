# v2.2 Design Import 全屏重做 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 design import 6 屏（ProjectList · Wizard · FpEditor · ParamManager · Result · Audit）以像素级复刻落到现有 Vue 3 + Pinia + FastAPI 体系；补齐 4 个后端 endpoint 支撑 design 隐含数据。一次性发布为 v2.2.0。

**Architecture:** 5 phase / 30 task / 1 PR。Phase 1 重写设计 token；Phase 2 改 App shell 为 sidebar+topbar 双区；Phase 3-5 各屏重做 + 后端 endpoint 同步落地。沿用现有 component/composable 模式，新组件按 `shell/projects/result/audit/fp` 命名空间分目录。

**Tech Stack:** Vue 3.5 + Pinia + TypeScript + vite + vitest（单测）/ Playwright（e2e）; FastAPI + SQLAlchemy + Alembic + pytest（后端）; Noto Sans SC + JetBrains Mono（字体）。

**Branch:** `v2.2-design-import-redesign`（已 commit spec 在 8e0febf）。

**Design source:** `docs/design-imports/V2.2/project/src/` — 实施时直接读此目录对照像素效果。

---

## Phase 1 — 设计系统重做

### Task 1: 重写 `tokens.css` 为 design token + legacy alias

**Files:**
- Modify: `web/src/styles/tokens.css`

实施时不允许 silent drift：每个数值必须与 `docs/design-imports/V2.2/project/src/styles.css` 顶部 `:root` 块一致。Legacy alias 段保留所有旧名以避免组件样式断裂。

- [ ] **Step 1: 备份现有 tokens.css 内容到注释，然后整文件改写**

完整新 tokens.css：

```css
:root {
  /* === v2.2 主层 — 来自 design import (styles.css L2-L40) === */
  --bg: #F7F8FA;
  --surface: #FFFFFF;
  --surface-2: #FAFBFC;
  --surface-sunken: #F0F2F5;
  --border: #E4E7EB;
  --border-strong: #CBD2D9;
  --text: #0F172A;
  --text-2: #475569;
  --text-3: #7B8794;
  --text-4: #A8B0BB;

  --accent: #1E5EFF;
  --accent-hover: #1849D6;
  --accent-soft: #EAF0FF;
  --accent-soft-strong: #D6E0FF;

  --red: #DC2626;
  --red-soft: #FEE2E2;
  --amber: #B45309;
  --amber-soft: #FEF3C7;
  --amber-strong: #FBBF24;
  --green: #047857;
  --green-soft: #D1FAE5;
  --purple: #7C3AED;
  --purple-soft: #EDE9FE;
  --teal: #0E7490;
  --teal-soft: #CFFAFE;

  --radius: 6px;
  --radius-lg: 10px;
  --shadow-sm: 0 1px 2px rgba(15, 23, 42, 0.04), 0 1px 1px rgba(15, 23, 42, 0.03);
  --shadow-md: 0 4px 12px -2px rgba(15, 23, 42, 0.08), 0 2px 4px rgba(15, 23, 42, 0.04);
  --shadow-lg: 0 20px 40px -12px rgba(15, 23, 42, 0.14), 0 8px 16px -8px rgba(15, 23, 42, 0.08);

  --font-sans: "Noto Sans SC", -apple-system, BlinkMacSystemFont, "PingFang SC",
               "Microsoft YaHei", "Helvetica Neue", Helvetica, Arial, sans-serif;
  --font-mono: "JetBrains Mono", ui-monospace, "SF Mono", Menlo, Consolas, monospace;

  /* === 间距（与 v1.1 兼容） === */
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-5: 20px;
  --space-6: 24px;
  --space-8: 32px;
  --space-10: 40px;

  /* === 字号 — 13px 基线（design） === */
  --font-family: var(--font-sans);
  --font-size-xs: 11px;
  --font-size-sm: 13px;
  --font-size-md: 15px;
  --font-size-lg: 18px;
  --font-size-xl: 22px;
  --line-height-base: 1.5;
  --line-height-tight: 1.35;

  /* === 动效 === */
  --duration-fast: 150ms;
  --duration-normal: 250ms;
  --ease-out: cubic-bezier(0.16, 1, 0.3, 1);

  /* === Layout === */
  --layout-max-width: 1280px;
  --layout-sidebar-width: 232px;
  --layout-topbar-height: 56px;
  --touch-target: 32px;
  --touch-target-comfortable: 36px;

  /* === Sidebar 深色（design 独有） === */
  --sidebar-bg: #0F1626;
  --sidebar-text: #CBD5E1;
  --sidebar-text-muted: #64748B;
  --sidebar-active-bg: rgba(30, 94, 255, 0.16);
  --sidebar-active-text: #FFFFFF;
  --sidebar-border: rgba(255, 255, 255, 0.06);

  /* === Legacy alias — 旧 token 名向新主层映射，避免组件样式断裂 === */
  --color-bg: var(--bg);
  --color-bg-elevated: var(--surface);
  --color-bg-hover: var(--surface-2);
  --color-surface: var(--bg);
  --color-border: var(--border);
  --color-border-strong: var(--border-strong);
  --color-text: var(--text-2);
  --color-text-title: var(--text);
  --color-text-body: var(--text-2);
  --color-text-muted: var(--text-3);
  --color-text-disabled: var(--text-4);
  --color-primary: var(--accent);
  --color-primary-hover: var(--accent-hover);
  --color-primary-active: var(--accent-hover);
  --color-primary-bg: var(--accent-soft);
  --color-accent: var(--accent);
  --color-success: var(--green);
  --color-success-bg: var(--green-soft);
  --color-warning: var(--amber);
  --color-warning-bg: var(--amber-soft);
  --color-danger: var(--red);
  --color-danger-bg: var(--red-soft);
  --color-data: var(--purple);
  --color-data-bg: var(--purple-soft);
  --color-warn-bg: var(--amber-soft);
  --color-warn-stripe: var(--amber);
  --color-error: var(--red);
  --radius-sm: 2px;
  --radius-md: var(--radius);
  --layout-header-height: var(--layout-topbar-height);
}
```

- [ ] **Step 2: 运行单测验证 legacy alias 链路不破**

```bash
cd web && pnpm vitest run --reporter=verbose 2>&1 | tail -30
```

Expected: 220 + 之前的 isolation smoke 仍 PASS（任何引用 legacy token 的组件都走 alias，不应 fail）。

- [ ] **Step 3: Commit**

```bash
git add web/src/styles/tokens.css
git commit -m "feat(design): tokens.css 重写为 v2.2 design import token + legacy alias"
```

---

### Task 2: 重写 `global.css` 加 13px 基线 + `.app/.sidebar/.topbar` grid + utility class

**Files:**
- Modify: `web/src/styles/global.css`

新增 utility class 来源：`docs/design-imports/V2.2/project/src/styles.css` 中 `.mono / .muted / .chip / .badge / .table / .card / .field / .seg / .timeline / .kpi-card / .toolbar / .factor-chain / .result-card / .phase-card / .pipeline-cell / .tl-* / .nav-* / .topbar / .crumbs` 等。

- [ ] **Step 1: 整文件改写**

`global.css` 完整替换为：

```css
* , *::before, *::after { box-sizing: border-box; }
html, body { margin: 0; padding: 0; }
body {
  font-family: var(--font-sans);
  font-size: 13px;
  line-height: 1.5;
  color: var(--text);
  background: var(--bg);
  -webkit-font-smoothing: antialiased;
  font-feature-settings: "tnum" 1;
}
button, input, select, textarea { font: inherit; color: inherit; }
button { cursor: pointer; background: none; border: 0; padding: 0; }
a { color: inherit; text-decoration: none; }

/* === Utility === */
.mono { font-family: var(--font-mono); font-feature-settings: "tnum" 1; }
.muted { color: var(--text-3); }
.spacer { flex: 1; }
.page-spacer { flex: 1; }

/* === App shell layout === */
.app {
  display: grid;
  grid-template-columns: var(--layout-sidebar-width) 1fr;
  grid-template-rows: 100vh;
  height: 100vh;
  min-width: 1280px;
}
.app-main {
  display: flex;
  flex-direction: column;
  overflow: hidden;
}
.app-content {
  flex: 1;
  overflow: auto;
  padding: 24px 28px;
}
.page { max-width: 1280px; margin: 0 auto; }
.page-header {
  display: flex;
  align-items: flex-end;
  gap: 12px;
  margin-bottom: 20px;
}
.page-title {
  font-size: 22px;
  font-weight: 600;
  letter-spacing: -0.01em;
  color: var(--text);
  margin: 0;
}
.page-title.tight { font-size: 18px; }
.page-sub { color: var(--text-3); font-size: 12px; margin-top: 4px; }
.section { margin-top: 20px; }
.section-head { margin-bottom: 14px; }
.section-title { font-size: 15px; font-weight: 600; }
.section-sub { color: var(--text-3); font-size: 12px; }
.hero-bg {
  background: linear-gradient(180deg, var(--accent-soft) 0%, var(--bg) 200px);
}

/* === Card === */
.card {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-sm);
}

/* === Buttons (overrides legacy) === */
.btn {
  display: inline-flex; align-items: center; justify-content: center; gap: 6px;
  height: var(--touch-target-comfortable);
  padding: 0 14px;
  font-size: 13px;
  border: 1px solid var(--border);
  border-radius: var(--radius);
  background: var(--surface);
  color: var(--text);
  transition: all .15s var(--ease-out);
}
.btn:hover { border-color: var(--accent); color: var(--accent); }
.btn:active { transform: scale(.98); }
.btn-sm { height: var(--touch-target); padding: 0 10px; font-size: 12px; }
.btn-primary {
  background: var(--accent); border-color: var(--accent); color: #fff;
}
.btn-primary:hover { background: var(--accent-hover); border-color: var(--accent-hover); color: #fff; }
.btn-ghost { border-color: transparent; background: transparent; }
.btn-ghost:hover { background: var(--surface-2); color: var(--accent); border-color: transparent; }
.btn-danger { background: var(--red); border-color: var(--red); color: #fff; }
.btn-icon { width: 32px; padding: 0; }
.btn-disabled, .btn:disabled { opacity: .5; cursor: not-allowed; }

.icon-btn {
  width: 32px; height: 32px; border-radius: var(--radius);
  display: inline-flex; align-items: center; justify-content: center;
  color: var(--text-3);
}
.icon-btn:hover { background: var(--surface-2); color: var(--accent); }

/* === Form fields === */
.field { margin-bottom: 16px; }
.field-row { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
.field-label { display: block; font-size: 12px; color: var(--text-2); font-weight: 500; margin-bottom: 6px; }
.field-input, .field-select {
  width: 100%; height: 36px; padding: 0 12px;
  border: 1px solid var(--border); border-radius: var(--radius);
  background: var(--surface); color: var(--text); font-size: 13px;
}
.field-input:focus, .field-select:focus { outline: 2px solid var(--accent-soft-strong); outline-offset: -1px; border-color: var(--accent); }
.field-hint { font-size: 11px; color: var(--text-3); margin-top: 4px; }

/* === Badges + chips === */
.badge {
  display: inline-flex; align-items: center; gap: 4px;
  padding: 2px 8px; border-radius: 10px;
  font-size: 11px; font-weight: 500; line-height: 1.4;
  background: var(--surface-2); color: var(--text-2);
  border: 1px solid var(--border);
}
.badge-blue { background: var(--accent-soft); color: var(--accent); border-color: var(--accent-soft-strong); }
.badge-amber { background: var(--amber-soft); color: var(--amber); border-color: var(--amber-strong); }
.badge-green { background: var(--green-soft); color: var(--green); border-color: var(--green); }
.badge-purple, .badge-violet { background: var(--purple-soft); color: var(--purple); border-color: var(--purple); }
.badge-teal { background: var(--teal-soft); color: var(--teal); border-color: var(--teal); }
.badge-dot::before { content: ""; width: 6px; height: 6px; border-radius: 50%; background: currentColor; margin-right: 2px; }
.chip {
  display: inline-flex; align-items: center; padding: 2px 8px;
  background: var(--surface-2); border: 1px solid var(--border);
  border-radius: 10px; font-size: 11px; color: var(--text-2);
}
.cat {
  display: inline-block; padding: 1px 6px;
  font-family: var(--font-mono); font-size: 10px; font-weight: 600;
  border-radius: 3px;
}
.cat-EI { background: #DBEAFE; color: #1E40AF; }
.cat-EO { background: #FED7AA; color: #9A3412; }
.cat-EQ { background: #D1FAE5; color: #065F46; }
.cat-ILF { background: #EDE9FE; color: #5B21B6; }
.cat-EIF { background: #FCE7F3; color: #9D174D; }

/* === Tables === */
.table {
  width: 100%; border-collapse: collapse;
  font-size: 12px; background: var(--surface);
}
.table th {
  text-align: left; font-weight: 500; color: var(--text-3);
  padding: 10px 12px;
  background: var(--surface-2);
  border-bottom: 1px solid var(--border);
}
.table td {
  padding: 10px 12px; border-bottom: 1px solid var(--border);
  vertical-align: middle;
}
.table tr.row-link { cursor: pointer; }
.table tr.row-link:hover { background: var(--surface-2); }
.table tr.override-row td { background: var(--amber-soft); }
.override-cell { font-weight: 600; }

/* === Tabs === */
.tabs { display: flex; gap: 4px; border-bottom: 1px solid var(--border); }
.tab {
  padding: 10px 14px;
  font-size: 13px; color: var(--text-3); cursor: pointer;
  border-bottom: 2px solid transparent;
  display: inline-flex; align-items: center; gap: 8px;
}
.tab.active { color: var(--text); border-bottom-color: var(--accent); font-weight: 500; }
.tab .count { font-size: 11px; color: var(--text-3); }

/* === Banner === */
.banner {
  display: flex; align-items: center; gap: 10px;
  padding: 10px 14px;
  border-radius: var(--radius);
  font-size: 12px;
}
.banner-blue { background: var(--accent-soft); color: var(--accent); }
.banner-amber { background: var(--amber-soft); color: var(--amber); }
.banner-green { background: var(--green-soft); color: var(--green); }

/* === Toolbar === */
.toolbar {
  display: flex; align-items: center; gap: 8px;
  padding: 12px 0; margin-bottom: 8px;
}
.seg {
  display: inline-flex; border: 1px solid var(--border); border-radius: var(--radius);
  overflow: hidden;
}
.seg button { padding: 6px 10px; color: var(--text-3); border-right: 1px solid var(--border); }
.seg button:last-child { border-right: 0; }
.seg button.active { background: var(--accent-soft); color: var(--accent); }
```

> 该 utility 仅是 Phase 1 基础；Phase 2/3/4/5 各 task 还会按需追加（KPI / wizard / fp-layout / timeline / result-trio 等命名空间下的 utility）。

- [ ] **Step 2: 重新跑 dev server 检查现有页面是否 visual regression**

```bash
cd web && pnpm dev
# 访问 http://127.0.0.1:5173 — 项目列表、wizard、FpEditor 应仍可渲染（字号略缩、配色微变），
# 不应出现破版或 console error
```

Expected: 5 个视图都能渲染（视觉效果待 Phase 2-5 完整改造，本步只保证不破）。手动 visual smoke 后 Ctrl+C 停 dev server。

- [ ] **Step 3: Commit**

```bash
git add web/src/styles/global.css
git commit -m "feat(design): global.css 13px 基线 + .app shell grid + design utility 集合"
```

---

### Task 3: `index.html` 加 Noto Sans SC + JetBrains Mono preload

**Files:**
- Modify: `web/index.html`

- [ ] **Step 1: 在 `<head>` 加字体 preconnect + preload + link**

定位 `web/index.html` 的 `<head>` 段，在 `<title>` 后插入：

```html
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Noto+Sans+SC:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500;600&display=swap" />
```

(注：`display=swap` 符合性能规则 web/performance.md)

- [ ] **Step 2: 验证字体加载**

```bash
cd web && pnpm dev
# 访问 / — DevTools Network 检查 fonts.googleapis.com 200，
# body 计算样式 font-family 第一项是 "Noto Sans SC"
```

- [ ] **Step 3: Commit**

```bash
git add web/index.html
git commit -m "feat(design): index.html 加 Noto Sans SC + JetBrains Mono 字体 preload"
```

---

## Phase 2 — App Shell

### Task 4: `Sidebar.vue` — 深色侧边栏 + 导航

**Files:**
- Create: `web/src/components/shell/Sidebar.vue`

参考 `docs/design-imports/V2.2/project/src/app.jsx` 的 `Sidebar` 函数 L31-86。

- [ ] **Step 1: 创建文件**

```vue
<script setup lang="ts">
import { useRoute, useRouter } from "vue-router";
import { computed } from "vue";

const route = useRoute();
const router = useRouter();

interface NavItem {
  key: string;
  label: string;
  icon: string;
  to?: string;
  disabled?: boolean;
}

const MAIN_NAV: NavItem[] = [
  { key: "projects", label: "项目工作台", icon: "folder", to: "/" },
  { key: "params-global", label: "全局参数库", icon: "settings", to: "/params/global" },
  { key: "templates", label: "模板与场景", icon: "layers", disabled: true },
  { key: "reports", label: "报告中心", icon: "file", to: "/reports" },
  { key: "audit-global", label: "审计日志", icon: "history", to: "/audit" },
];

const projectId = computed<string | null>(() => {
  const id = route.params.id;
  return typeof id === "string" && id ? id : null;
});

const PROJECT_NAV = computed<NavItem[]>(() => {
  if (!projectId.value) return [];
  return [
    { key: "fp", label: "FP 编辑", icon: "grid", to: `/projects/${projectId.value}/functions` },
    { key: "result", label: "三档造价", icon: "trending", to: `/projects/${projectId.value}/result` },
    { key: "params", label: "参数管理", icon: "settings", to: `/projects/${projectId.value}/parameters` },
    { key: "audit", label: "审计", icon: "history", to: `/projects/${projectId.value}/audit` },
  ];
});

function isActive(item: NavItem): boolean {
  if (!item.to) return false;
  if (item.to === "/") return route.path === "/";
  return route.path.startsWith(item.to);
}

function go(item: NavItem) {
  if (item.disabled || !item.to) return;
  router.push(item.to);
}
</script>

<template>
  <aside class="sidebar">
    <div class="sidebar-brand">
      <div class="sidebar-brand-mark">F</div>
      <div class="sidebar-brand-text">
        <div class="sidebar-brand-title">FP-Studio</div>
        <div class="sidebar-brand-sub mono">v2.2 · CSBMK 202510</div>
      </div>
    </div>

    <nav class="sidebar-nav">
      <div class="sidebar-section-label">主导航</div>
      <a
        v-for="it in MAIN_NAV"
        :key="it.key"
        class="nav-item"
        :class="{ active: isActive(it), disabled: it.disabled }"
        @click="go(it)"
      >
        <span class="nav-icon" :data-icon="it.icon" />
        <span class="nav-label">{{ it.label }}</span>
        <span v-if="it.disabled" class="nav-soon">敬请期待</span>
      </a>

      <template v-if="projectId">
        <div class="sidebar-section-label">当前项目</div>
        <a
          v-for="it in PROJECT_NAV"
          :key="it.key"
          class="nav-item sub"
          :class="{ active: isActive(it) }"
          @click="go(it)"
        >
          <span class="nav-icon" :data-icon="it.icon" />
          <span class="nav-label">{{ it.label }}</span>
        </a>
      </template>
    </nav>

    <div class="sidebar-foot">
      <div class="sidebar-help mono">CSBMK®-202510</div>
    </div>
  </aside>
</template>

<style scoped>
.sidebar {
  background: var(--sidebar-bg);
  color: var(--sidebar-text);
  display: flex;
  flex-direction: column;
  overflow: hidden;
  height: 100vh;
}
.sidebar-brand {
  display: flex; align-items: center; gap: 10px;
  padding: 16px 18px 14px;
  border-bottom: 1px solid var(--sidebar-border);
}
.sidebar-brand-mark {
  width: 28px; height: 28px; border-radius: 7px;
  background: linear-gradient(135deg, #2563EB 0%, #4F46E5 100%);
  display: grid; place-items: center;
  color: white; font-weight: 700; font-family: var(--font-mono);
  font-size: 13px; letter-spacing: -0.5px;
}
.sidebar-brand-title { color: #fff; font-weight: 600; font-size: 13px; }
.sidebar-brand-sub { color: var(--sidebar-text-muted); font-size: 11px; }
.sidebar-nav { padding: 12px 8px; flex: 1; overflow-y: auto; }
.sidebar-section-label {
  color: var(--sidebar-text-muted);
  font-size: 10px; font-weight: 600;
  letter-spacing: 0.08em; text-transform: uppercase;
  padding: 12px 10px 4px;
}
.nav-item {
  display: flex; align-items: center; gap: 10px;
  padding: 7px 10px; border-radius: var(--radius);
  color: var(--sidebar-text); cursor: pointer;
  font-size: 13px; transition: background var(--duration-fast);
}
.nav-item:hover { background: rgba(255, 255, 255, 0.04); }
.nav-item.active {
  background: var(--sidebar-active-bg);
  color: var(--sidebar-active-text);
  box-shadow: inset 2px 0 0 var(--accent);
}
.nav-item.disabled { color: var(--sidebar-text-muted); cursor: not-allowed; }
.nav-item.sub { padding-left: 22px; }
.nav-icon {
  width: 14px; height: 14px;
  background: currentColor;
  mask: var(--icon-url) center/contain no-repeat;
  flex-shrink: 0;
}
.nav-label { flex: 1; }
.nav-soon {
  font-size: 10px; padding: 1px 6px;
  background: rgba(255, 255, 255, 0.06); border-radius: 3px;
  color: var(--sidebar-text-muted);
}
.sidebar-foot { padding: 12px 18px; border-top: 1px solid var(--sidebar-border); }
.sidebar-help { color: var(--sidebar-text-muted); font-size: 10px; }
</style>
```

(注：`.nav-icon` 用 `data-icon` + CSS mask 技法。下一步加 icon stylesheet。)

- [ ] **Step 2: 新增 icon stylesheet** `web/src/styles/icons.css`

```css
/* Inline SVG icons referenced via data-icon. 来源：design ui.jsx 中 Icon 组件。 */
.nav-icon[data-icon="folder"] { --icon-url: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><path d="M22 19a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h5l2 3h9a2 2 0 0 1 2 2z"/></svg>'); }
.nav-icon[data-icon="settings"] { --icon-url: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-4 0v-.09a1.65 1.65 0 0 0-1-1.51 1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1 0-4h.09a1.65 1.65 0 0 0 1.51-1 1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 2.83-2.83l.06.06a1.65 1.65 0 0 0 1.82.33 1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82 1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1z"/></svg>'); }
.nav-icon[data-icon="layers"] { --icon-url: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><polygon points="12 2 2 7 12 12 22 7 12 2"/><polyline points="2 17 12 22 22 17"/><polyline points="2 12 12 17 22 12"/></svg>'); }
.nav-icon[data-icon="file"] { --icon-url: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/></svg>'); }
.nav-icon[data-icon="history"] { --icon-url: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><polyline points="23 4 23 10 17 10"/><path d="M20.49 15a9 9 0 1 1-2.12-9.36L23 10"/></svg>'); }
.nav-icon[data-icon="grid"] { --icon-url: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75"><polygon points="12 2 2 7 12 12 22 7 12 2"/><polyline points="2 17 12 22 22 17"/></svg>'); }
.nav-icon[data-icon="trending"] { --icon-url: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 18 15 12 9 6"/></svg>'); }
```

- [ ] **Step 3: 在 `main.ts` 引入 icons.css**

修改 `web/src/main.ts`：在 `import "./styles/global.css";` 之后插入：

```ts
import "./styles/icons.css";
```

- [ ] **Step 4: 写单测**

`web/tests/components/Sidebar.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { mount } from "@vue/test-utils";
import { createRouter, createMemoryHistory } from "vue-router";
import Sidebar from "@/components/shell/Sidebar.vue";

const router = createRouter({
  history: createMemoryHistory(),
  routes: [
    { path: "/", name: "home", component: { template: "<div/>" } },
    { path: "/params/global", name: "pg", component: { template: "<div/>" } },
    { path: "/projects/:id/functions", name: "fp", component: { template: "<div/>" } },
  ],
});

describe("Sidebar", () => {
  it("highlights active main nav item by route", async () => {
    await router.push("/");
    await router.isReady();
    const w = mount(Sidebar, { global: { plugins: [router] } });
    const items = w.findAll(".nav-item");
    expect(items[0].classes()).toContain("active");
  });

  it("hides project nav when not on a project route", async () => {
    await router.push("/");
    const w = mount(Sidebar, { global: { plugins: [router] } });
    expect(w.text()).not.toContain("FP 编辑");
  });

  it("shows project nav when on /projects/:id/*", async () => {
    await router.push("/projects/p-abc/functions");
    const w = mount(Sidebar, { global: { plugins: [router] } });
    expect(w.text()).toContain("FP 编辑");
  });

  it("marks 模板与场景 as disabled", () => {
    const w = mount(Sidebar, { global: { plugins: [router] } });
    const disabled = w.findAll(".nav-item.disabled");
    expect(disabled.length).toBe(1);
    expect(disabled[0].text()).toContain("模板与场景");
  });
});
```

- [ ] **Step 5: 跑测**

```bash
cd web && pnpm vitest run tests/components/Sidebar.test.ts --reporter=verbose
```

Expected: 4 PASS.

- [ ] **Step 6: Commit**

```bash
git add web/src/components/shell/Sidebar.vue web/src/styles/icons.css web/src/main.ts web/tests/components/Sidebar.test.ts
git commit -m "feat(shell): Sidebar 深色侧边栏 + nav + icon mask 系统"
```

---

### Task 5: `Breadcrumbs.vue` + `Topbar.vue` 框架

**Files:**
- Create: `web/src/components/shell/Breadcrumbs.vue`
- Create: `web/src/components/shell/Topbar.vue`

- [ ] **Step 1: 创建 Breadcrumbs.vue**

```vue
<script setup lang="ts">
import { useRoute, useRouter } from "vue-router";
import { computed } from "vue";
import { useProjectsStore } from "@/stores/projects";

const route = useRoute();
const router = useRouter();
const projectsStore = useProjectsStore();

interface Crumb { label: string; to?: string; }

const crumbs = computed<Crumb[]>(() => {
  const out: Crumb[] = [{ label: "项目工作台", to: "/" }];
  const id = typeof route.params.id === "string" ? route.params.id : null;

  if (route.name === "project-wizard") out.push({ label: "新建项目" });
  if (id) {
    const project = projectsStore.list.find((p) => p.id === id);
    out.push({ label: project?.name ?? id, to: `/projects/${id}/functions` });
    if (route.name === "param-manager") out.push({ label: "参数管理" });
    if (route.name === "result-view") out.push({ label: "三档造价" });
    if (route.name === "project-audit") out.push({ label: "审计" });
  }
  if (route.name === "audit-global") out[0] = { label: "审计日志" };
  if (route.name === "params-global") out[0] = { label: "全局参数库" };
  if (route.name === "report-center") out[0] = { label: "报告中心" };
  return out;
});

function jump(to: string | undefined) { if (to) router.push(to); }
</script>

<template>
  <nav class="crumbs" aria-label="面包屑">
    <template v-for="(c, i) in crumbs" :key="i">
      <span v-if="i > 0" class="crumb-sep" aria-hidden="true">›</span>
      <a class="crumb" :class="{ current: i === crumbs.length - 1 }" @click="jump(c.to)">
        {{ c.label }}
      </a>
    </template>
  </nav>
</template>

<style scoped>
.crumbs { display: flex; align-items: center; gap: 8px; font-size: 13px; }
.crumb { color: var(--text-3); cursor: pointer; }
.crumb:hover { color: var(--accent); }
.crumb.current { color: var(--text); font-weight: 500; cursor: default; }
.crumb-sep { color: var(--text-4); font-size: 11px; }
</style>
```

- [ ] **Step 2: 创建 Topbar.vue**

```vue
<script setup lang="ts">
import { ref } from "vue";
import { useRouter } from "vue-router";
import Breadcrumbs from "./Breadcrumbs.vue";

const router = useRouter();
const paletteOpen = ref(false);

function openPalette() { paletteOpen.value = true; }
function gotoAudit() { router.push("/audit"); }

defineExpose({ openPalette });
</script>

<template>
  <header class="topbar">
    <Breadcrumbs />
    <div class="topbar-spacer" />
    <button class="topbar-search" @click="openPalette" aria-label="打开命令面板 (⌘K)">
      <span class="topbar-search-icon">🔍</span>
      <span class="topbar-search-text">搜索项目、城市、模块… (⌘K)</span>
    </button>
    <button class="icon-btn" title="审计日志" @click="gotoAudit">📜</button>
    <button class="icon-btn" title="帮助">？</button>
    <div class="user-chip">
      <div class="user-avatar">用</div>
      <div class="user-meta">
        <div>当前用户</div>
        <div class="muted">咨询编辑</div>
      </div>
    </div>
  </header>
</template>

<style scoped>
.topbar {
  height: var(--layout-topbar-height);
  padding: 0 24px;
  background: var(--surface);
  border-bottom: 1px solid var(--border);
  display: flex; align-items: center; gap: 12px;
  flex-shrink: 0;
}
.topbar-spacer { flex: 1; }
.topbar-search {
  display: inline-flex; align-items: center; gap: 8px;
  height: 32px; padding: 0 12px; min-width: 320px;
  background: var(--surface-2); border: 1px solid var(--border);
  border-radius: var(--radius); color: var(--text-3); font-size: 12px;
}
.topbar-search:hover { border-color: var(--accent); }
.user-chip {
  display: inline-flex; align-items: center; gap: 8px;
  padding: 4px 10px 4px 4px;
  border-radius: 18px; border: 1px solid var(--border);
  background: var(--surface-2); font-size: 12px;
}
.user-avatar {
  width: 26px; height: 26px; border-radius: 50%;
  background: var(--accent); color: #fff;
  display: grid; place-items: center; font-weight: 600;
}
.user-meta { line-height: 1.2; }
.user-meta .muted { font-size: 10px; }
</style>
```

- [ ] **Step 3: 写单测**

`web/tests/components/Breadcrumbs.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { mount } from "@vue/test-utils";
import { createRouter, createMemoryHistory } from "vue-router";
import { createPinia, setActivePinia } from "pinia";
import Breadcrumbs from "@/components/shell/Breadcrumbs.vue";

describe("Breadcrumbs", () => {
  it("shows 项目工作台 + 新建项目 on wizard route", async () => {
    setActivePinia(createPinia());
    const router = createRouter({
      history: createMemoryHistory(),
      routes: [
        { path: "/", name: "project-list", component: { template: "<div/>" } },
        { path: "/projects/new", name: "project-wizard", component: { template: "<div/>" } },
      ],
    });
    await router.push("/projects/new");
    await router.isReady();
    const w = mount(Breadcrumbs, { global: { plugins: [router] } });
    expect(w.text()).toContain("项目工作台");
    expect(w.text()).toContain("新建项目");
  });
});
```

- [ ] **Step 4: 跑测**

```bash
cd web && pnpm vitest run tests/components/Breadcrumbs.test.ts
```

Expected: 1 PASS.

- [ ] **Step 5: Commit**

```bash
git add web/src/components/shell/Breadcrumbs.vue web/src/components/shell/Topbar.vue web/tests/components/Breadcrumbs.test.ts
git commit -m "feat(shell): Breadcrumbs + Topbar 框架（CommandPalette 集成留 T6）"
```

---

### Task 6: `CommandPalette.vue` — ⌘K 全局搜索

**Files:**
- Create: `web/src/components/shell/CommandPalette.vue`
- Modify: `web/src/components/shell/Topbar.vue`（接入 palette）

- [ ] **Step 1: 创建 CommandPalette.vue**

```vue
<script setup lang="ts">
import { ref, computed, watch, nextTick } from "vue";
import { useRouter } from "vue-router";
import { useProjectsStore } from "@/stores/projects";

const props = defineProps<{ open: boolean }>();
const emit = defineEmits<{ "update:open": [value: boolean] }>();

const router = useRouter();
const store = useProjectsStore();
const query = ref("");
const inputRef = ref<HTMLInputElement | null>(null);
const activeIdx = ref(0);

const results = computed(() => {
  const q = query.value.trim().toLowerCase();
  if (!q) return store.list.slice(0, 8);
  return store.list
    .filter((p) =>
      p.name.toLowerCase().includes(q) ||
      p.id.toLowerCase().includes(q) ||
      (p.client ?? "").toLowerCase().includes(q) ||
      p.city.toLowerCase().includes(q)
    )
    .slice(0, 8);
});

watch(() => props.open, async (isOpen) => {
  if (isOpen) {
    query.value = "";
    activeIdx.value = 0;
    await nextTick();
    inputRef.value?.focus();
  }
});

function close() { emit("update:open", false); }
function select(idx: number) {
  const project = results.value[idx];
  if (!project) return;
  router.push(`/projects/${project.id}/functions`);
  close();
}
function onKey(e: KeyboardEvent) {
  if (e.key === "Escape") close();
  if (e.key === "ArrowDown") { e.preventDefault(); activeIdx.value = Math.min(activeIdx.value + 1, results.value.length - 1); }
  if (e.key === "ArrowUp") { e.preventDefault(); activeIdx.value = Math.max(activeIdx.value - 1, 0); }
  if (e.key === "Enter") select(activeIdx.value);
}
</script>

<template>
  <div v-if="open" class="palette-backdrop" @click="close">
    <div class="palette" role="dialog" aria-label="命令面板" @click.stop>
      <input
        ref="inputRef"
        class="palette-input"
        v-model="query"
        placeholder="搜索项目名 / 编码 / 客户 / 城市..."
        @keydown="onKey"
      />
      <div class="palette-list">
        <div v-if="results.length === 0" class="palette-empty">
          无匹配项目 · 试试新建项目?
        </div>
        <div
          v-for="(p, i) in results"
          :key="p.id"
          class="palette-item"
          :class="{ active: i === activeIdx }"
          @click="select(i)"
          @mouseenter="activeIdx = i"
        >
          <div class="palette-name">{{ p.name }}</div>
          <div class="palette-meta mono">
            {{ p.id }} · {{ p.city }} · {{ p.industry }}
          </div>
        </div>
      </div>
      <div class="palette-foot mono">
        <span>↑↓ 选择</span><span>↵ 进入</span><span>esc 关闭</span>
      </div>
    </div>
  </div>
</template>

<style scoped>
.palette-backdrop {
  position: fixed; inset: 0;
  background: rgba(15, 23, 42, 0.45);
  display: grid; place-items: start center;
  padding-top: 12vh; z-index: 1000;
}
.palette {
  width: 560px; max-width: 92vw;
  background: var(--surface); border-radius: var(--radius-lg);
  box-shadow: var(--shadow-lg);
  overflow: hidden;
}
.palette-input {
  width: 100%; padding: 14px 18px;
  border: 0; outline: 0;
  font-size: 14px;
  border-bottom: 1px solid var(--border);
}
.palette-list { max-height: 50vh; overflow-y: auto; padding: 6px; }
.palette-empty { padding: 30px; text-align: center; color: var(--text-3); font-size: 12px; }
.palette-item {
  padding: 8px 12px; border-radius: var(--radius); cursor: pointer;
}
.palette-item.active { background: var(--accent-soft); }
.palette-name { font-weight: 500; color: var(--text); }
.palette-meta { color: var(--text-3); font-size: 11px; margin-top: 2px; }
.palette-foot {
  padding: 8px 18px; border-top: 1px solid var(--border);
  display: flex; gap: 16px; font-size: 11px; color: var(--text-3);
}
</style>
```

- [ ] **Step 2: 接入 Topbar.vue**

修改 `web/src/components/shell/Topbar.vue` 的 `<script setup>` 和 `<template>`：

`<script setup>` 末尾加：

```ts
import CommandPalette from "./CommandPalette.vue";
```

`<template>` 末尾（`</header>` 之前）加：

```vue
  <CommandPalette v-model:open="paletteOpen" />
```

注意：把原模板中 `<header class="topbar">...</header>` 包裹改为：

```vue
<template>
  <header class="topbar">
    <!-- 原有内容保持不变 -->
  </header>
  <CommandPalette v-model:open="paletteOpen" />
</template>
```

Vue 3 单根模板限制 — 需要在 root 加 `<div>` 包裹，或在 script 顶部 disable single-root：

简单做法：把整体包一层 `<div class="topbar-root">`:

```vue
<template>
  <div class="topbar-root">
    <header class="topbar">
      ...
    </header>
    <CommandPalette v-model:open="paletteOpen" />
  </div>
</template>
```

样式段 root：

```css
.topbar-root { display: contents; }
```

- [ ] **Step 3: 全局键盘快捷键** 在 `App.vue`（Task 7 改造时统一加，本步只准备 API）

`CommandPalette` 已通过 `v-model:open` 暴露开关，Task 7 会在 App.vue 加 `Mod+K` 监听。

- [ ] **Step 4: 写单测**

`web/tests/components/CommandPalette.test.ts`:

```ts
import { describe, it, expect, beforeEach } from "vitest";
import { mount } from "@vue/test-utils";
import { createRouter, createMemoryHistory } from "vue-router";
import { createPinia, setActivePinia } from "pinia";
import { useProjectsStore } from "@/stores/projects";
import CommandPalette from "@/components/shell/CommandPalette.vue";

describe("CommandPalette", () => {
  beforeEach(() => {
    setActivePinia(createPinia());
    const store = useProjectsStore();
    store.list = [
      { id: "p-001", name: "政务平台", city: "北京", industry: "电子政务" } as any,
      { id: "p-002", name: "税务系统", city: "杭州", industry: "金融" } as any,
    ];
  });

  it("filters projects by query", async () => {
    const router = createRouter({ history: createMemoryHistory(), routes: [{ path: "/", component: { template: "<div/>" } }] });
    const w = mount(CommandPalette, { props: { open: true }, global: { plugins: [router] } });
    await w.find(".palette-input").setValue("税务");
    const items = w.findAll(".palette-item");
    expect(items.length).toBe(1);
    expect(items[0].text()).toContain("税务系统");
  });

  it("emits update:open when esc pressed", async () => {
    const router = createRouter({ history: createMemoryHistory(), routes: [{ path: "/", component: { template: "<div/>" } }] });
    const w = mount(CommandPalette, { props: { open: true }, global: { plugins: [router] } });
    await w.find(".palette-input").trigger("keydown", { key: "Escape" });
    expect(w.emitted("update:open")?.[0]).toEqual([false]);
  });
});
```

- [ ] **Step 5: 跑测**

```bash
cd web && pnpm vitest run tests/components/CommandPalette.test.ts
```

Expected: 2 PASS.

- [ ] **Step 6: Commit**

```bash
git add web/src/components/shell/CommandPalette.vue web/src/components/shell/Topbar.vue web/tests/components/CommandPalette.test.ts
git commit -m "feat(shell): CommandPalette ⌘K 全局搜索 + Topbar 集成"
```

---

### Task 7: `App.vue` 改 sidebar + topbar 双区 + Mod+K 监听

**Files:**
- Modify: `web/src/App.vue`

- [ ] **Step 1: 整文件改写**

```vue
<script setup lang="ts">
import { onMounted, onBeforeUnmount, ref } from "vue";
import Sidebar from "@/components/shell/Sidebar.vue";
import Topbar from "@/components/shell/Topbar.vue";

const topbarRef = ref<InstanceType<typeof Topbar> | null>(null);

function onKey(e: KeyboardEvent) {
  const isMod = e.metaKey || e.ctrlKey;
  if (isMod && e.key.toLowerCase() === "k") {
    e.preventDefault();
    topbarRef.value?.openPalette();
  }
}

onMounted(() => window.addEventListener("keydown", onKey));
onBeforeUnmount(() => window.removeEventListener("keydown", onKey));
</script>

<template>
  <div class="app">
    <Sidebar />
    <div class="app-main">
      <Topbar ref="topbarRef" />
      <main class="app-content" aria-live="polite">
        <router-view />
      </main>
    </div>
  </div>
</template>
```

- [ ] **Step 2: 验证 dev server**

```bash
cd web && pnpm dev
# 访问 / — 左边出现深色 sidebar，右边出现 topbar + 内容区。
# 按 ⌘K（Mac）/ Ctrl+K（Windows）— palette 弹出。
# 关闭 dev server 后继续。
```

- [ ] **Step 3: Commit**

```bash
git add web/src/App.vue
git commit -m "feat(shell): App.vue 改 sidebar+topbar 双区 + Mod+K 监听"
```

---

### Task 8: 路由加 `/params/global`、`/reports`、`/audit`（全局）

**Files:**
- Modify: `web/src/router/index.ts`
- Create: `web/src/views/AuditGlobalView.vue`（最简 stub，复用 AuditView 但不带 :id 过滤）

- [ ] **Step 1: 修改 router/index.ts**

在 `const routes: RouteRecordRaw[] = [` 数组内，在 `not-found` 之前插入：

```ts
  {
    path: "/params/global",
    name: "params-global",
    component: () => import("@/views/ParamManager.vue"),
    props: () => ({ projectId: null }),
  },
  {
    path: "/reports",
    name: "report-center",
    component: () => import("@/views/ReportCenterView.vue"),
  },
  {
    path: "/audit",
    name: "audit-global",
    component: () => import("@/views/AuditGlobalView.vue"),
  },
```

(`ReportCenterView.vue` 在 Task 29 创建；本步先让路由占位 — 用 lazy import 即可避免编译期错误，但运行期访问会 404。Task 29 落地后即可。)

(`AuditGlobalView.vue` 本 task 创建占位；Task 28 重做 AuditView 时联动。)

- [ ] **Step 2: 创建 AuditGlobalView.vue 占位**

```vue
<script setup lang="ts">
import AuditView from "./AuditView.vue";
</script>

<template>
  <AuditView :global="true" />
</template>
```

> `AuditView` Task 28 时会加 `global?: boolean` prop — 此处先 declare prop 路径，Task 28 把它接通。

- [ ] **Step 3: 创建 ReportCenterView.vue 占位**（Task 29 完整实装）

```vue
<script setup lang="ts">
import { ref, onMounted } from "vue";
import { projectsApi, type Project } from "@/api/projects";
import { reportsApi } from "@/api/reports";

const projects = ref<Project[]>([]);
onMounted(async () => {
  const res = await projectsApi.list();
  projects.value = res.filter((p) => p.basis_data_ver);
});

async function downloadReport(id: string) {
  const blob = await reportsApi.excel(id);
  const url = URL.createObjectURL(blob);
  const a = document.createElement("a");
  a.href = url; a.download = `${id}.xlsx`; a.click();
  URL.revokeObjectURL(url);
}
</script>

<template>
  <div class="page">
    <div class="page-header">
      <div>
        <h1 class="page-title">报告中心</h1>
        <div class="page-sub">已计算项目的 Excel 报告导出入口</div>
      </div>
    </div>
    <div class="card" style="padding: 0">
      <table class="table">
        <thead><tr><th>项目</th><th>城市/行业</th><th>更新时间</th><th>操作</th></tr></thead>
        <tbody>
          <tr v-for="p in projects" :key="p.id">
            <td><b>{{ p.name }}</b><div class="muted mono" style="font-size:11px">{{ p.id }}</div></td>
            <td>{{ p.city }} · {{ p.industry }}</td>
            <td class="muted mono" style="font-size:11px">—</td>
            <td><button class="btn btn-sm" @click="downloadReport(p.id)">下载 P50 报告</button></td>
          </tr>
        </tbody>
      </table>
    </div>
  </div>
</template>
```

- [ ] **Step 4: 验证编译**

```bash
cd web && pnpm vue-tsc --noEmit --pretty false 2>&1 | tail -20
```

Expected: 无 type error。如果报错 `'@/api/reports'` 没有 `reportsApi`，本步先把 import 改为 `import { excelReport } from "@/api/reports"` 并相应改 call site — 取决于现有 reports.ts export 名。检查 `web/src/api/reports.ts` 实际 export。

- [ ] **Step 5: Commit**

```bash
git add web/src/router/index.ts web/src/views/AuditGlobalView.vue web/src/views/ReportCenterView.vue
git commit -m "feat(shell): 路由加 /params/global + /reports + /audit 全局入口"
```

---

### Task 9: E2E `v2.2-design-shell.spec.ts`

**Files:**
- Create: `web/tests/e2e/v2.2-design-shell.spec.ts`

- [ ] **Step 1: 创建 spec**

```ts
import { test, expect } from "@playwright/test";

test.describe("v2.2 Design Shell", () => {
  test("sidebar shows 5 main nav items + project sub-nav appears on project route", async ({ page }) => {
    await page.goto("/");
    const sidebar = page.locator(".sidebar");
    await expect(sidebar).toBeVisible();
    await expect(sidebar.locator(".nav-item").first()).toContainText("项目工作台");
    await expect(sidebar.locator(".nav-item.disabled")).toContainText("模板与场景");

    // Project nav should NOT show on list page
    await expect(sidebar.locator(".nav-item.sub")).toHaveCount(0);

    // Navigate to a project (any first row)
    const firstRow = page.locator("tr.row-link, tr[data-project-id]").first();
    if (await firstRow.count() > 0) {
      await firstRow.click();
      // Sidebar 当前项目 section
      await expect(sidebar.locator(".nav-item.sub")).toHaveCount(4);
      await expect(sidebar).toContainText("FP 编辑");
    }
  });

  test("Cmd+K opens command palette", async ({ page }) => {
    await page.goto("/");
    const meta = process.platform === "darwin" ? "Meta" : "Control";
    await page.keyboard.press(`${meta}+KeyK`);
    const palette = page.locator(".palette");
    await expect(palette).toBeVisible();
    await page.keyboard.press("Escape");
    await expect(palette).not.toBeVisible();
  });

  test("breadcrumb updates with route", async ({ page }) => {
    await page.goto("/projects/new");
    await expect(page.locator(".crumbs")).toContainText("项目工作台");
    await expect(page.locator(".crumbs")).toContainText("新建项目");
  });
});
```

- [ ] **Step 2: 跑 e2e**

```bash
cd web && pnpm playwright test tests/e2e/v2.2-design-shell.spec.ts --reporter=line
```

Expected: 3 PASS（前提：dev server 已起，data fixtures 含至少 1 个项目）。如本地无 server，先 `pnpm dev:fullstack`（或参考 README）。

- [ ] **Step 3: Commit**

```bash
git add web/tests/e2e/v2.2-design-shell.spec.ts
git commit -m "test(e2e): v2.2 design shell — sidebar + ⌘K + breadcrumb"
```

---

## Phase 3 — ProjectList + Stats Endpoint

### Task 10: `services/projects.py` — `get_stats(db, month)` 业务函数

**Files:**
- Modify: `server/app/services/projects.py`
- Create: `server/app/tests/integration/test_v2_2_projects_stats_service.py`

- [ ] **Step 1: 写失败测**

```python
"""v2.2 — projects.get_stats 单测：当月聚合 + 上月对比 + counts。"""
import pytest
from datetime import datetime, timedelta, timezone
from app.services import projects as ps
from app.db.models import Project


@pytest.mark.asyncio
async def test_get_stats_counts_by_status(db_session):
    now = datetime.now(timezone.utc)
    db_session.add_all([
        Project(id="p1", name="P1", project_type="dev_only", phase="bidding",
                city="北京", industry="电子政务", mode="forward",
                basis_data_ver="CSBMK-202510", status="草稿",
                created_at=now, updated_at=now),
        Project(id="p2", name="P2", project_type="dev_only", phase="bidding",
                city="北京", industry="电子政务", mode="forward",
                basis_data_ver="CSBMK-202510", status="已计算",
                created_at=now, updated_at=now),
        Project(id="p3", name="P3", project_type="dev_only", phase="bidding",
                city="北京", industry="电子政务", mode="forward",
                basis_data_ver="CSBMK-202510", status="已归档",
                created_at=now - timedelta(days=60), updated_at=now - timedelta(days=60)),
    ])
    db_session.commit()
    stats = ps.get_stats(db_session, month=now.strftime("%Y-%m"))
    assert stats["counts"]["total"] == 3
    assert stats["counts"]["draft"] == 1
    assert stats["counts"]["in_progress"] == 1
    assert stats["counts"]["archived"] == 1
    assert stats["monthly_count"] == 2  # p1 + p2 created this month
```

- [ ] **Step 2: 跑测 — 应该 FAIL（无 get_stats）**

```bash
cd server && .venv/bin/pytest tests/integration/test_v2_2_projects_stats_service.py -v
```

Expected: `AttributeError: module ... has no attribute 'get_stats'`

- [ ] **Step 3: 实装 get_stats**

打开 `server/app/services/projects.py`，文件末尾追加：

```python
from datetime import datetime, timezone
from sqlalchemy import func


def get_stats(db, month: str | None = None) -> dict:
    """v2.2 — 项目 KPI 汇总（counts + 本月 P50 sum + MoM 增长率）。

    month: "YYYY-MM"。None 表示当前自然月。
    """
    if not month:
        month = datetime.now(timezone.utc).strftime("%Y-%m")
    year, mo = month.split("-")
    year, mo = int(year), int(mo)

    # counts by status
    rows = db.query(Project.status, func.count(Project.id)).group_by(Project.status).all()
    counts_raw = {row[0]: row[1] for row in rows}
    total = sum(counts_raw.values())
    counts = {
        "total": total,
        "draft": counts_raw.get("草稿", 0),
        "in_progress": counts_raw.get("计算中", 0) + counts_raw.get("已计算", 0),
        "archived": counts_raw.get("已归档", 0),
        "delivered": counts_raw.get("已交付", 0),
    }

    # monthly count + P50 sum (本月)
    from calendar import monthrange
    start = datetime(year, mo, 1, tzinfo=timezone.utc)
    end_day = monthrange(year, mo)[1]
    end = datetime(year, mo, end_day, 23, 59, 59, tzinfo=timezone.utc)
    monthly_q = db.query(Project).filter(
        Project.created_at >= start, Project.created_at <= end
    )
    monthly_count = monthly_q.count()
    # P50 sum: prefer result_p50_total if cached, else 0
    monthly_p50_sum = 0.0
    for p in monthly_q.all():
        v = getattr(p, "result_p50_total", None)
        if v is not None:
            monthly_p50_sum += float(v)

    # last month P50 sum for MoM
    last_mo = mo - 1 or 12
    last_year = year if mo > 1 else year - 1
    last_start = datetime(last_year, last_mo, 1, tzinfo=timezone.utc)
    last_end = datetime(last_year, last_mo, monthrange(last_year, last_mo)[1], 23, 59, 59, tzinfo=timezone.utc)
    last_q = db.query(Project).filter(Project.created_at >= last_start, Project.created_at <= last_end)
    last_sum = 0.0
    for p in last_q.all():
        v = getattr(p, "result_p50_total", None)
        if v is not None:
            last_sum += float(v)
    growth_pct = ((monthly_p50_sum - last_sum) / last_sum * 100) if last_sum > 0 else 0.0

    return {
        "counts": counts,
        "monthly_count": monthly_count,
        "monthly_p50_sum": monthly_p50_sum,
        "monthly_growth_pct": round(growth_pct, 1),
    }
```

注：`Project.result_p50_total` 可能不存在 — 用 `getattr(..., None)` 兜底，没缓存时计 0。后续如需更准可加 alembic migration 加 `result_p50_total` column，本期 fallback 已够 KPI 展示用（前端配文案"基于已缓存计算"）。

- [ ] **Step 4: 跑测**

```bash
cd server && .venv/bin/pytest tests/integration/test_v2_2_projects_stats_service.py -v
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add server/app/services/projects.py server/tests/integration/test_v2_2_projects_stats_service.py
git commit -m "feat(server): projects.get_stats — KPI 聚合 + 本月 P50 sum + MoM 增长率"
```

---

### Task 11: `/api/projects/stats` endpoint + schema

**Files:**
- Modify: `server/app/api/projects.py`
- Modify: `server/app/schemas/project.py`
- Create: `server/tests/integration/test_v2_2_projects_stats_api.py`

- [ ] **Step 1: 加 schema**

在 `server/app/schemas/project.py` 末尾追加：

```python
class ProjectStatsCounts(BaseModel):
    total: int
    draft: int
    in_progress: int
    archived: int
    delivered: int

class ProjectStats(BaseModel):
    counts: ProjectStatsCounts
    monthly_count: int
    monthly_p50_sum: float
    monthly_growth_pct: float
```

- [ ] **Step 2: 加 endpoint**

修改 `server/app/api/projects.py`，找到 `router = APIRouter(...)` 后面，加：

```python
from .. import schemas
from ..services import projects as ps_svc


@router.get("/stats", response_model=schemas.project.ProjectStats)
def get_project_stats(month: str | None = None, db = Depends(get_db)):
    return ps_svc.get_stats(db, month=month)
```

(如果 import 风格不一样，按现有 file 风格调整 — 参考 `services/projects.py` 已有的 import 路径。)

- [ ] **Step 3: 写 integration test**

```python
"""v2.2 — /api/projects/stats endpoint."""
import pytest


@pytest.mark.asyncio
async def test_stats_endpoint_returns_envelope(client_factory):
    async with await client_factory() as client:
        r = await client.get("/api/projects/stats")
        assert r.status_code == 200
        body = r.json()
        assert "counts" in body
        assert "monthly_count" in body
        assert "monthly_p50_sum" in body
        assert "monthly_growth_pct" in body
        assert isinstance(body["counts"]["total"], int)


@pytest.mark.asyncio
async def test_stats_respects_month_query(client_factory):
    async with await client_factory() as client:
        r = await client.get("/api/projects/stats?month=2026-04")
        assert r.status_code == 200
```

- [ ] **Step 4: 跑测**

```bash
cd server && .venv/bin/pytest tests/integration/test_v2_2_projects_stats_api.py -v
```

Expected: 2 PASS.

- [ ] **Step 5: Commit**

```bash
git add server/app/api/projects.py server/app/schemas/project.py server/tests/integration/test_v2_2_projects_stats_api.py
git commit -m "feat(server): /api/projects/stats endpoint + schema (v2.2 P3)"
```

---

### Task 12: `web/src/api/stats.ts` client + `KpiCardRow.vue`

**Files:**
- Create: `web/src/api/stats.ts`
- Create: `web/src/components/projects/KpiCardRow.vue`

- [ ] **Step 1: 创建 stats.ts**

```ts
import { apiFetch } from "./client";

export interface ProjectStats {
  counts: {
    total: number;
    draft: number;
    in_progress: number;
    archived: number;
    delivered: number;
  };
  monthly_count: number;
  monthly_p50_sum: number;
  monthly_growth_pct: number;
}

export const statsApi = {
  async getProjectStats(month?: string): Promise<ProjectStats> {
    const qs = month ? `?month=${encodeURIComponent(month)}` : "";
    return apiFetch<ProjectStats>(`/api/projects/stats${qs}`);
  },
};
```

> 注：`apiFetch` 是 `client.ts` 现有 export。如果实际叫别的名（如 `request` / `httpClient`），用 `grep -n "export" web/src/api/client.ts` 查实际 export 名后调整。

- [ ] **Step 2: 创建 KpiCardRow.vue**

```vue
<script setup lang="ts">
import type { ProjectStats } from "@/api/stats";

const props = defineProps<{
  stats: ProjectStats;
  activeFilter: string;
}>();
const emit = defineEmits<{ filter: [key: string] }>();

const cards = [
  { key: "全部", label: "全部", icon: "folder", countKey: "total" as const },
  { key: "草稿", label: "草稿", icon: "edit", countKey: "draft" as const },
  { key: "计算中", label: "计算中 / 已计算", icon: "refresh", countKey: "in_progress" as const },
  { key: "已归档", label: "已归档", icon: "folder", countKey: "archived" as const },
];

function fmtWan(n: number): string {
  return (n / 10000).toFixed(2);
}
</script>

<template>
  <div class="kpi-row">
    <button
      v-for="c in cards"
      :key="c.key"
      class="kpi-card"
      :class="{ active: activeFilter === c.key }"
      @click="emit('filter', c.key)"
    >
      <div class="kpi-icon">📁</div>
      <div>
        <div class="kpi-label">{{ c.label }}</div>
        <div class="kpi-val mono">{{ stats.counts[c.countKey] }}</div>
      </div>
    </button>
    <div class="kpi-card kpi-summary">
      <div>
        <div class="kpi-label">本月总造价（P50）</div>
        <div class="kpi-val mono">¥{{ fmtWan(stats.monthly_p50_sum) }}<span class="kpi-unit">万</span></div>
      </div>
      <div class="kpi-trend" :class="stats.monthly_growth_pct >= 0 ? 'up' : 'down'">
        {{ stats.monthly_growth_pct >= 0 ? '↑' : '↓' }} {{ Math.abs(stats.monthly_growth_pct) }}%
      </div>
    </div>
  </div>
</template>

<style scoped>
.kpi-row {
  display: grid;
  grid-template-columns: repeat(4, 1fr) 1.5fr;
  gap: 12px;
  margin-bottom: 16px;
}
.kpi-card {
  display: flex; align-items: center; gap: 12px;
  padding: 14px 16px;
  background: var(--surface); border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  text-align: left;
}
.kpi-card.active {
  border-color: var(--accent);
  box-shadow: 0 0 0 2px var(--accent-soft);
}
.kpi-icon {
  width: 32px; height: 32px; border-radius: var(--radius);
  background: var(--accent-soft); color: var(--accent);
  display: grid; place-items: center;
}
.kpi-label { font-size: 11px; color: var(--text-3); }
.kpi-val { font-size: 22px; font-weight: 600; letter-spacing: -0.02em; color: var(--text); }
.kpi-unit { font-size: 12px; font-weight: 400; color: var(--text-3); margin-left: 2px; }
.kpi-summary {
  justify-content: space-between;
  background: linear-gradient(135deg, var(--accent-soft), var(--surface));
  border-color: var(--accent-soft-strong);
}
.kpi-trend.up { color: var(--green); }
.kpi-trend.down { color: var(--red); }
</style>
```

- [ ] **Step 3: Commit**

```bash
git add web/src/api/stats.ts web/src/components/projects/KpiCardRow.vue
git commit -m "feat(web): stats API client + KpiCardRow 组件"
```

---

### Task 13: `ProjectFilterBar.vue` — 城市/行业/阶段筛选

**Files:**
- Create: `web/src/components/projects/ProjectFilterBar.vue`

- [ ] **Step 1: 创建组件**

```vue
<script setup lang="ts">
import { computed } from "vue";

const props = defineProps<{
  city: string | null;
  industry: string | null;
  phase: string | null;
  view: "table" | "card";
  total: number;
  filtered: number;
}>();

const emit = defineEmits<{
  "update:city": [v: string | null];
  "update:industry": [v: string | null];
  "update:phase": [v: string | null];
  "update:view": [v: "table" | "card"];
}>();

const CITIES = ["全部", "北京", "上海", "深圳", "杭州", "苏州", "南京", "成都", "武汉"];
const INDUSTRIES = ["全部", "电子政务", "金融", "电信", "制造", "能源", "交通"];
const PHASES = [
  { value: "", label: "全部" },
  { value: "budget", label: "预算编制" },
  { value: "bidding", label: "招投标" },
  { value: "planning", label: "立项审批" },
  { value: "change", label: "变更评估" },
  { value: "settled", label: "结算审计" },
];

function setVal(k: "city" | "industry" | "phase", v: string) {
  const out = v === "全部" || v === "" ? null : v;
  if (k === "city") emit("update:city", out);
  if (k === "industry") emit("update:industry", out);
  if (k === "phase") emit("update:phase", out);
}
</script>

<template>
  <div class="toolbar">
    <select class="field-select compact" :value="city ?? '全部'" @change="setVal('city', ($event.target as HTMLSelectElement).value)">
      <option v-for="c in CITIES" :key="c" :value="c">城市：{{ c }}</option>
    </select>
    <select class="field-select compact" :value="industry ?? '全部'" @change="setVal('industry', ($event.target as HTMLSelectElement).value)">
      <option v-for="i in INDUSTRIES" :key="i" :value="i">行业：{{ i }}</option>
    </select>
    <select class="field-select compact" :value="phase ?? ''" @change="setVal('phase', ($event.target as HTMLSelectElement).value)">
      <option v-for="p in PHASES" :key="p.value" :value="p.value">阶段：{{ p.label }}</option>
    </select>
    <div style="flex: 1" />
    <div class="seg">
      <button :class="{ active: view === 'table' }" @click="emit('update:view', 'table')" aria-label="表格视图">⊞</button>
      <button :class="{ active: view === 'card' }" @click="emit('update:view', 'card')" aria-label="卡片视图">▦</button>
    </div>
    <span class="muted" style="font-size: 12px">{{ filtered }} / {{ total }}</span>
  </div>
</template>

<style scoped>
.field-select.compact { width: auto; height: 32px; font-size: 12px; padding: 0 28px 0 10px; }
</style>
```

- [ ] **Step 2: Commit**

```bash
git add web/src/components/projects/ProjectFilterBar.vue
git commit -m "feat(web): ProjectFilterBar 城市/行业/阶段 + 视图切换"
```

---

### Task 14: `ProjectList.vue` 重做 — KPI + Filter + table/card 视图

**Files:**
- Modify: `web/src/views/ProjectList.vue`

- [ ] **Step 1: 备份现有 ProjectList**（不需要单独提交，git 会留 diff）

- [ ] **Step 2: 整文件改写**

打开 `web/src/views/ProjectList.vue`，整文件替换：

```vue
<script setup lang="ts">
import { ref, computed, onMounted } from "vue";
import { useRouter } from "vue-router";
import { projectsApi, type Project, type ProjectQuery } from "@/api/projects";
import { statsApi, type ProjectStats } from "@/api/stats";
import { ApiError } from "@/api/client";
import KpiCardRow from "@/components/projects/KpiCardRow.vue";
import ProjectFilterBar from "@/components/projects/ProjectFilterBar.vue";
import LoadingSkeleton from "@/components/status/LoadingSkeleton.vue";
import EmptyState from "@/components/status/EmptyState.vue";
import ErrorBanner from "@/components/status/ErrorBanner.vue";
import ProjectActionMenu from "@/components/ProjectActionMenu.vue";

const COST_PER_WAN = 10000;
const PAGE_SIZE = 50;

const router = useRouter();
const items = ref<Project[]>([]);
const total = ref(0);
const stats = ref<ProjectStats | null>(null);
const page = ref(1);
const state = ref<"idle" | "loading" | "success" | "error">("idle");
const error = ref<ApiError | null>(null);

const filterStatus = ref<string>("全部");
const filterCity = ref<string | null>(null);
const filterIndustry = ref<string | null>(null);
const filterPhase = ref<string | null>(null);
const search = ref<string>("");
const view = ref<"table" | "card">("table");

const filtered = computed(() => {
  let xs = items.value;
  if (filterStatus.value !== "全部") {
    if (filterStatus.value === "计算中") {
      xs = xs.filter((p) => (p as any).status === "计算中" || (p as any).status === "已计算");
    } else {
      xs = xs.filter((p) => (p as any).status === filterStatus.value);
    }
  }
  if (search.value.trim()) {
    const q = search.value.toLowerCase();
    xs = xs.filter((p) =>
      p.name.toLowerCase().includes(q) ||
      p.id.toLowerCase().includes(q) ||
      (p.client ?? "").toLowerCase().includes(q)
    );
  }
  return xs;
});

const phaseLabel: Record<string, string> = {
  budget: "预算编制", bidding: "招投标", planning: "立项审批",
  change: "变更评估", settled: "结算审计",
};

async function load() {
  state.value = "loading";
  error.value = null;
  try {
    const q: ProjectQuery = {
      offset: (page.value - 1) * PAGE_SIZE,
      limit: PAGE_SIZE,
      city: filterCity.value ?? undefined,
      industry: filterIndustry.value ?? undefined,
      phase: (filterPhase.value || undefined) as any,
    };
    const res = await projectsApi.query(q);
    items.value = res.data;
    total.value = res.meta.total;
    state.value = "success";
  } catch (e) {
    error.value = e instanceof ApiError ? e : new ApiError("加载失败", -1);
    state.value = "error";
  }
}

async function loadStats() {
  try { stats.value = await statsApi.getProjectStats(); }
  catch { /* KPI 失败不阻断主流程 */ }
}

onMounted(() => { void load(); void loadStats(); });

function onFilter(key: string) {
  filterStatus.value = key;
}
function onOpen(p: Project) {
  router.push(`/projects/${p.id}/functions`);
}
function onNew() { router.push("/projects/new"); }

function fmtWan(n: number | null | undefined): string {
  if (n == null) return "—";
  return (n / COST_PER_WAN).toFixed(2);
}
function modeLabel(m: string): string {
  return m === "reverse" ? "反向反推" : "正向估算";
}
</script>

<template>
  <div class="page">
    <div class="page-header">
      <div>
        <h1 class="page-title">项目工作台</h1>
        <div class="page-sub">软件造价咨询项目 · GB/T 36964-2018 + CSBMK®-202510</div>
      </div>
      <div class="page-spacer" />
      <button class="btn btn-ghost">导入</button>
      <button class="btn btn-ghost">批量导出</button>
      <button class="btn btn-primary" @click="onNew">+ 新建项目</button>
    </div>

    <KpiCardRow v-if="stats" :stats="stats" :active-filter="filterStatus" @filter="onFilter" />

    <ProjectFilterBar
      v-model:city="filterCity"
      v-model:industry="filterIndustry"
      v-model:phase="filterPhase"
      v-model:view="view"
      :total="total"
      :filtered="filtered.length"
    />

    <ErrorBanner v-if="error" :error="error" @retry="load" />
    <LoadingSkeleton v-if="state === 'loading'" />
    <EmptyState v-else-if="filtered.length === 0" title="暂无项目" />

    <div v-else-if="view === 'table'" class="card" style="padding: 0">
      <table class="table">
        <thead>
          <tr>
            <th>项目名 / 编码</th>
            <th>模式</th>
            <th>客户 · 城市 · 行业</th>
            <th>阶段</th>
            <th style="text-align: right">规模 (FP)</th>
            <th style="text-align: right">P50 造价</th>
            <th>状态</th>
            <th>更新时间</th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          <tr v-for="p in filtered" :key="p.id" class="row-link" :data-project-id="p.id" @click="onOpen(p)">
            <td>
              <div style="font-weight: 500">{{ p.name }}</div>
              <div class="muted mono" style="font-size: 11px">{{ p.id }}</div>
            </td>
            <td><span class="badge" :class="p.mode === 'reverse' ? 'badge-amber' : 'badge-blue'">{{ modeLabel(p.mode) }}</span></td>
            <td>
              <div style="font-size: 12px">{{ p.client ?? '—' }}</div>
              <div class="muted" style="font-size: 11px">{{ p.city }} · {{ p.industry }}</div>
            </td>
            <td><span class="chip">{{ phaseLabel[p.phase] }}</span></td>
            <td class="mono" style="text-align: right">{{ ((p as any).scale_fp ?? '—') }}</td>
            <td class="mono" style="text-align: right; font-weight: 500">
              <span v-if="(p as any).result_p50_total">¥{{ fmtWan((p as any).result_p50_total) }}万</span>
              <span v-else class="muted">—</span>
            </td>
            <td><span class="badge">{{ (p as any).status ?? '草稿' }}</span></td>
            <td class="muted mono" style="font-size: 11px">{{ (p.updated_at ?? '').slice(0, 16) }}</td>
            <td @click.stop>
              <ProjectActionMenu :project="p" @refresh="load" />
            </td>
          </tr>
        </tbody>
      </table>
    </div>

    <div v-else class="card-grid">
      <div v-for="p in filtered" :key="p.id" class="card card-tile" @click="onOpen(p)">
        <div class="card-tile-head">
          <div>
            <div style="font-weight: 600; font-size: 14px">{{ p.name }}</div>
            <div class="muted mono" style="font-size: 11px">{{ p.id }}</div>
          </div>
          <span class="badge" :class="p.mode === 'reverse' ? 'badge-amber' : 'badge-blue'">{{ modeLabel(p.mode) }}</span>
        </div>
        <div class="card-tile-chips">
          <span class="chip">{{ p.city }}</span>
          <span class="chip">{{ p.industry }}</span>
          <span class="chip">{{ phaseLabel[p.phase] }}</span>
        </div>
        <div class="card-tile-foot">
          <div>
            <div class="muted" style="font-size: 11px">规模</div>
            <div class="mono">{{ (p as any).scale_fp ?? '—' }} FP</div>
          </div>
          <div style="text-align: right">
            <div class="muted" style="font-size: 11px">P50 造价</div>
            <div class="mono" style="font-weight: 600; color: var(--accent)">
              {{ (p as any).result_p50_total ? `¥${fmtWan((p as any).result_p50_total)}万` : '—' }}
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</template>

<style scoped>
.card-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(320px, 1fr)); gap: 12px; }
.card-tile { padding: 18px; cursor: pointer; transition: border-color var(--duration-fast); }
.card-tile:hover { border-color: var(--accent); }
.card-tile-head { display: flex; justify-content: space-between; align-items: flex-start; margin-bottom: 10px; }
.card-tile-chips { display: flex; gap: 6px; flex-wrap: wrap; margin-bottom: 12px; }
.card-tile-foot { display: flex; justify-content: space-between; padding-top: 12px; border-top: 1px solid var(--border); }
</style>
```

- [ ] **Step 3: 验证编译 + 视觉冒烟**

```bash
cd web && pnpm vue-tsc --noEmit --pretty false 2>&1 | tail -10
```

```bash
pnpm dev
# 访问 / — 看到 KPI cards + filter bar + table；点 KPI 卡过滤；切 card 视图正常。
```

- [ ] **Step 4: Commit**

```bash
git add web/src/views/ProjectList.vue
git commit -m "feat(projects): ProjectList 重做 — KPI cards + filter bar + table/card 视图切换"
```

---

### Task 15: E2E `v2.2-kpi-filter.spec.ts`

**Files:**
- Create: `web/tests/e2e/v2.2-kpi-filter.spec.ts`

- [ ] **Step 1: 创建 spec**

```ts
import { test, expect } from "@playwright/test";

test.describe("v2.2 ProjectList KPI + Filter", () => {
  test("KPI cards render with counts from /api/projects/stats", async ({ page }) => {
    await page.goto("/");
    await expect(page.locator(".kpi-row")).toBeVisible();
    await expect(page.locator(".kpi-card")).toHaveCount(5); // 4 status + 1 summary
    await expect(page.locator(".kpi-summary")).toContainText("本月总造价");
  });

  test("clicking 草稿 KPI card filters table", async ({ page }) => {
    await page.goto("/");
    await page.locator(".kpi-card", { hasText: "草稿" }).click();
    await expect(page.locator(".kpi-card.active", { hasText: "草稿" })).toBeVisible();
  });

  test("filter bar switches between table and card view", async ({ page }) => {
    await page.goto("/");
    const cardBtn = page.locator(".seg button").nth(1);
    await cardBtn.click();
    await expect(page.locator(".card-grid")).toBeVisible();
    await expect(page.locator(".table")).not.toBeVisible();
  });
});
```

- [ ] **Step 2: 跑 e2e**

```bash
cd web && pnpm playwright test tests/e2e/v2.2-kpi-filter.spec.ts --reporter=line
```

Expected: 3 PASS.

- [ ] **Step 3: Commit**

```bash
git add web/tests/e2e/v2.2-kpi-filter.spec.ts
git commit -m "test(e2e): v2.2 ProjectList KPI + filter + view 切换"
```

---

## Phase 4 — ResultView Pipeline + CostBar + Compliance

### Task 16: 扩展 `ForwardResult` — 加 `trace` + `composition`

**Files:**
- Modify: `server/app/core/forward.py`
- Create: `server/tests/integration/test_v2_2_forward_trace.py`

- [ ] **Step 1: 写失败测**

```python
"""v2.2 — forward calc 返回 trace + composition 字段。"""
from app.core.forward import calculate_forward, ForwardInput, FpItem
from app.core.context import EvaluationContext


def _ctx() -> EvaluationContext:
    return EvaluationContext(
        city="北京", industry="电子政务", phase="bidding", year_version="2024",
        cf_overrides={}, pdr_dev_overrides={}, pdr_ops_overrides={}, city_rate_overrides={},
    )


def test_forward_returns_trace():
    ctx = _ctx()
    res = calculate_forward(ctx, ForwardInput(
        items=[FpItem(us=275.0)],
        dev_factor=1.0, ops_factor=1.0,
        include_dev=True, include_ops=False, other_cost=25000,
    ))
    assert hasattr(res, "trace")
    t = res.trace
    assert t["us"] == 275.0
    assert "cf" in t
    assert "s_adjusted" in t
    assert "pdr_p50" in t
    assert "dev_factor" in t
    assert "eff_pm_p50" in t
    assert "f_city" in t
    assert "total_p50" in t


def test_forward_returns_composition():
    ctx = _ctx()
    res = calculate_forward(ctx, ForwardInput(
        items=[FpItem(us=275.0)],
        dev_factor=1.0, ops_factor=1.0,
        include_dev=True, include_ops=True, other_cost=25000,
    ))
    assert hasattr(res, "composition")
    c = res.composition
    assert "dev_labor" in c
    assert "ops_labor" in c
    assert "other" in c
    assert "indirect" in c
    # 4 段相加约等 total（允许 indirect 0~5% 误差）
    total = c["dev_labor"] + c["ops_labor"] + c["other"] + c["indirect"]
    assert abs(total - res.cost_total_yuan["P50"]) < 1.0
```

- [ ] **Step 2: 跑测 — 应该 FAIL**

```bash
cd server && .venv/bin/pytest tests/integration/test_v2_2_forward_trace.py -v
```

Expected: AttributeError trace / composition.

- [ ] **Step 3: 扩展 `ForwardResult` 和 `calculate_forward`**

打开 `server/app/core/forward.py`，找到 `ForwardResult` dataclass，**追加** 2 个字段：

```python
@dataclass
class ForwardResult:
    scale_us: float
    scale_adjusted: float
    cf_used: float
    effort_dev_hours: dict
    effort_ops_hours: dict
    cost_dev_yuan: dict
    cost_ops_yuan: dict
    cost_other_yuan: float
    cost_total_yuan: dict
    warning_messages: list[str] = field(default_factory=list)
    # v2.2 新增
    trace: dict = field(default_factory=dict)
    composition: dict = field(default_factory=dict)
```

修改 `calculate_forward` 函数末尾，在 `return ForwardResult(...)` 之前装配两个 dict：

```python
def calculate_forward(ctx: EvaluationContext, inp: ForwardInput) -> ForwardResult:
    us = sum(i.us for i in inp.items)
    cf = ctx.cf()
    s = us * cf
    eff_dev = ({b: s * ctx.pdr_dev(b) * inp.dev_factor for b in BANDS}
               if inp.include_dev else {b: 0.0 for b in BANDS})
    eff_ops = ({b: s * ctx.pdr_ops(b) * inp.ops_factor for b in BANDS}
               if inp.include_ops else {b: 0.0 for b in BANDS})
    pm = ctx.hours_per_pm
    rate_dev = ctx.city_rate_dev()
    rate_ops = ctx.city_rate_ops()
    cost_dev = {b: eff_dev[b] / pm * rate_dev for b in BANDS}
    cost_ops = {b: eff_ops[b] / pm * rate_ops for b in BANDS}
    total = {b: cost_dev[b] + cost_ops[b] + inp.other_cost for b in BANDS}

    # v2.2 trace: 9 步详解 — 仅 P50 档
    eff_pm_p50 = eff_dev["P50"] / pm if inp.include_dev else 0.0
    trace = {
        "us": us,
        "cf": cf,
        "s_adjusted": s,
        "pdr_p50": ctx.pdr_dev("P50") if inp.include_dev else 0.0,
        "dev_factor": inp.dev_factor,
        "eff_pm_p50": eff_pm_p50,
        "eff_hours_p50": eff_dev["P50"],
        "f_city": rate_dev,
        "ops_plus_other": cost_ops["P50"] + inp.other_cost,
        "total_p50": total["P50"],
    }

    # v2.2 composition: 4 段拆分（间接 = total - dev_labor - ops_labor - other，钳到 0+）
    indirect = max(0.0, total["P50"] - cost_dev["P50"] - cost_ops["P50"] - inp.other_cost)
    composition = {
        "dev_labor": cost_dev["P50"],
        "ops_labor": cost_ops["P50"],
        "other": inp.other_cost,
        "indirect": indirect,
    }

    return ForwardResult(
        scale_us=us, scale_adjusted=s, cf_used=cf,
        effort_dev_hours=eff_dev, effort_ops_hours=eff_ops,
        cost_dev_yuan=cost_dev, cost_ops_yuan=cost_ops,
        cost_other_yuan=inp.other_cost, cost_total_yuan=total,
        trace=trace, composition=composition,
    )
```

- [ ] **Step 4: 跑测**

```bash
cd server && .venv/bin/pytest tests/integration/test_v2_2_forward_trace.py -v
```

Expected: 2 PASS.

- [ ] **Step 5: 跑全套（确保现有测试不破）**

```bash
cd server && .venv/bin/pytest -q 2>&1 | tail -10
```

Expected: 175+ PASS（v2.1 的 173 + 本 phase 新增 2）。

- [ ] **Step 6: Commit**

```bash
git add server/app/core/forward.py server/tests/integration/test_v2_2_forward_trace.py
git commit -m "feat(server): ForwardResult 加 trace (9 步详解) + composition (4 段) — v2.2 P4"
```

---

### Task 17: `services/calc.py` — 让 trace/composition 自然透出到 API

**Files:**
- Verify: `server/app/services/calc.py:86` 返回 `out.__dict__`

由于 `ForwardResult` 是 dataclass，`__dict__` 会自然包含新加的字段，**这一 task 主要是验证**。

- [ ] **Step 1: 加一个 service-level 测试**

`server/tests/integration/test_v2_2_calc_service_trace.py`:

```python
"""v2.2 — services.calc.calc_forward dict 包含 trace 和 composition。"""
import pytest
from app.services.calc import calc_forward


@pytest.mark.asyncio
async def test_calc_forward_dict_has_trace(db_session, sample_project):
    payload = {
        "project_id": sample_project.id,
        "items": [{"us": 275.0}],
        "dev_factor": 1.0, "ops_factor": 1.0,
        "include_dev": True, "include_ops": False,
        "other_cost": 25000.0,
    }
    res = calc_forward(db_session, payload)
    assert "trace" in res
    assert "composition" in res
    assert res["trace"]["us"] == 275.0
    assert res["composition"]["dev_labor"] > 0
```

> `sample_project` fixture 需要在 conftest 加。如果没有，参考 v2.1 conftest 模式新建一个 minimal Project 行。

- [ ] **Step 2: 检查 conftest sample_project fixture**

```bash
cd server && grep -rn "sample_project" tests/ | head -5
```

若不存在，加到 `server/tests/conftest.py`：

```python
@pytest.fixture
def sample_project(db_session):
    from app.db.models import Project
    from datetime import datetime, timezone
    p = Project(
        id="p-test-001", name="测试项目",
        project_type="dev_only", phase="bidding",
        city="北京", industry="电子政务",
        mode="forward", basis_data_ver="CSBMK-202510",
        status="草稿",
        created_at=datetime.now(timezone.utc),
        updated_at=datetime.now(timezone.utc),
    )
    db_session.add(p)
    db_session.commit()
    db_session.refresh(p)
    return p
```

- [ ] **Step 3: 跑测**

```bash
cd server && .venv/bin/pytest tests/integration/test_v2_2_calc_service_trace.py -v
```

Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add server/tests/integration/test_v2_2_calc_service_trace.py server/tests/conftest.py
git commit -m "test(server): calc service trace/composition 透出验证 (v2.2 P4)"
```

---

### Task 18: `ResultTrio.vue` — 三档卡片组件

**Files:**
- Create: `web/src/components/result/ResultTrio.vue`

- [ ] **Step 1: 创建组件**

```vue
<script setup lang="ts">
interface Tier {
  key: "P10" | "P50" | "P90";
  label: string;
  cost: number;
  hours?: number;
  fp?: number;
  recommended?: boolean;
  unit?: "yuan" | "fp";
  extras?: Array<[string, string]>;
}

defineProps<{ tiers: Tier[] }>();

function fmtMoney(n: number): string {
  return Math.round(n).toLocaleString("zh-CN");
}
function fmtWan(n: number): string {
  return (n / 10000).toFixed(2);
}
</script>

<template>
  <div class="result-trio">
    <div
      v-for="t in tiers"
      :key="t.key"
      class="result-card"
      :class="{ recommended: t.recommended }"
    >
      <div v-if="t.recommended" class="result-card-pill">推荐 · P50</div>
      <div class="result-card-tag">{{ t.key }}</div>
      <div class="result-card-name">{{ t.label }}</div>
      <div class="result-card-amt" :class="{ dimmed: !t.recommended }">
        <template v-if="t.unit === 'fp'">
          {{ t.fp?.toFixed(2) }}<span class="unit">FP</span>
        </template>
        <template v-else>
          ¥{{ fmtMoney(t.cost) }}<span class="unit">元</span>
        </template>
      </div>
      <div class="muted mono" style="font-size: 12px">
        <template v-if="t.unit === 'fp'">≈ {{ t.fp ? Math.round(t.fp / 30) : '—' }} 个模块</template>
        <template v-else>= {{ fmtWan(t.cost) }} 万元</template>
      </div>
      <div v-if="t.extras && t.extras.length" class="result-card-extras">
        <div v-for="(row, i) in t.extras" :key="i" class="result-card-row">
          <span>{{ row[0] }}</span>
          <span class="v">{{ row[1] }}</span>
        </div>
      </div>
    </div>
  </div>
</template>

<style scoped>
.result-trio { display: grid; grid-template-columns: repeat(3, 1fr); gap: 14px; margin-bottom: 16px; }
.result-card {
  position: relative;
  padding: 22px 22px 20px;
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
}
.result-card.recommended {
  border-color: var(--accent);
  box-shadow: 0 0 0 3px var(--accent-soft);
}
.result-card-pill {
  position: absolute; top: -12px; left: 22px;
  padding: 4px 10px;
  background: var(--accent); color: #fff;
  font-size: 11px; font-weight: 600;
  border-radius: 10px;
}
.result-card-tag {
  font-family: var(--font-mono); font-size: 11px; font-weight: 600;
  color: var(--text-3);
  padding: 2px 8px;
  display: inline-block;
  border: 1px solid var(--border);
  border-radius: 4px;
  margin-bottom: 12px;
}
.result-card-name { font-size: 12px; color: var(--text-2); margin-bottom: 14px; }
.result-card-amt {
  font-family: var(--font-mono);
  font-size: 32px; font-weight: 600; letter-spacing: -0.02em;
  color: var(--accent);
  line-height: 1;
}
.result-card-amt.dimmed { color: var(--text-2); font-size: 26px; }
.result-card-amt .unit { font-size: 14px; font-weight: 400; color: var(--text-3); margin-left: 4px; }
.result-card-extras { margin-top: 16px; padding-top: 12px; border-top: 1px solid var(--border); }
.result-card-row { display: flex; justify-content: space-between; padding: 4px 0; font-size: 12px; }
.result-card-row span:first-child { color: var(--text-3); }
.result-card-row .v { font-family: var(--font-mono); color: var(--text); }
</style>
```

- [ ] **Step 2: Commit**

```bash
git add web/src/components/result/ResultTrio.vue
git commit -m "feat(result): ResultTrio 三档卡片组件（P10/P50/P90）"
```

---

### Task 19: `PipelineGrid.vue` — 9 步计算路径详解

**Files:**
- Create: `web/src/components/result/PipelineGrid.vue`

- [ ] **Step 1: 创建组件**

```vue
<script setup lang="ts">
interface PipelineTrace {
  us: number;
  cf: number;
  s_adjusted: number;
  pdr_p50: number;
  dev_factor: number;
  eff_pm_p50: number;
  eff_hours_p50: number;
  f_city: number;
  ops_plus_other: number;
  total_p50: number;
}

const props = defineProps<{ trace: PipelineTrace; phaseLabel: string; cityLabel: string }>();

const steps = computed(() => [
  { tag: "US", label: "未调整规模", val: props.trace.us.toFixed(2), unit: "FP", note: "Σ FP[i].us" },
  { tag: "×", label: "阶段调整因子 CF", val: props.trace.cf.toFixed(2), unit: "", note: props.phaseLabel },
  { tag: "S", label: "调整后规模", val: props.trace.s_adjusted.toFixed(2), unit: "FP", note: "US × CF", highlight: true },
  { tag: "÷", label: "PDR P50", val: props.trace.pdr_p50.toFixed(2), unit: "FP/PM", note: "CSBMK §4.1" },
  { tag: "×", label: "开发因子乘积", val: props.trace.dev_factor.toFixed(2), unit: "", note: "5 项" },
  { tag: "EFF", label: "开发工作量", val: props.trace.eff_pm_p50.toFixed(2), unit: "人月", note: `= ${props.trace.eff_hours_p50.toFixed(0)} 人时`, highlight: true },
  { tag: "×", label: `F_city (${props.cityLabel})`, val: props.trace.f_city.toLocaleString(), unit: "元/PM", note: "" },
  { tag: "+", label: "运维 + 其他费", val: Math.round(props.trace.ops_plus_other).toLocaleString(), unit: "元", note: "" },
  { tag: "=", label: "P50 总造价", val: Math.round(props.trace.total_p50).toLocaleString(), unit: "元", note: `≈ ${(props.trace.total_p50 / 10000).toFixed(2)} 万元`, final: true },
]);

import { computed } from "vue";
</script>

<template>
  <div class="pipeline-grid">
    <div
      v-for="(s, i) in steps"
      :key="i"
      class="pipeline-cell"
      :class="{ highlight: s.highlight, final: s.final }"
    >
      <div class="pipeline-head">
        <span class="pipeline-tag mono">{{ s.tag }}</span>
        <span class="muted" style="font-size: 11px">{{ s.label }}</span>
      </div>
      <div class="pipeline-val mono" :class="{ final: s.final }">
        {{ s.val }}<span class="unit">{{ s.unit }}</span>
      </div>
      <div v-if="s.note" class="muted mono" style="font-size: 10px; margin-top: 2px">{{ s.note }}</div>
    </div>
  </div>
</template>

<style scoped>
.pipeline-grid {
  display: grid; grid-template-columns: repeat(3, 1fr); gap: 10px;
}
.pipeline-cell {
  padding: 12px 14px;
  background: var(--surface-2);
  border: 1px solid var(--border);
  border-radius: 8px;
  position: relative;
}
.pipeline-cell.highlight {
  background: var(--accent-soft);
  border-color: var(--accent-soft-strong);
}
.pipeline-cell.final {
  background: var(--accent-soft);
  border-color: var(--accent);
}
.pipeline-head { display: flex; align-items: baseline; gap: 8px; }
.pipeline-tag {
  font-size: 11px; font-weight: 600;
  padding: 1px 6px;
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 4px;
  color: var(--text-3);
}
.pipeline-cell.final .pipeline-tag { color: var(--accent); border-color: var(--accent); }
.pipeline-val {
  font-size: 18px; font-weight: 600;
  margin-top: 6px;
  letter-spacing: -0.01em;
  color: var(--text);
}
.pipeline-val.final { font-size: 22px; color: var(--accent); }
.pipeline-val .unit { font-size: 12px; font-weight: 400; color: var(--text-3); margin-left: 4px; }
</style>
```

- [ ] **Step 2: Commit**

```bash
git add web/src/components/result/PipelineGrid.vue
git commit -m "feat(result): PipelineGrid — 9 步计算路径可视化"
```

---

### Task 20: `CostBar.vue` — 成本构成柱状图

**Files:**
- Create: `web/src/components/result/CostBar.vue`

- [ ] **Step 1: 创建组件**

```vue
<script setup lang="ts">
import { computed } from "vue";

interface Composition {
  dev_labor: number;
  ops_labor: number;
  other: number;
  indirect: number;
}

const props = defineProps<{ composition: Composition }>();

const items = computed(() => [
  { name: "开发人工", value: props.composition.dev_labor, color: "#2563EB" },
  { name: "运维人工", value: props.composition.ops_labor, color: "#7C3AED" },
  { name: "其他费用", value: props.composition.other, color: "#0E7490" },
  { name: "间接/管理", value: props.composition.indirect, color: "#94A3B8" },
].filter((it) => it.value > 0));

const total = computed(() => items.value.reduce((a, b) => a + b.value, 0));

function fmtMoney(n: number): string {
  return Math.round(n).toLocaleString("zh-CN");
}
function pct(v: number): number {
  if (total.value === 0) return 0;
  return (v / total.value) * 100;
}
</script>

<template>
  <div class="cost-bar">
    <div class="cost-bar-track">
      <div
        v-for="it in items"
        :key="it.name"
        :title="`${it.name} ¥${fmtMoney(it.value)}`"
        class="cost-bar-seg"
        :style="{ background: it.color, width: `${pct(it.value)}%` }"
      >
        <span v-if="pct(it.value) > 8">{{ pct(it.value).toFixed(1) }}%</span>
      </div>
    </div>
    <div class="cost-bar-legend">
      <div v-for="it in items" :key="it.name" class="cost-bar-row">
        <span class="cost-bar-dot" :style="{ background: it.color }" />
        <span class="cost-bar-name">{{ it.name }}</span>
        <span class="mono">¥{{ fmtMoney(it.value) }}</span>
        <span class="muted mono cost-bar-pct">{{ pct(it.value).toFixed(1) }}%</span>
      </div>
    </div>
  </div>
</template>

<style scoped>
.cost-bar-track {
  display: flex; height: 28px;
  border: 1px solid var(--border);
  border-radius: 6px;
  overflow: hidden;
}
.cost-bar-seg {
  display: grid; place-items: center;
  color: white; font-size: 11px; font-weight: 500;
}
.cost-bar-legend { display: grid; grid-template-columns: 1fr 1fr; gap: 8px; margin-top: 14px; }
.cost-bar-row { display: flex; align-items: center; gap: 8px; font-size: 12px; }
.cost-bar-dot { width: 10px; height: 10px; border-radius: 2px; flex-shrink: 0; }
.cost-bar-name { flex: 1; }
.cost-bar-pct { width: 50px; text-align: right; }
</style>
```

- [ ] **Step 2: Commit**

```bash
git add web/src/components/result/CostBar.vue
git commit -m "feat(result): CostBar — 4 段成本构成柱状图 + legend"
```

---

### Task 21: `ComplianceCard.vue` — 合规说明 + 报告话术

**Files:**
- Create: `web/src/components/result/ComplianceCard.vue`

- [ ] **Step 1: 创建组件**

```vue
<script setup lang="ts">
const COMPLIANCE = [
  ["GB/T 36964-2018", "§7.2 PDR 公式 · 三档行业基准"],
  ["T/CCUA 005-2024", "附录 D 算例 · 黄金测试基准"],
  ["GB/T 28827.7-2022", "§6 运维 11 项因子"],
  ["CSBMK®-202510", "1500+ 在网项目统计"],
];

defineProps<{ p50Wan: string }>();
</script>

<template>
  <div class="card compliance-card">
    <div class="section-title" style="margin-bottom: 14px">合规说明</div>
    <div style="display: grid; gap: 10px">
      <div v-for="row in COMPLIANCE" :key="row[0]" class="compliance-row">
        <span class="compliance-check">✓</span>
        <div>
          <div class="mono"><b>{{ row[0] }}</b></div>
          <div class="muted">{{ row[1] }}</div>
        </div>
      </div>
    </div>
    <div class="compliance-quote">
      <b>💡 报告话术：</b>本项目 P50 推荐造价 <span class="mono">{{ p50Wan }} 万元</span>；
      P10/P90 为基于 CSBMK®-202510 行业生产率分布的敏感度边界，<b>非团队承诺值</b>。
    </div>
  </div>
</template>

<style scoped>
.compliance-card { padding: 20px; }
.compliance-row { display: flex; gap: 10px; font-size: 12px; padding: 6px 0; border-bottom: 1px dashed var(--border); }
.compliance-row:last-child { border-bottom: 0; }
.compliance-check { color: var(--green); margin-top: 2px; }
.compliance-quote {
  margin-top: 14px; padding: 10px;
  background: var(--surface-sunken);
  border-radius: 6px;
  font-size: 11px; line-height: 1.6;
  color: var(--text-2);
}
</style>
```

- [ ] **Step 2: Commit**

```bash
git add web/src/components/result/ComplianceCard.vue
git commit -m "feat(result): ComplianceCard — 4 项国标 + 报告话术"
```

---

### Task 22: `ResultView.vue` 重做 + e2e

**Files:**
- Modify: `web/src/views/ResultView.vue`
- Create: `web/tests/e2e/v2.2-pipeline-render.spec.ts`
- Create: `web/tests/e2e/v2.2-cost-composition.spec.ts`

- [ ] **Step 1: 整文件改写 ResultView.vue**

参考 `docs/design-imports/V2.2/project/src/screens-result.jsx`。**保留** 现有的 store 接入、reverse 模式（allocator），**改写**：

打开 `web/src/views/ResultView.vue` — 这是 514 行的现有文件。整体结构改为：

```vue
<script setup lang="ts">
import { ref, onMounted, computed } from "vue";
import { useRouter } from "vue-router";
import { useResultsStore } from "@/stores/results";
import { useProjectsStore } from "@/stores/projects";
import { calcApi } from "@/api/calc";
import { reportsApi } from "@/api/reports";
import ResultTrio from "@/components/result/ResultTrio.vue";
import PipelineGrid from "@/components/result/PipelineGrid.vue";
import CostBar from "@/components/result/CostBar.vue";
import ComplianceCard from "@/components/result/ComplianceCard.vue";
import LoadingSkeleton from "@/components/status/LoadingSkeleton.vue";
import ErrorBanner from "@/components/status/ErrorBanner.vue";

const props = defineProps<{ projectId: string }>();
const router = useRouter();
const resultsStore = useResultsStore();
const projectsStore = useProjectsStore();

const state = ref<"idle" | "loading" | "success" | "error">("idle");
const error = ref<Error | null>(null);

const project = computed(() => projectsStore.list.find((p) => p.id === props.projectId));
const result = computed(() => resultsStore.byProjectId(props.projectId));

const phaseLabel: Record<string, string> = {
  budget: "预算编制", bidding: "招投标", planning: "立项审批",
  change: "变更评估", settled: "结算审计",
};

async function runCalc() {
  if (!project.value) return;
  state.value = "loading";
  try {
    const res = await calcApi.forward({
      project_id: props.projectId,
      include_dev: true,
      include_ops: project.value.include_ops,
      other_cost: project.value.other_cost ?? 0,
      dev_factor: 1.0,
      ops_factor: 1.0,
    });
    resultsStore.upsert(props.projectId, res);
    state.value = "success";
  } catch (e) {
    error.value = e as Error;
    state.value = "error";
  }
}

onMounted(async () => {
  await projectsStore.fetchAll();
  if (!result.value) await runCalc();
  else state.value = "success";
});

const tiersForward = computed(() => {
  if (!result.value) return [];
  return (["P10", "P50", "P90"] as const).map((k) => ({
    key: k,
    label: k === "P10" ? "乐观 · 行业最高效率" : k === "P50" ? "中位 · CSBMK 行业基准" : "保守 · 含返工/沟通损耗",
    cost: result.value!.cost_total_yuan[k],
    recommended: k === "P50",
    extras: [
      ["调整后规模 S", `${result.value!.scale_adjusted?.toFixed(2)} FP`],
      ["开发工作量", `${result.value!.effort_dev_hours[k]?.toFixed(2)} 人时`],
      ["运维工作量", `${result.value!.effort_ops_hours[k]?.toFixed(2)} 人时`],
      ["其他费用", `${(result.value!.cost_other_yuan ?? 0).toLocaleString()} 元`],
    ] as Array<[string, string]>,
  }));
});

async function downloadReport() {
  const blob = await reportsApi.excel(props.projectId);
  const url = URL.createObjectURL(blob);
  const a = document.createElement("a");
  a.href = url; a.download = `${project.value?.name ?? props.projectId}.xlsx`; a.click();
  URL.revokeObjectURL(url);
}

function fmtWan(n: number): string { return (n / 10000).toFixed(2); }
</script>

<template>
  <div class="page hero-bg">
    <div class="page-header">
      <button class="btn btn-ghost" @click="router.push(`/projects/${projectId}/functions`)">← FP 编辑</button>
      <div style="margin-left: 8px">
        <h1 class="page-title tight">三档造价 · {{ project?.name }}</h1>
        <div class="page-sub">基于 CSBMK®-202510 · P10/P50/P90 敏感度边界</div>
      </div>
      <div class="page-spacer" />
      <button class="btn btn-ghost" @click="runCalc">↻ 重新计算</button>
      <button class="btn btn-primary" @click="downloadReport">下载 Excel 报告</button>
    </div>

    <ErrorBanner v-if="error" :error="error" />
    <LoadingSkeleton v-if="state === 'loading'" />

    <template v-if="state === 'success' && result">
      <ResultTrio :tiers="tiersForward" />

      <div class="section">
        <div class="section-head">
          <div class="section-title">计算路径详解 · P50 推荐档</div>
          <div class="section-sub">附录 D 算例 · 政务服务平台 · 期望 ±100 元</div>
        </div>
        <div class="card" style="padding: 20px">
          <PipelineGrid
            v-if="result.trace"
            :trace="result.trace as any"
            :phase-label="phaseLabel[project?.phase ?? 'bidding']"
            :city-label="project?.city ?? '—'"
          />
          <div v-else class="muted">trace 数据待计算 — 点上方 "重新计算"</div>
        </div>
      </div>

      <div style="display: grid; grid-template-columns: 1.4fr 1fr; gap: 16px; margin-top: 16px">
        <div class="card" style="padding: 20px">
          <div style="display: flex; align-items: baseline; justify-content: space-between; margin-bottom: 14px">
            <div class="section-title">成本构成分布</div>
            <span class="muted mono" style="font-size: 11px">P50 · 合计 {{ Math.round(result.cost_total_yuan.P50).toLocaleString() }} 元</span>
          </div>
          <CostBar v-if="result.composition" :composition="result.composition as any" />
          <div v-else class="muted">composition 数据待计算</div>
        </div>
        <ComplianceCard :p50-wan="fmtWan(result.cost_total_yuan.P50)" />
      </div>
    </template>
  </div>
</template>
```

> 注：本 task **仅改 forward 路径**。reverse 路径在 task 末尾合并改造（如果 ResultView 当前已实装 reverse view，保留它并在合适位置加 ResultTrio + 一致性 banner）。

- [ ] **Step 2: 跑 dev server 视觉冒烟**

```bash
cd web && pnpm dev
# 访问 /projects/<id>/result — 看 3 张三档卡 + Pipeline 9 cell + CostBar + Compliance
```

- [ ] **Step 3: 创建 e2e v2.2-pipeline-render**

```ts
import { test, expect } from "@playwright/test";

test("ResultView forward 显示 Pipeline 9 步", async ({ page }) => {
  await page.goto("/");
  const firstRow = page.locator("tr.row-link").first();
  await firstRow.click();
  await page.locator("text=三档造价").first().click();
  await page.waitForLoadState("networkidle");
  const cells = page.locator(".pipeline-cell");
  await expect(cells).toHaveCount(9);
  await expect(page.locator(".pipeline-cell.final")).toContainText("元");
});
```

- [ ] **Step 4: 创建 e2e v2.2-cost-composition**

```ts
import { test, expect } from "@playwright/test";

test("ResultView CostBar 显示 4 段构成", async ({ page }) => {
  await page.goto("/");
  const firstRow = page.locator("tr.row-link").first();
  await firstRow.click();
  await page.locator("text=三档造价").first().click();
  await page.waitForLoadState("networkidle");
  await expect(page.locator(".cost-bar-track")).toBeVisible();
  const segments = page.locator(".cost-bar-seg");
  expect(await segments.count()).toBeGreaterThanOrEqual(2);
});
```

- [ ] **Step 5: 跑 e2e**

```bash
cd web && pnpm playwright test tests/e2e/v2.2-pipeline-render.spec.ts tests/e2e/v2.2-cost-composition.spec.ts
```

Expected: 2 PASS.

- [ ] **Step 6: Commit**

```bash
git add web/src/views/ResultView.vue web/tests/e2e/v2.2-pipeline-render.spec.ts web/tests/e2e/v2.2-cost-composition.spec.ts
git commit -m "feat(result): ResultView 重做 — ResultTrio + Pipeline + CostBar + Compliance + e2e"
```

---

## Phase 5 — Polish

### Task 23: `AiTask` model + Alembic migration

**Files:**
- Modify: `server/app/db/models.py`
- Create: `server/alembic/versions/c2f8a1e9d3b6_add_ai_tasks.py`

- [ ] **Step 1: 加 model**

打开 `server/app/db/models.py`，在末尾追加：

```python
class AiTask(Base):
    """v2.2 — AI 任务状态表，让前端 polling 查询提取/分摊进度。"""
    __tablename__ = "ai_tasks"
    id: Mapped[str] = mapped_column(String(36), primary_key=True)
    project_id: Mapped[str] = mapped_column(String(36), ForeignKey("projects.id", ondelete="CASCADE"), index=True)
    kind: Mapped[str] = mapped_column(String(32))  # "extract" | "allocate"
    status: Mapped[str] = mapped_column(String(16), default="queued")  # queued|running|done|failed
    progress_pct: Mapped[float] = mapped_column(Float, default=0.0)
    stage_log: Mapped[str] = mapped_column(Text, default="")
    output_json: Mapped[Optional[str]] = mapped_column(Text, nullable=True)
    error_message: Mapped[Optional[str]] = mapped_column(Text, nullable=True)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), default=lambda: datetime.now(timezone.utc))
    updated_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), default=lambda: datetime.now(timezone.utc),
                                                 onupdate=lambda: datetime.now(timezone.utc))
```

(必要时确认顶部 import 已有 `ForeignKey, Float, Text, datetime, timezone, Optional`。)

- [ ] **Step 2: 创建 migration**

```bash
cd server && .venv/bin/alembic revision --autogenerate -m "add_ai_tasks_table"
```

期望生成 `server/alembic/versions/<hash>_add_ai_tasks_table.py`。打开生成文件验证 `op.create_table("ai_tasks", ...)` 含全部字段；如缺索引，手动追加：

```python
def upgrade():
    op.create_table(
        "ai_tasks",
        sa.Column("id", sa.String(36), primary_key=True),
        sa.Column("project_id", sa.String(36), sa.ForeignKey("projects.id", ondelete="CASCADE"), nullable=False, index=True),
        sa.Column("kind", sa.String(32), nullable=False),
        sa.Column("status", sa.String(16), nullable=False, server_default="queued"),
        sa.Column("progress_pct", sa.Float, nullable=False, server_default="0"),
        sa.Column("stage_log", sa.Text, nullable=False, server_default=""),
        sa.Column("output_json", sa.Text, nullable=True),
        sa.Column("error_message", sa.Text, nullable=True),
        sa.Column("created_at", sa.DateTime(timezone=True), nullable=False),
        sa.Column("updated_at", sa.DateTime(timezone=True), nullable=False),
    )
    op.create_index("ix_ai_tasks_project_status", "ai_tasks", ["project_id", "status"])


def downgrade():
    op.drop_index("ix_ai_tasks_project_status", "ai_tasks")
    op.drop_table("ai_tasks")
```

- [ ] **Step 3: 跑 migration**

```bash
cd server && .venv/bin/alembic upgrade head
```

Expected: 输出 `Running upgrade <prev> -> <new>, add_ai_tasks_table`.

- [ ] **Step 4: Commit**

```bash
git add server/app/db/models.py server/alembic/versions/*add_ai_tasks*.py
git commit -m "feat(server): AiTask model + alembic migration (v2.2 P5)"
```

---

### Task 24: AiTask schema + service

**Files:**
- Create: `server/app/schemas/ai_tasks.py`
- Create: `server/app/services/ai_tasks.py`
- Create: `server/tests/integration/test_v2_2_ai_tasks_service.py`

- [ ] **Step 1: 写 schema**

```python
"""v2.2 — AiTask schemas."""
from pydantic import BaseModel, ConfigDict
from typing import Literal, Optional
from datetime import datetime


class AiTaskCreate(BaseModel):
    project_id: str
    kind: Literal["extract", "allocate"]


class AiTaskUpdate(BaseModel):
    status: Optional[Literal["queued", "running", "done", "failed"]] = None
    progress_pct: Optional[float] = None
    stage_log_append: Optional[str] = None
    output_json: Optional[str] = None
    error_message: Optional[str] = None


class AiTaskRead(BaseModel):
    id: str
    project_id: str
    kind: str
    status: str
    progress_pct: float
    stage_log: str
    output_json: Optional[str]
    error_message: Optional[str]
    created_at: datetime
    updated_at: datetime
    model_config = ConfigDict(from_attributes=True)
```

- [ ] **Step 2: 写 service**

```python
"""v2.2 — AiTask service：CRUD + adopt."""
import uuid
from sqlalchemy.orm import Session
from ..db.models import AiTask


def create_task(db: Session, project_id: str, kind: str) -> AiTask:
    task = AiTask(id=str(uuid.uuid4()), project_id=project_id, kind=kind, status="queued")
    db.add(task)
    db.commit()
    db.refresh(task)
    return task


def get_task(db: Session, task_id: str) -> AiTask | None:
    return db.query(AiTask).filter(AiTask.id == task_id).first()


def list_for_project(db: Session, project_id: str, limit: int = 10) -> list[AiTask]:
    return (db.query(AiTask)
            .filter(AiTask.project_id == project_id)
            .order_by(AiTask.created_at.desc())
            .limit(limit).all())


def update_task(db: Session, task_id: str, *, status: str | None = None,
                progress_pct: float | None = None,
                stage_log_append: str | None = None,
                output_json: str | None = None,
                error_message: str | None = None) -> AiTask | None:
    t = get_task(db, task_id)
    if not t: return None
    if status is not None: t.status = status
    if progress_pct is not None: t.progress_pct = progress_pct
    if stage_log_append is not None: t.stage_log = (t.stage_log or "") + stage_log_append + "\n"
    if output_json is not None: t.output_json = output_json
    if error_message is not None: t.error_message = error_message
    db.commit()
    db.refresh(t)
    return t
```

- [ ] **Step 3: 写测**

```python
"""v2.2 — AiTask service smoke."""
import pytest
from app.services import ai_tasks as svc


@pytest.mark.asyncio
async def test_create_and_progress(db_session, sample_project):
    t = svc.create_task(db_session, sample_project.id, "extract")
    assert t.status == "queued"
    assert t.progress_pct == 0.0
    t2 = svc.update_task(db_session, t.id, status="running", progress_pct=48.0, stage_log_append="✓ 章节切分")
    assert t2 is not None and t2.status == "running"
    assert "章节切分" in t2.stage_log
    listed = svc.list_for_project(db_session, sample_project.id)
    assert len(listed) == 1
```

- [ ] **Step 4: 跑测**

```bash
cd server && .venv/bin/pytest tests/integration/test_v2_2_ai_tasks_service.py -v
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add server/app/schemas/ai_tasks.py server/app/services/ai_tasks.py server/tests/integration/test_v2_2_ai_tasks_service.py
git commit -m "feat(server): ai_tasks schema + service (v2.2 P5)"
```

---

### Task 25: `/api/ai-tasks` API endpoints

**Files:**
- Create: `server/app/api/ai_tasks.py`
- Modify: `server/app/main.py`（注册路由）
- Create: `server/tests/integration/test_v2_2_ai_tasks_api.py`

- [ ] **Step 1: 创建 api 文件**

```python
"""v2.2 — /api/ai-tasks endpoints."""
from fastapi import APIRouter, Depends, HTTPException
from ..deps import get_db
from ..services import ai_tasks as svc
from ..schemas import ai_tasks as schemas


router = APIRouter(prefix="/api/ai-tasks", tags=["ai-tasks"])


@router.post("", response_model=schemas.AiTaskRead, status_code=201)
def create(payload: schemas.AiTaskCreate, db=Depends(get_db)):
    t = svc.create_task(db, payload.project_id, payload.kind)
    return t


@router.get("", response_model=list[schemas.AiTaskRead])
def list_tasks(project_id: str, db=Depends(get_db)):
    return svc.list_for_project(db, project_id)


@router.get("/{task_id}", response_model=schemas.AiTaskRead)
def read_one(task_id: str, db=Depends(get_db)):
    t = svc.get_task(db, task_id)
    if not t: raise HTTPException(404, "task not found")
    return t


@router.patch("/{task_id}", response_model=schemas.AiTaskRead)
def patch(task_id: str, payload: schemas.AiTaskUpdate, db=Depends(get_db)):
    t = svc.update_task(
        db, task_id,
        status=payload.status,
        progress_pct=payload.progress_pct,
        stage_log_append=payload.stage_log_append,
        output_json=payload.output_json,
        error_message=payload.error_message,
    )
    if not t: raise HTTPException(404, "task not found")
    return t
```

- [ ] **Step 2: 注册路由**

打开 `server/app/main.py`，找到现有 `app.include_router(...)` 调用群，**追加**：

```python
from .api import ai_tasks as ai_tasks_api
app.include_router(ai_tasks_api.router)
```

- [ ] **Step 3: 写 integration test**

```python
"""v2.2 — /api/ai-tasks 端到端。"""
import pytest


@pytest.mark.asyncio
async def test_ai_task_crud(client_factory, sample_project):
    async with await client_factory() as client:
        # create
        r = await client.post("/api/ai-tasks", json={"project_id": sample_project.id, "kind": "extract"})
        assert r.status_code == 201
        task_id = r.json()["id"]
        # patch progress
        r = await client.patch(f"/api/ai-tasks/{task_id}", json={"status": "running", "progress_pct": 48.0, "stage_log_append": "✓ 解析完成"})
        assert r.status_code == 200
        assert r.json()["progress_pct"] == 48.0
        # list
        r = await client.get(f"/api/ai-tasks?project_id={sample_project.id}")
        assert r.status_code == 200
        assert len(r.json()) == 1
```

- [ ] **Step 4: 跑测**

```bash
cd server && .venv/bin/pytest tests/integration/test_v2_2_ai_tasks_api.py -v
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add server/app/api/ai_tasks.py server/app/main.py server/tests/integration/test_v2_2_ai_tasks_api.py
git commit -m "feat(server): /api/ai-tasks CRUD endpoints (v2.2 P5)"
```

---

### Task 26: web 端 `aiTasks.ts` client + `useAiTaskPolling.ts` composable + `AiTaskModal.vue`

**Files:**
- Create: `web/src/api/aiTasks.ts`
- Create: `web/src/composables/useAiTaskPolling.ts`
- Create: `web/src/components/fp/AiTaskModal.vue`

- [ ] **Step 1: client**

```ts
import { apiFetch } from "./client";

export type AiTaskKind = "extract" | "allocate";
export type AiTaskStatus = "queued" | "running" | "done" | "failed";

export interface AiTask {
  id: string;
  project_id: string;
  kind: AiTaskKind;
  status: AiTaskStatus;
  progress_pct: number;
  stage_log: string;
  output_json: string | null;
  error_message: string | null;
  created_at: string;
  updated_at: string;
}

export const aiTasksApi = {
  list(projectId: string): Promise<AiTask[]> {
    return apiFetch<AiTask[]>(`/api/ai-tasks?project_id=${encodeURIComponent(projectId)}`);
  },
  get(id: string): Promise<AiTask> {
    return apiFetch<AiTask>(`/api/ai-tasks/${id}`);
  },
};
```

- [ ] **Step 2: composable**

```ts
import { ref, onUnmounted } from "vue";
import { aiTasksApi, type AiTask } from "@/api/aiTasks";

const POLL_INTERVAL_MS = 1500;

export function useAiTaskPolling(projectId: string) {
  const task = ref<AiTask | null>(null);
  const polling = ref(false);
  let timer: ReturnType<typeof setInterval> | null = null;

  async function fetchLatest() {
    try {
      const tasks = await aiTasksApi.list(projectId);
      task.value = tasks[0] ?? null;
    } catch {
      /* swallow — modal 不阻断 */
    }
  }

  function start() {
    if (polling.value) return;
    polling.value = true;
    void fetchLatest();
    timer = setInterval(() => {
      void fetchLatest();
      if (task.value && (task.value.status === "done" || task.value.status === "failed")) stop();
    }, POLL_INTERVAL_MS);
  }

  function stop() {
    polling.value = false;
    if (timer) clearInterval(timer);
    timer = null;
  }

  onUnmounted(stop);
  return { task, polling, start, stop, fetchLatest };
}
```

- [ ] **Step 3: AiTaskModal.vue**

```vue
<script setup lang="ts">
import { onMounted, onBeforeUnmount } from "vue";
import { useAiTaskPolling } from "@/composables/useAiTaskPolling";

const props = defineProps<{ open: boolean; projectId: string }>();
const emit = defineEmits<{ "update:open": [v: boolean] }>();

const { task, start, stop } = useAiTaskPolling(props.projectId);

onMounted(() => { if (props.open) start(); });
onBeforeUnmount(stop);

function close() { stop(); emit("update:open", false); }
</script>

<template>
  <div v-if="open" class="modal-backdrop" @click="close">
    <div class="card ai-modal" @click.stop>
      <div class="ai-modal-head">
        <div class="ai-modal-spark">✨</div>
        <div class="ai-modal-meta">
          <div class="ai-modal-title">Claude AI 提取任务</div>
          <div class="muted" style="font-size: 12px">
            <template v-if="task">任务 ID {{ task.id.slice(0, 8) }}</template>
            <template v-else>暂无任务 — 在终端运行 <span class="mono">/cost {{ projectId }}</span> 让 Claude 启动提取</template>
          </div>
        </div>
        <button class="btn btn-ghost btn-sm" @click="close">关闭</button>
      </div>

      <template v-if="task">
        <div class="ai-modal-log">
          <pre>{{ task.stage_log || '（等待 plugin 上报日志）' }}</pre>
        </div>
        <div class="ai-modal-progress">
          <div class="ai-modal-bar">
            <div class="ai-modal-bar-fill" :style="{ width: task.progress_pct + '%' }" />
          </div>
          <div class="muted mono" style="font-size: 11px; margin-top: 4px">{{ task.progress_pct.toFixed(0) }}% · {{ task.status }}</div>
        </div>
      </template>

      <div class="ai-modal-foot">
        <button class="btn btn-ghost" @click="close">{{ task && task.status === 'done' ? '查看结果' : '后台运行' }}</button>
        <button v-if="task && task.status === 'done'" class="btn btn-primary">采纳 FP</button>
      </div>
    </div>
  </div>
</template>

<style scoped>
.modal-backdrop {
  position: fixed; inset: 0;
  background: rgba(15, 23, 42, 0.45);
  display: grid; place-items: center;
  z-index: 1000;
}
.ai-modal { width: 540px; padding: 24px; }
.ai-modal-head { display: flex; align-items: center; gap: 12px; }
.ai-modal-spark {
  width: 40px; height: 40px; border-radius: 10px;
  background: linear-gradient(135deg, #2563EB, #7C3AED);
  color: white; display: grid; place-items: center; font-size: 20px;
}
.ai-modal-meta { flex: 1; }
.ai-modal-title { font-size: 16px; font-weight: 600; }
.ai-modal-log {
  margin-top: 20px; padding: 14px;
  background: var(--surface-sunken);
  border-radius: 8px;
  font-family: var(--font-mono); font-size: 11px;
  max-height: 200px; overflow-y: auto;
}
.ai-modal-log pre { margin: 0; white-space: pre-wrap; }
.ai-modal-progress { margin-top: 16px; }
.ai-modal-bar {
  height: 6px; background: var(--surface-sunken);
  border-radius: 3px; overflow: hidden;
}
.ai-modal-bar-fill {
  height: 100%; transition: width .3s;
  background: linear-gradient(90deg, var(--accent), #7C3AED);
}
.ai-modal-foot { display: flex; justify-content: flex-end; gap: 8px; margin-top: 20px; }
</style>
```

- [ ] **Step 4: Commit**

```bash
git add web/src/api/aiTasks.ts web/src/composables/useAiTaskPolling.ts web/src/components/fp/AiTaskModal.vue
git commit -m "feat(fp): AiTask client + useAiTaskPolling composable + AiTaskModal 组件"
```

---

### Task 27: `FpEditor.vue` 集成 `AiTaskModal`

**Files:**
- Modify: `web/src/views/FpEditor.vue`

- [ ] **Step 1: 在 FpEditor 顶部 toolbar 加按钮 + 模态**

打开 `web/src/views/FpEditor.vue`，在 `<script setup>` 段加：

```ts
import AiTaskModal from "@/components/fp/AiTaskModal.vue";
const aiModalOpen = ref(false);
```

在现有 toolbar 区（找 "AI 提取" 或 "上传文档" 按钮附近）加按钮：

```vue
<button class="btn btn-ghost" @click="aiModalOpen = true">✨ AI 提取</button>
```

在 `<template>` 末尾加模态：

```vue
<AiTaskModal v-model:open="aiModalOpen" :project-id="projectId" />
```

- [ ] **Step 2: 视觉冒烟**

```bash
cd web && pnpm dev
# 进入任意项目 FP 编辑 → 点 "AI 提取" → 模态弹出，显示 "暂无任务"
```

- [ ] **Step 3: Commit**

```bash
git add web/src/views/FpEditor.vue
git commit -m "feat(fp): FpEditor 集成 AI 提取模态对话框"
```

---

### Task 28: `AuditTimeline.vue` + `AuditView.vue` 重做 + e2e

**Files:**
- Create: `web/src/components/audit/AuditTimeline.vue`
- Modify: `web/src/views/AuditView.vue`
- Create: `web/tests/e2e/v2.2-audit-timeline.spec.ts`

- [ ] **Step 1: 创建 AuditTimeline.vue**

```vue
<script setup lang="ts">
import type { AuditEvent } from "@/api/audit";

defineProps<{ events: AuditEvent[] }>();

function typeOf(e: AuditEvent): "user" | "calc" | "ai" | "system" {
  const a = e.action ?? "";
  if (a.includes("ai") || a.includes("extract") || a.includes("allocate")) return "ai";
  if (a.includes("calc")) return "calc";
  if (a.includes("snapshot") || a.includes("migration")) return "system";
  return "user";
}

const TYPE_LABELS: Record<string, string> = {
  user: "🙋", calc: "🧮", ai: "✨", system: "⚙️",
};
const TYPE_COLORS: Record<string, string> = {
  user: "var(--accent)", calc: "var(--text-2)", ai: "var(--purple)", system: "var(--text-3)",
};
</script>

<template>
  <div class="timeline">
    <div v-for="(e, i) in events" :key="e.id ?? i" class="tl-item">
      <div class="tl-dot" :style="{ background: TYPE_COLORS[typeOf(e)] }">
        {{ TYPE_LABELS[typeOf(e)] }}
      </div>
      <div v-if="i < events.length - 1" class="tl-line" />
      <div class="tl-body">
        <div class="tl-head">
          <span class="tl-title">{{ e.action }}</span>
          <span class="tl-time mono">{{ e.created_at }}</span>
        </div>
        <div class="tl-meta">
          <span class="badge" :class="`badge-${typeOf(e) === 'ai' ? 'purple' : typeOf(e) === 'calc' ? 'blue' : ''}`">
            {{ e.action }}
          </span>
          <span class="muted">{{ e.actor ?? '系统' }}</span>
          <span class="muted mono" style="font-size: 11px">{{ e.id }}</span>
        </div>
        <div class="tl-desc">{{ e.detail ?? '' }}</div>
      </div>
    </div>
  </div>
</template>

<style scoped>
.timeline { position: relative; padding-left: 4px; }
.tl-item { position: relative; padding-left: 30px; padding-bottom: 20px; }
.tl-dot {
  position: absolute; left: 0; top: 0;
  width: 22px; height: 22px; border-radius: 50%;
  background: var(--text-3); color: white;
  display: grid; place-items: center;
  font-size: 12px;
}
.tl-line {
  position: absolute; left: 10px; top: 22px; bottom: -2px;
  width: 2px; background: var(--border);
}
.tl-body { background: transparent; }
.tl-head { display: flex; justify-content: space-between; align-items: baseline; }
.tl-title { font-weight: 500; }
.tl-time { font-size: 11px; color: var(--text-3); }
.tl-meta { display: flex; align-items: center; gap: 8px; margin: 4px 0; }
.tl-desc { font-size: 12px; color: var(--text-2); margin-top: 4px; }
</style>
```

- [ ] **Step 2: 改 AuditView.vue**

打开 `web/src/views/AuditView.vue`，整体改为：

```vue
<script setup lang="ts">
import { ref, onMounted, computed } from "vue";
import { useRoute } from "vue-router";
import { auditApi, type AuditEvent } from "@/api/audit";
import AuditTimeline from "@/components/audit/AuditTimeline.vue";
import LoadingSkeleton from "@/components/status/LoadingSkeleton.vue";

const props = defineProps<{ global?: boolean }>();
const route = useRoute();
const events = ref<AuditEvent[]>([]);
const state = ref<"idle" | "loading" | "success" | "error">("idle");

const projectId = computed<string | null>(() => {
  const id = route.params.id;
  if (typeof id === "string" && id) return id;
  return null;
});

async function load() {
  state.value = "loading";
  try {
    events.value = await auditApi.list(projectId.value);
    state.value = "success";
  } catch {
    state.value = "error";
  }
}

onMounted(load);
</script>

<template>
  <div class="page">
    <div class="page-header">
      <div>
        <h1 class="page-title tight">
          {{ global ? '全局审计日志' : '项目审计' }}
        </h1>
        <div class="page-sub">{{ events.length }} 条事件 · 不可变 append-only</div>
      </div>
    </div>
    <LoadingSkeleton v-if="state === 'loading'" />
    <div v-else-if="events.length > 0" class="card" style="padding: 20px 24px">
      <AuditTimeline :events="events" />
    </div>
    <div v-else class="card" style="padding: 40px; text-align: center; color: var(--text-3)">
      暂无审计事件
    </div>
  </div>
</template>
```

> 注：现有 `auditApi.list` 可能不支持 `projectId=null`。若需调整 API 客户端，将 `projectId.value` 传给 `list` — 看 `web/src/api/audit.ts` 的签名，必要时小改让 `null` → 全局列表（`GET /api/audit` 或现状路由复用）。

- [ ] **Step 3: e2e**

```ts
import { test, expect } from "@playwright/test";

test("AuditView timeline renders dots", async ({ page }) => {
  await page.goto("/");
  const firstRow = page.locator("tr.row-link").first();
  await firstRow.click();
  // 用 sidebar 进项目 audit
  await page.locator(".sidebar .nav-item.sub", { hasText: "审计" }).click();
  await expect(page.locator(".timeline")).toBeVisible();
  await expect(page.locator(".tl-dot").first()).toBeVisible();
});
```

- [ ] **Step 4: 跑 e2e**

```bash
cd web && pnpm playwright test tests/e2e/v2.2-audit-timeline.spec.ts
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add web/src/components/audit/AuditTimeline.vue web/src/views/AuditView.vue web/tests/e2e/v2.2-audit-timeline.spec.ts
git commit -m "feat(audit): AuditTimeline + AuditView 重做 + e2e（v2.2 P5）"
```

---

### Task 29: `ParamManager.vue` 加全局模式 + `ReportCenterView.vue` 完整实装

**Files:**
- Modify: `web/src/views/ParamManager.vue`
- Modify: `web/src/views/ReportCenterView.vue`（已在 Task 8 stub，本步完善）

- [ ] **Step 1: ParamManager 加 `projectId: string | null` prop**

打开 `web/src/views/ParamManager.vue`，找到 `defineProps`：

```ts
const props = defineProps<{ projectId: string | null }>();
```

(如果当前是 string，改为 `string | null`)

在加载 effective 参数的地方加分支：

```ts
async function loadEffective() {
  if (props.projectId) {
    return paramsApi.effectiveForProject(props.projectId);
  }
  return paramsApi.effective();  // 全局
}
```

在标题部分加全局提示：

```vue
<h1 class="page-title tight">
  <template v-if="projectId">参数管理 · {{ project?.name }}</template>
  <template v-else>全局参数库</template>
</h1>
<div class="page-sub" v-if="!projectId">全局参数（影响所有项目） · 当前基准 CSBMK®-202510</div>
```

恢复默认按钮 v-if:

```vue
<button v-if="projectId" class="btn btn-ghost">↻ 恢复默认</button>
```

> 如果 `paramsApi.effective()` 不存在，参考 `paramsApi.effectiveForProject` 的实现加一个 `effective()`：调 `GET /api/params/effective`（已在后端 `params.py:@router.get("/api/params/effective")`）。

- [ ] **Step 2: ReportCenterView 已在 T8 stub，本步不动**

- [ ] **Step 3: 视觉冒烟**

```bash
cd web && pnpm dev
# 访问 /params/global — 应展示 6 tab，标题为 "全局参数库"
# 访问 /reports — 展示项目列表 + 下载按钮
```

- [ ] **Step 4: Commit**

```bash
git add web/src/views/ParamManager.vue
git commit -m "feat(params): ParamManager 支持 projectId=null 全局模式（v2.2 P5）"
```

---

### Task 30: 全套验证 + Coverage baseline 更新 + README v2.2 章节

**Files:**
- Modify: `coverage-baseline.json`
- Modify: `README.md`

- [ ] **Step 1: 全套测试**

```bash
cd server && .venv/bin/pytest -q 2>&1 | tail -5
cd ../web && pnpm vitest run --reporter=verbose 2>&1 | tail -10
pnpm playwright test --reporter=line 2>&1 | tail -10
pnpm vue-tsc --noEmit --pretty false 2>&1 | tail -10
pnpm eslint --max-warnings 0 src 2>&1 | tail -5
```

Expected: 全 PASS（pytest 180+，vitest 230+，e2e 18+，type/lint clean）。任一失败先修。

- [ ] **Step 2: 跑 coverage baseline**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用
./scripts/check-coverage.sh
```

Expected: 输出 backend / frontend coverage 与 baseline 对比。若整体高于 baseline，跑：

```bash
./scripts/update-coverage-baseline.sh
```

更新 baseline 文件。

- [ ] **Step 3: README 加 v2.2 章节**

在 `README.md` 现有 v2.1 章节之后追加：

```markdown
## v2.2 — 设计 import 全屏重做

v2.2 把 design import（claude.ai/design）6 屏以像素级复刻落到前端，并补齐后端：

**前端：**
- 暗色 sidebar + topbar 双区布局 + ⌘K 命令面板
- ProjectList KPI cards + 城市/行业/阶段 filter + table/card 视图切换
- ResultView 9 步 Pipeline 详解 + 4 段 CostBar + 合规说明卡
- AuditView 时间轴重做
- FpEditor AI 提取模态对话框（CLI 触发 + polling 进度）

**后端：**
- `GET /api/projects/stats` — KPI 汇总（counts + 本月 P50 + MoM 增长率）
- forward calc 返回 `trace`（9 步详解）+ `composition`（4 段拆分）
- `/api/ai-tasks` CRUD endpoints — AI 任务状态轮询

**新表：** `ai_tasks`（alembic migration 自动应用）

测试基线 v2.2：~180 pytest / ~230 vitest / ~18 e2e。
```

- [ ] **Step 4: 终验**

```bash
cd /Users/renzhongyuan/WorkData/项目/2026/造价应用 && git status && git log --oneline v2.1-test-infrastructure..HEAD | wc -l
```

Expected: working tree clean，30+ commits。

- [ ] **Step 5: Commit**

```bash
git add coverage-baseline.json README.md
git commit -m "docs: v2.2 README + coverage baseline 更新（v2.2 final）"
```

---

## Self-Review

### Spec coverage

- ✅ §4 设计系统 → T1-T3
- ✅ §5 App Shell → T4-T9
- ✅ §6 ProjectList + KPI + filter → T10-T15
- ✅ §7 ResultView Pipeline/CostBar/Compliance → T16-T22
- ✅ §8 AuditView Timeline → T28
- ✅ §9 FpEditor AI 模态 + AiTask 后端 → T23-T27
- ✅ §10 Sidebar 三项递归（全局参数库 + 报告中心）→ T8 + T29
- ✅ §11 数据流（4 个 endpoint）→ T11 + T17 + T25
- ✅ §13 测试（5 e2e）→ T9, T15, T22 (×2), T28
- ✅ §15 Migration → T23

### Placeholder scan

无 "TBD/TODO/implement later" — 每个步骤有完整代码。
"占位组件" 出现 1 次（T8 ReportCenterView.vue），但同 task 给了完整 ~50 行实装，T29 进一步增强 — 不算 placeholder。

### Type consistency

- `ProjectStats` interface（T12 web client）字段与 `ProjectStats` Pydantic schema（T11）逐字段对齐 ✓
- `PipelineTrace` interface（T19 web）的 10 字段与 `trace` dict（T16 backend forward.py）逐 key 对齐 ✓
- `Composition` interface（T20 web）的 4 字段与 `composition` dict（T16）逐 key 对齐 ✓
- `AiTask` interface（T26）与 `AiTaskRead` schema（T24）字段对齐 ✓
- `AiTaskKind = "extract" | "allocate"` 在 web/backend 一致 ✓

### Risk notes

- T8 `AuditGlobalView.vue` 创建时引用 `<AuditView :global="true" />` —— 此 prop 在 T28 才加。中间状态（T9-T27 期间）访问 `/audit` 全局会 404 prop 不存在。**可接受**，因为 T9 e2e 不点 `/audit`，T28 完整修复。
- T17 conftest `sample_project` fixture 若已存在则只验证不重写 — 任务说明已显式 `grep` 验证步骤。
- T29 `paramsApi.effective()` 调用——可能需要新增方法到 `web/src/api/params.ts`。如不存在，参考 `effectiveForProject` 加 5 行就够。

---

**Plan complete.** 30 task / 5 phase / 1 PR · `v2.2-design-import-redesign` branch.
